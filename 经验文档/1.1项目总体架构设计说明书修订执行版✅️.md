# 项目总体架构设计说明书 V1.8

> **版本**: V1.9  
> **修订日期**: 2026-03-29  
> **创建日期**: 2026-03-21  
> **最终评审完成日期**: 2026-03-21  
> **性能优化集成日期**: 2026-03-28  
> **卡顿优化完成日期**: 2026-03-28  
> **Claw化最终验收日期**: 2026-03-29  
> **知识库验证器完成日期**: 2026-03-29  
> **项目**: Novel Writing Assistant-Agent Pro  
> **核心理念**: 微内核 + 插件化 + AI深度集成 + 生产级可靠性 + 长期记忆 + 统一异步架构  
> **技术栈**: Python 3.12.x + Tkinter + Pydantic v2 + LanceDB

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1.9 | 2026-03-29 | 知识库验证器整合版，新增知识库验证器插件（三层查重+五维度质量评估+Claw集成），验收评分100/100分 |
| V1.8 | 2026-03-29 | Claw化最终验收版，所有P0/P1/P2问题修复完成，验收评分95/100分，目标达成度95% |
| V1.7 | 2026-03-28 | 卡顿优化完成版，新增HF_HUB_OFFLINE修复、热榜异步化、向量模型延迟加载、性能优化验收标准 |
| V1.6 | 2026-03-28 | 性能优化集成版，新增ThreadPoolManager统一线程池管理、V5核心模块保护策略、Service层异步调度、特性开关与降级策略 |
| V1.5 | 2026-03-25 | 长期记忆架构版，新增OpenClaw mem9五层记忆架构、SESSION-STATE管理、WAL协议、本地知识库设计 |
| V1.4 | 2026-03-24 | 整合未实装功能文档，新增19.4逆向反馈、19.5续写功能、19.6快捷创作、19.7审查总结 |

### V1.4 修订内容汇总

**未实装功能整合**：
1. **逆向反馈功能**：ReverseFeedbackPlugin 接口预设、ConsistencyIssue 数据模型、SettingsCorrector 修正模块
2. **续写功能**：ContinuationPlugin 接口预设、多版本管理数据结构、流式生成支持
3. **快捷创作功能**：QuickCreationPlugin 接口预设、一键生成设定流程、导入导出支持
4. **审查总结**：P1优先级待开发工作清单

---

### V1.3 修订内容汇总

**V5强制保护机制**：
1. **五大核心功能模块保护**：大纲解析、风格学习、人物设定、世界观构建、热榜功能
2. **评分反馈循环优化生成流程保护**：核心工作流运行逻辑不可修改
3. **运行时保护机制**：PluginRegistry V1.3 保护检查

**新增功能接口预设**：
1. **创作进度管理**：ProgressManager 接口预设
2. **逆向优化**：ReverseOptimizer 接口预设
3. **一键生成前期内容**：PregenerationService 接口预设

---

## 文档定位说明

**本文档定位**：Agent Pro的架构接口规范，定义微内核架构、插件系统接口、Agent协作模式、通信协议等高层设计。

**与其他文档的关系**：
- 《✅️AgentPRO最全经验文档.md》：开发实战经验文档，包含V5→Pro升级经验、技术决策过程、踩坑总结
- 《✅️Novel Writing Assistant-Agent Pro程序框架方案_V3.0.md》：完整架构设计方案，包含实施路线图、风险与缓解措施
- 本文档侧重于接口规范和数据模型定义，作为上述两份文档的补充和统一参考

---

## 📋 目录

1. [架构总览](#一架构总览)
2. [分层架构设计](#二分层架构设计)
3. [微内核核心引擎](#三微内核核心引擎)
4. [插件系统](#四插件系统)
5. [Agent深度集成](#五agent深度集成)
6. [长期记忆架构（OpenClaw mem9）](#六长期记忆架构openclaw-mem9)
7. [AI工程接口设计](#七ai工程接口设计)
8. [通信协议与数据格式](#八通信协议与数据格式)
9. [性能优化](#九性能优化)
10. [附录：关键技术决策](#十附录关键技术决策)
11. [附录：安全与可靠性设计](#十一附录安全与可靠性设计)
12. [附录：容器化方案](#十二附录容器化方案v12新增)
13. [附录：插件代码签名机制](#十三附录插件代码签名机制v12新增)
14. [附录：架构决策记录](#十四附录架构决策记录v12新增)
15. [附录：故障排查手册](#十五附录故障排查手册v12新增)
16. [附录：风险提示与缓解措施](#十六附录风险提示与缓解措施v12新增)
17. [附录：实施检查清单](#十七附录实施检查清单v12新增)
18. [附录：性能基准测试规范](#十八附录性能基准测试规范v12新增)
19. [附录：V5强制保护机制](#十九附录v5强制保护机制v13新增)
20. [附录：新增功能接口预设](#二十附录新增功能接口预设v13新增)
21. [附录：知识库验证器](#二十一附录知识库验证器v19新增)

---

## 一、架构总览

### 1.1 架构愿景

Novel Writing Assistant-Agent Pro 采用**微内核架构**，核心系统仅提供基础调度服务（EventBus、PluginRegistry、ServiceLocator、ConfigManager），所有业务功能以插件形式存在。这种架构实现了：

- **高内聚低耦合**：模块间依赖最小化，循环依赖=0
- **热插拔支持**：插件动态加载/卸载，开发环境支持热重载
- **可扩展性**：新增功能无需修改核心代码
- **可维护性**：清晰的模块边界，便于测试和调试

### 1.2 架构分层图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户界面层 (UI Layer)                    │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────┐ │
│  │   主窗口     │ │  工作台     │ │  设置面板   │ │ 插件管理 │ │
│  │  Tkinter    │ │  Notebook   │ │             │ │         │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      应用层 (Application Layer)                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   MasterAgent                          │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐    │   │
│  │  │ Thinker │ │Optimizer│ │Validator│ │Planner  │    │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
┌───────────────────────┐ ┌───────────────┐ ┌───────────────────────┐
│   微内核核心 (Kernel)   │ │  插件系统      │ │   服务层 (Services)    │
│  ┌──────────────────┐ │ │ ┌───────────┐ │ │  ┌─────────────────┐  │
│  │  EventBus       │ │ │ │ PluginMgr │ │ │  │   AI API服务     │  │
│  │  PluginRegistry │ │ │ │ Loader    │ │ │  │  (DeepSeek等)   │  │
│  │  ServiceLocator │ │ │ │ HotSwap   │ │ │  └─────────────────┘  │
│  │  ConfigManager   │ │ │ └───────────┘ │ │  ┌─────────────────┐  │
│  └──────────────────┘ │ │               │ │  │   文件服务      │  │
└───────────────────────┘ └───────────────┘ │  │  (I/O操作)      │  │
                                    │  └─────────────────┘  │
                                    │                       │
                                    ▼                       ▼
                         ┌───────────────────────┐  ┌───────────────────────┐
                         │      插件层            │  │    基础设施层        │
                         │  [Analyzer插件]       │  │  ┌─────────────────┐  │
                         │  [Generator插件]      │  │  │ 结构化日志      │  │
                         │  [Validator插件]      │  │  │ 指标采集        │  │
                         │  [Tool插件]           │  │  │ 熔断器          │  │
                         │                       │  │  │ 持久化层        │  │
                         └───────────────────────┘  │  │ 健康检查        │  │
                                                  │  └─────────────────┘  │
                                                  └───────────────────────┘
```

**分层架构说明**：
- **UI层**：用户交互界面，基于Tkinter实现，所有UI操作必须在主线程执行
- **应用层**：业务逻辑协调，由MasterAgent和专家Agent组成
- **微内核核心层**：提供EventBus、PluginRegistry、ServiceLocator、ConfigManager等基础调度服务
- **插件系统层**：所有业务功能以插件形式实现（Analyzer、Generator、Validator、Tool等）
- **服务层**：提供AI API、文件操作、记忆管理等共享服务
- **基础设施层**：提供日志、监控、数据库、熔断器等基础能力

### 1.3 核心设计原则

#### 1.3.1 微内核原则

**定义**：核心系统仅提供最基本的服务（插件调度、事件分发、依赖管理），所有业务功能通过插件实现。

**核心服务清单**：

| 服务 | 说明 | 接口 |
|------|------|------|
| EventBus | 插件间通信 | `subscribe()`, `publish()`, `publish_sync()` |
| PluginRegistry | 插件管理 | `register()`, `unregister()`, `get_plugin()`, `activate()`, `deactivate()`, `recover()` |
| ServiceLocator | 依赖注入 | `register_service()`, `get_service()` |
| ConfigManager | 配置管理 | `get_config()`, `set_config()` |

**非核心服务（插件化）**：
- 大纲解析、风格学习、人物管理、世界观解析
- 上下文构建、迭代生成、加权验证
- 热榜功能、统计分析

#### 1.3.2 榫卯式集成原则

**核心理念**：插件与核心通过标准接口"咬合"，接口定义严格，实现灵活。

**接口设计原则**：
1. **最小化接口**：只定义必要的方法，避免过度设计
2. **向后兼容**：新增方法使用默认实现，不破坏现有插件
3. **显式依赖**：插件依赖在清单中明确声明
4. **清晰边界**：插件不直接访问核心内部状态

#### 1.3.3 事件驱动原则

**核心理念**：插件间通过事件总线通信，解耦依赖。

**事件类型**：

| 事件类型 | 说明 | 典型场景 |
|----------|------|----------|
| 系统事件 | 插件加载/卸载 | `plugin.loaded`, `plugin.unloaded`, `plugin.error`, `plugin.recovered` |
| 生成事件 | 生成流程 | `generation.started`, `generation.progress`, `generation.completed` |
| 验证事件 | 质量验证 | `validation.completed` |
| 用户事件 | 用户操作 | `user.generation_requested` |
| 配置事件 | 配置变更 | `config.changed` |

---

## 二、分层架构设计

### 2.1 用户界面层 (UI Layer)

**职责**：负责用户交互和数据展示

**核心组件**：
- 主窗口：Tkinter主窗口，sv_ttk现代化主题
- 工作台：Notebook多标签页界面
- 设置面板：配置管理界面
- 插件管理器：插件启用/禁用界面

**技术栈**：
- Tkinter + sv_ttk 2.2+
- Python 3.12.x

**Tkinter线程安全规范**（重要）：
- **所有UI操作必须在主线程执行**
- 异步事件的handler在独立线程池执行，禁止直接操作UI
- 需要更新UI时，使用`widget.after()`调度到主线程
- `publish_sync()`在调用线程执行，主线程调用时可直接操作UI

**通信方式**：
- UI → PluginManager：通过ServiceLocator获取插件
- UI → EventBus：发布用户事件（如`user.generation_requested`）
- EventBus → UI：订阅插件事件，通过`after()`安全更新UI状态

### 2.2 应用层 (Application Layer)

**职责**：业务逻辑协调和Agent编排

**核心组件**：
- **MasterAgent**：总控Agent，协调专家Agent协作
  - **Thinker**：深度思考推理（CoT、ToT）
  - **Optimizer**：迭代优化（反馈驱动、自我修正）
  - **Validator**：质量验证（多维度评分、规则检查）
  - **Planner**：任务规划（目标分解、依赖分析）

**Agent协作模式**：
1. **链式协作**：Planner → Thinker → Optimizer → Validator → Planner（循环）
2. **树状协作**：Planner分发任务到多个Thinker，汇总后Optimizer优化
3. **网状协作**：Planner与Validator持续验证，多个Thinker并行思考

**Agent执行约束**：

| Agent | 超时 | 重试 | 降级策略 |
|-------|------|------|----------|
| Thinker | 30s | 1次 | 跳过CoT，使用直接推理 |
| Optimizer | 20s | 1次 | 返回原始生成结果 |
| Validator | 15s | 0次 | 返回默认评分0.5 |
| Planner | 10s | 0次 | 使用上一次计划 |

**技术栈**：
- Python 3.12.x
- openai SDK 1.60+（统一LLM客户端）
- Pydantic 2.10.6（数据验证）

### 2.3 微内核核心层 (Kernel Core)

**职责**：提供基础调度服务

**核心组件**：
- EventBus：发布订阅机制（线程安全，真异步实现）
- PluginRegistry：插件管理（含完整状态转换）
- ServiceLocator：依赖注入（含循环依赖检测，锁内执行）
- ConfigManager：配置管理（含验证和变更通知）

**详细设计**见第三章。

### 2.4 插件系统层 (Plugin System)

**职责**：模块化功能实现

**插件类型**：
```
BasePlugin (基础插件)
    ├── ProtocolPlugin (协议插件)
    ├── AIPlugin (AI插件)
    ├── StoragePlugin (存储插件)
    ├── AnalyzerPlugin (分析器插件)
    │   ├── OutlineParserPlugin (大纲解析)
    │   ├── StyleLearnerPlugin (风格学习)
    │   ├── CharacterManagerPlugin (人物管理)
    │   └── WorldviewParserPlugin (世界观解析)
    ├── GeneratorPlugin (生成器插件)
    │   ├── ContextBuilderPlugin (上下文构建)
    │   └── NovelGeneratorPlugin (小说生成)
    ├── ValidatorPlugin (验证器插件)
    │   └── WeightedValidatorPlugin (加权验证)
    └── ToolPlugin (工具插件)
        ├── HotRankingPlugin (热榜功能)
        └── KnowledgeVerifierPlugin (知识库验证器)
```

**详细设计**见第四章。

### 2.5 服务层 (Services Layer)

**职责**：提供共享服务

**核心服务**：
- AI API服务：统一LLM客户端（DeepSeek、OpenAI、Anthropic）
- 文件服务：I/O操作封装
- 记忆服务：长期记忆管理（QClaw-memory）
- **AI工程服务**：提示词引擎、RAGPipeline、模型管理等

### 2.6 基础设施层 (Infrastructure Layer)

**职责**：提供基础能力支撑

**核心组件**：
- 结构化日志：JSON格式 + 追踪ID + 敏感信息脱敏
- 指标采集：Counter、Gauge、Histogram、Timer（支持标签）
- 熔断器：故障自动恢复（CLOSED→OPEN→HALF_OPEN→CLOSED）
- 持久化层：SQLite WAL模式 + 连接池 + 数据库迁移机制
- 健康检查：组件级健康状态 + 依赖健康传播

---

## 三、微内核核心引擎

### 3.1 事件总线 (EventBus)

#### 3.1.1 核心职责

插件间发布-订阅通信的核心机制，实现插件解耦。**必须保证线程安全**。

#### 3.1.2 关键接口

```python
# core/event_bus.py
from typing import Dict, List, Any, Callable, Optional
from dataclasses import dataclass
from abc import ABC, abstractmethod
import threading
import time
from uuid import uuid4
from concurrent.futures import ThreadPoolExecutor
from datetime import datetime, timezone

@dataclass
class Event:
    """事件对象"""
    type: str                      # 事件类型
    data: Any                      # 事件数据（支持复杂对象）
    source: Optional[str] = None    # 事件来源
    timestamp: Optional[float] = None  # 时间戳
    event_id: Optional[str] = None     # 事件ID

    def __post_init__(self):
        if self.timestamp is None:
            self.timestamp = datetime.now(timezone.utc).timestamp()
        if self.event_id is None:
            self.event_id = f"evt-{uuid4().hex[:8]}"

class EventBus:
    """事件总线 - 线程安全，真异步实现"""
    
    def __init__(self, max_workers: int = 10):
        self._handlers: Dict[str, List[Dict[str, Any]]] = {}
        self._lock = threading.RLock()
        self._subscriptions: Dict[str, str] = {}
        self._executor = ThreadPoolExecutor(max_workers=max_workers)
        self._dead_letter_queue: List[Dict] = []
    
    def subscribe(self, event_type: str, handler: Callable, 
                   handler_id: str = None) -> str:
        """订阅事件，返回订阅ID"""
        if handler_id is None:
            handler_id = f"handler-{uuid4().hex[:8]}"
        
        with self._lock:
            if event_type not in self._handlers:
                self._handlers[event_type] = []
            
            self._handlers[event_type].append({
                "id": handler_id,
                "handler": handler,
            })
            self._subscriptions[handler_id] = event_type
        
        return handler_id
    
    def unsubscribe(self, subscription_id: str) -> bool:
        """取消订阅"""
        with self._lock:
            event_type = self._subscriptions.get(subscription_id)
            if not event_type:
                return False
            
            handlers = self._handlers.get(event_type, [])
            self._handlers[event_type] = [
                h for h in handlers if h["id"] != subscription_id
            ]
            
            del self._subscriptions[subscription_id]
            return True
    
    def publish(self, event_type: str, data: Any, source: str = None):
        """发布事件 - 真异步，使用线程池执行"""
        event = Event(type=event_type, data=data, source=source)
        
        with self._lock:
            handlers = list(self._handlers.get(event_type, []))
        
        # 提交到线程池异步执行
        for handler_info in handlers:
            self._executor.submit(self._safe_invoke, handler_info, event)
    
    def _safe_invoke(self, handler_info: Dict, event: Event):
        """安全调用handler，带异常处理和超时控制"""
        handler_id = handler_info["id"]
        handler = handler_info["handler"]
        
        try:
            handler(event)
        except Exception as e:
            # 记录到日志，不传播异常
            print(f"事件处理失败 {event.type} [handler={handler_id}]: {e}")
            # 添加到死信队列
            self._dead_letter_queue.append({
                "event": event,
                "handler_id": handler_id,
                "error": str(e),
                "timestamp": time.time()
            })
    
    def publish_sync(self, event_type: str, data: Any,
                     timeout: float = 5.0) -> List[Any]:
        """同步发布事件，等待所有处理器返回结果"""
        event = Event(type=event_type, data=data)
        results = []
        
        with self._lock:
            handlers = list(self._handlers.get(event_type, []))
        
        for handler_info in handlers:
            try:
                result = handler_info["handler"](event)
                results.append(result)
            except Exception as e:
                print(f"事件处理失败 {event_type}: {e}")
        
        return results
```

**使用示例**：
```python
# 订阅事件
def on_generation_started(event: Event):
    print(f"生成开始: {event.data['chapter_title']}")

event_bus.subscribe(EVENT_GENERATION_STARTED, on_generation_started)

# 发布事件（真异步，立即返回）
event_bus.publish(EVENT_GENERATION_STARTED, {
    "chapter_title": "第一章",
    "word_count_target": 2000
}, source="UI")

# 同步发布（主线程中可安全操作UI）
results = event_bus.publish_sync(EVENT_USER_GENERATION_REQUESTED, {
    "chapter": "第一章"
})
```

#### 3.1.3 性能优化

**异步事件处理**：使用线程池处理高并发事件，publish()立即返回

**批量处理**：同类事件批量提交，减少开销

**可靠事件总线增强**：
- 失败事件最多重试3次
- 超时控制默认5秒
- 死信队列（最大1000条）
- 连续失败的handler触发熔断机制

### 3.2 插件注册表 (PluginRegistry)

#### 3.2.1 核心职责

管理所有已加载的插件，提供查询、状态转换和插槽绑定功能。

#### 3.2.2 关键接口

```python
# core/plugin_registry.py
from typing import Dict, List, Optional
from enum import Enum
import threading

class PluginState(Enum):
    """插件状态"""
    LOADED = "loaded"      # 已加载（初始化完成）
    ACTIVE = "active"      # 已激活（可执行）
    ERROR = "error"        # 错误状态
    UNLOADING = "unloading"  # 卸载中

class PluginRegistry:
    """插件注册表 - 含完整状态管理"""
    
    def __init__(self):
        self._plugins: Dict[str, BasePlugin] = {}
        self._states: Dict[str, PluginState] = {}
        self._slots: Dict[str, Optional[str]] = {}
        self._lock = threading.RLock()
        self._error_counts: Dict[str, int] = {}
        self._max_retries = 3
    
    def register(self, plugin: BasePlugin) -> bool:
        """注册插件"""
        plugin_id = plugin.metadata.id
        
        with self._lock:
            if plugin_id in self._plugins:
                return False
            
            self._plugins[plugin_id] = plugin
            self._states[plugin_id] = PluginState.LOADED
            self._error_counts[plugin_id] = 0
            return True
    
    def unregister(self, plugin_id: str) -> bool:
        """注销插件"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            
            # 解绑插槽
            for slot_id, pid in self._slots.items():
                if pid == plugin_id:
                    self._slots[slot_id] = None
            
            del self._plugins[plugin_id]
            del self._states[plugin_id]
            del self._error_counts[plugin_id]
            return True
    
    def get_plugin(self, plugin_id: str) -> Optional[BasePlugin]:
        """获取插件实例"""
        return self._plugins.get(plugin_id)
    
    def get_plugins_by_type(self, plugin_type: PluginType) -> List[BasePlugin]:
        """按类型获取插件"""
        return [
            plugin for plugin in self._plugins.values()
            if plugin.metadata.plugin_type == plugin_type
        ]
    
    def get_state(self, plugin_id: str) -> Optional[PluginState]:
        """获取插件当前状态"""
        return self._states.get(plugin_id)
    
    def _validate_transition(self, plugin_id: str,
                            target_state: PluginState) -> bool:
        """验证状态转换是否合法"""
        valid_transitions = {
            PluginState.LOADED: {PluginState.ACTIVE, PluginState.UNLOADING, PluginState.ERROR},
            PluginState.ACTIVE: {PluginState.LOADED, PluginState.UNLOADING, PluginState.ERROR},
            PluginState.ERROR: {PluginState.LOADED, PluginState.UNLOADING},
            PluginState.UNLOADING: set(),  # 终态，下一步是移除
        }
        current = self._states.get(plugin_id)
        return target_state in valid_transitions.get(current, set())
    
    def activate(self, plugin_id: str) -> bool:
        """激活插件（LOADED → ACTIVE）"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            if not self._validate_transition(plugin_id, PluginState.ACTIVE):
                return False
            self._states[plugin_id] = PluginState.ACTIVE
            return True
    
    def deactivate(self, plugin_id: str) -> bool:
        """停用插件（ACTIVE → LOADED）"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            if not self._validate_transition(plugin_id, PluginState.LOADED):
                return False
            self._states[plugin_id] = PluginState.LOADED
            return True
    
    def set_error(self, plugin_id: str, error: Exception) -> bool:
        """设置插件错误状态"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            if not self._validate_transition(plugin_id, PluginState.ERROR):
                return False
            self._states[plugin_id] = PluginState.ERROR
            self._error_counts[plugin_id] = self._error_counts.get(plugin_id, 0) + 1
            return True
    
    def recover(self, plugin_id: str) -> bool:
        """从ERROR状态恢复插件（ERROR → LOADED）"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            if self._states.get(plugin_id) != PluginState.ERROR:
                return False
            
            # 检查重试次数
            count = self._error_counts.get(plugin_id, 0)
            if count >= self._max_retries:
                return False  # 超过最大重试次数
            
            plugin = self._plugins[plugin_id]
            try:
                success = plugin.initialize(self._context)
                if success:
                    self._states[plugin_id] = PluginState.LOADED
                    return True
            except Exception as e:
                self._error_counts[plugin_id] = count + 1
            
            return False
    
    def get_error_plugins(self) -> List[str]:
        """获取所有处于ERROR状态的插件ID"""
        with self._lock:
            return [pid for pid, state in self._states.items() if state == PluginState.ERROR]
    
    def get_slot_plugin(self, slot_id: str) -> Optional[BasePlugin]:
        """获取插槽当前绑定的插件"""
        plugin_id = self._slots.get(slot_id)
        if plugin_id:
            return self._plugins.get(plugin_id)
        return None
    
    def bind_slot(self, slot_id: str, plugin_id: str) -> bool:
        """绑定插件到插槽"""
        with self._lock:
            if plugin_id not in self._plugins:
                return False
            self._slots[slot_id] = plugin_id
            return True
```

**状态转换图（V1.1修订）**：
```
LOADED → ACTIVE        (activate成功)
ACTIVE → LOADED        (deactivate成功)
LOADED → ERROR         (initialize异常)
ACTIVE → ERROR         (execute连续失败)
ERROR → LOADED         (recover成功)
LOADED → UNLOADING     (卸载请求)
ACTIVE → UNLOADING     (卸载请求，需先deactivate)
UNLOADING → (removed)  (shutdown+cleanup完成)
```

### 3.3 服务定位器 (ServiceLocator)

#### 3.3.1 核心职责

提供核心服务的依赖注入，支持循环依赖检测。

#### 3.3.2 关键接口

```python
# core/service_locator.py
from typing import Dict, Type, Any, Set, Optional
import threading

class CircularDependencyError(Exception):
    """循环依赖异常"""
    pass

class ServiceLocator:
    """服务定位器 - 含循环依赖检测"""
    
    def __init__(self):
        self._services: Dict[Type, Any] = {}
        self._lock = threading.RLock()
    
    def register_service(self, service_type: Type, instance: Any):
        """注册服务实例"""
        with self._lock:
            self._services[service_type] = instance
            
            # 在锁内检测循环依赖
            if hasattr(instance, 'dependencies'):
                if self._check_circular_dependency_locked(service_type):
                    del self._services[service_type]
                    raise CircularDependencyError(
                        f"检测到循环依赖，涉及服务: {service_type.__name__}"
                    )
    
    def get_service(self, service_type: Type) -> Optional[Any]:
        """获取服务实例"""
        return self._services.get(service_type)
    
    def has_service(self, service_type: Type) -> bool:
        """检查服务是否存在"""
        return service_type in self._services
    
    def _check_circular_dependency_locked(self, service_type: Type,
                                          path: Set[Type] = None) -> bool:
        """检查循环依赖"""
        if path is None:
            path = set()
        
        if service_type in path:
            return True
        
        path.add(service_type)
        
        service = self._services.get(service_type)
        if service and hasattr(service, 'dependencies'):
            for dep_type in service.dependencies:
                if self._check_circular_dependency_locked(dep_type, path.copy()):
                    return True
        
        path.discard(service_type)
        return False
```

### 3.4 配置管理器 (ConfigManager)

#### 3.4.1 核心职责

统一配置管理，支持YAML/JSON双格式和环境变量覆盖，含配置变更通知。

#### 3.4.2 配置结构

```yaml
# config.yaml
app:
  name: "Novel Writing Assistant-Agent Pro"
  version: "6.0.0"

ai:
  provider: "deepseek"
  model: "deepseek-chat"
  temperature: 0.7
  max_tokens: 4096

plugins:
  directories:
    - "plugins"
    - "~/.agent-pro/plugins"
  hot_reload: true
  hot_reload_debounce: 1.0

logging:
  level: "INFO"
  format: "structured"
  output_dir: "日志"

database:
  path: "配置/app_data.db"
  wal_mode: true
```

#### 3.4.3 关键接口（V1.1修订：添加验证和写锁）

```python
# core/config_manager.py
from typing import Any, Dict, Optional, TYPE_CHECKING
import yaml
import json
import os
from pathlib import Path
import threading

if TYPE_CHECKING:
    from .event_bus import EventBus

# 配置验证模式
CONFIG_SCHEMA = {
    "ai.temperature": {"type": float, "range": (0.0, 2.0)},
    "ai.max_tokens": {"type": int, "range": (1, 100000)},
    "ai.timeout": {"type": int, "range": (1, 300)},
    "plugins.hot_reload_debounce": {"type": float, "range": (0.1, 10.0)},
}

class ConfigValidationError(Exception):
    """配置验证错误"""
    pass

class ConfigManager:
    """配置管理器 - 含变更通知和验证"""

    def __init__(self, config_path: str = "config.yaml", event_bus: 'EventBus' = None):
        self.config_path = Path(config_path)
        self._config: Dict[str, Any] = {}
        self._event_bus = event_bus
        self._lock = threading.RLock()
        self._write_lock = threading.Lock()  # 写锁保护
        self._load_config()
    
    def _load_config(self):
        """加载配置文件"""
        if not self.config_path.exists():
            return
        
        with open(self.config_path, 'r', encoding='utf-8') as f:
            if self.config_path.suffix in ['.yaml', '.yml']:
                self._config = yaml.safe_load(f)
            elif self.config_path.suffix == '.json':
                self._config = json.load(f)
        
        self._apply_env_overrides()
    
    def _apply_env_overrides(self):
        """应用环境变量覆盖"""
        prefix = "AGENT_"
        for key, value in os.environ.items():
            if key.startswith(prefix):
                config_key = key[len(prefix):].lower().replace('_', '.')
                self._set_nested(config_key, value)
    
    def _set_nested(self, key_path: str, value: Any):
        """设置嵌套配置"""
        keys = key_path.split('.')
        current = self._config
        for key in keys[:-1]:
            if key not in current:
                current[key] = {}
            current = current[key]
        current[keys[-1]] = value
    
    def get_config(self, key_path: str, default: Any = None) -> Any:
        """获取配置"""
        keys = key_path.split('.')
        current = self._config
        for key in keys:
            if isinstance(current, dict) and key in current:
                current = current[key]
            else:
                return default
        return current
    
    def _validate_config(self, key_path: str, value: Any):
        """验证配置值"""
        schema = CONFIG_SCHEMA.get(key_path)
        if not schema:
            return
        
        expected_type = schema.get("type")
        if expected_type and not isinstance(value, expected_type):
            raise ConfigValidationError(
                f"配置项 {key_path} 类型错误: 期望 {expected_type.__name__}, 实际 {type(value).__name__}"
            )
        
        value_range = schema.get("range")
        if value_range:
            min_val, max_val = value_range
            if not (min_val <= value <= max_val):
                raise ConfigValidationError(
                    f"配置项 {key_path} 超出范围: 期望 [{min_val}, {max_val}], 实际 {value}"
                )
    
    def set_config(self, key_path: str, value: Any):
        """设置配置"""
        # 验证配置值
        self._validate_config(key_path, value)

        with self._write_lock:
            old_value = self.get_config(key_path)
            self._set_nested(key_path, value)
            self._save_config_atomic()
        
        # 发布配置变更事件
        if self._event_bus:
            self._event_bus.publish("config.changed", {
                "key": key_path,
                "old_value": old_value,
                "new_value": value
            }, source="ConfigManager")
    
    def _save_config_atomic(self):
        """原子写入配置文件"""
        temp_path = self.config_path.with_suffix('.tmp')
        
        with open(temp_path, 'w', encoding='utf-8') as f:
            if self.config_path.suffix in ['.yaml', '.yml']:
                yaml.safe_dump(self._config, f, allow_unicode=True)
            elif self.config_path.suffix == '.json':
                json.dump(self._config, f, ensure_ascii=False, indent=2)
        
        # 原子重命名
        temp_path.replace(self.config_path)
```

---

## 四、插件系统

### 4.1 插件类型体系

同2.4节所示。

### 4.2 插件接口标准

#### 4.2.1 基础插件接口 (BasePlugin)

```python
# core/plugin_interface.py
from abc import ABC, abstractmethod
from typing import Dict, Any, List, Optional
from dataclasses import dataclass, field
from enum import Enum
from datetime import datetime, timezone
from pydantic import BaseModel, Field, ConfigDict

class PluginType(Enum):
    """插件类型枚举"""
    PROTOCOL = "protocol"
    AI = "ai"
    STORAGE = "storage"
    ANALYZER = "analyzer"
    GENERATOR = "generator"
    VALIDATOR = "validator"
    TOOL = "tool"

@dataclass
class PluginMetadata:
    """插件元数据"""
    id: str                          # 唯一标识符（如：outline-parser-v3）
    name: str                        # 显示名称
    version: str                     # 版本号（语义化版本）
    description: str                 # 描述
    author: str                     # 作者
    plugin_type: PluginType          # 插件类型
    api_version: str = "1.0"         # API版本
    priority: int = 100              # 加载优先级（越小越先）
    enabled: bool = True             # 是否启用
    dependencies: List[str] = field(default_factory=list)  # 依赖插件ID列表
    conflicts: List[str] = field(default_factory=list)     # 冲突插件ID列表
    permissions: List[str] = field(default_factory=list)   # 所需权限
    min_platform_version: str = "6.0.0"  # 最低平台版本

@dataclass
class PluginContext:
    """插件上下文"""
    event_bus: 'EventBus'
    service_locator: 'ServiceLocator'
    config_manager: 'ConfigManager'
    plugin_registry: 'PluginRegistry'
    logger: 'StructuredLogger'

class BasePlugin(ABC):
    """插件基类 - 所有插件必须继承"""

    def __init__(self, metadata: PluginMetadata):
        self.metadata = metadata
        self._context: Optional[PluginContext] = None
        self._cleanup_done = False
    
    @classmethod
    @abstractmethod
    def get_metadata(cls) -> PluginMetadata:
        """获取插件元数据"""
        pass
    
    @abstractmethod
    def initialize(self, context: PluginContext) -> bool:
        """初始化插件"""
        self._context = context
        return True
    
    def execute(self, *args, **kwargs) -> Any:
        """【可选】通用执行入口 - 请使用专用接口

        注意：专用插件应使用具体接口方法：
        - AnalyzerPlugin.analyze()
        - GeneratorPlugin.generate()
        - ValidatorPlugin.validate()
        """
        raise NotImplementedError(
            f"插件 {self.metadata.id} 未实现execute()方法，"
            f"请使用专用接口：analyze/generate/validate"
        )
    
    @abstractmethod
    def shutdown(self) -> bool:
        """关闭插件 - 优雅关闭，释放资源"""
        return True
    
    def cleanup(self) -> bool:
        """强制资源回收 - 与shutdown区分"""
        self._cleanup_done = True
        return True
```

#### 4.2.2 Pydantic数据模型

```python
# core/models.py
from pydantic import BaseModel, Field, ConfigDict, field_validator
from typing import Dict, Any, List, Optional
from datetime import datetime, timezone
from uuid import uuid4

class GenerationRequest(BaseModel):
    """生成请求模型"""
    model_config = ConfigDict(from_attributes=True)
    
    request_id: str = Field(
        default_factory=lambda: f"gen-{uuid4().hex[:8]}",
        description="请求唯一标识"
    )
    chapter_title: str = Field(..., description="章节标题")
    chapter_outline: str = Field(..., description="章节大纲")
    word_count_target: int = Field(default=2000, ge=100, le=10000)
    max_iterations: int = Field(default=5, ge=1, le=10)
    score_threshold: float = Field(default=0.8, ge=0.0, le=1.0)
    
    @field_validator('word_count_target')
    @classmethod
    def validate_word_count(cls, v: int) -> int:
        if v < 100 or v > 10000:
            raise ValueError('字数目标必须在100-10000之间')
        return v

class GenerationResult(BaseModel):
    """生成结果模型 - 使用timezone.utc"""
    model_config = ConfigDict(from_attributes=True)
    
    content: str = Field(..., description="生成内容")
    word_count: int = Field(..., description="实际字数")
    iteration_count: int = Field(..., description="迭代次数")
    final_score: float = Field(..., ge=0.0, le=1.0, description="最终评分")
    scores: Dict[str, float] = Field(..., description="各维度评分")
    timestamp: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc), 
        description="生成时间"
    )
    
    @field_validator('final_score')
    @classmethod
    def validate_score(cls, v: float) -> float:
        if not 0.0 <= v <= 1.0:
            raise ValueError('评分必须在0.0-1.0之间')
        return v

class ValidationScores(BaseModel):
    """验证评分模型 - V2.0版本：9维度智能评分"""
    model_config = ConfigDict(from_attributes=True)
    
    # 九维度评分（V2.0版本）
    worldview_score: float = Field(ge=0.0, le=1.0, description="世界观一致性")
    character_score: float = Field(ge=0.0, le=1.0, description="人设一致性")
    outline_score: float = Field(ge=0.0, le=1.0, description="大纲符合性")
    style_score: float = Field(ge=0.0, le=1.0, description="风格一致性")
    knowledge_reference_score: float = Field(default=0.5, ge=0.0, le=1.0, description="知识库引用")
    writing_technique_score: float = Field(default=0.5, ge=0.0, le=1.0, description="写作技巧")
    word_count_score: float = Field(ge=0.0, le=1.0, description="字数符合性")
    context_coherence_score: float = Field(default=0.5, ge=0.0, le=1.0, description="上下文衔接")
    ai_feeling_score: float = Field(default=0.5, ge=0.0, le=1.0, description="AI感（越低越好）")
    
    # 优化建议（V2.0新增）
    optimization_suggestions: List[str] = Field(default_factory=list, description="切实可执行的优化建议")
    modification_examples: List[str] = Field(default_factory=list, description="修改示例")
    
    @property
    def total_score(self) -> float:
        """计算总评分（9维度加权 - V2.0版本）"""
        return (
            self.worldview_score * 0.12 +
            self.character_score * 0.19 +
            self.outline_score * 0.13 +
            self.style_score * 0.19 +
            self.knowledge_reference_score * 0.08 +
            self.writing_technique_score * 0.08 +
            self.word_count_score * 0.08 +
            self.context_coherence_score * 0.08 +
            (1.0 - self.ai_feeling_score) * 0.05  # AI感越低越好
        )

class PluginEvent(BaseModel):
    """插件事件模型 - 使用timezone.utc"""
    model_config = ConfigDict(from_attributes=True)
    
    event_type: str = Field(..., description="事件类型")
    plugin_id: str = Field(..., description="插件ID")
    data: Dict[str, Any] = Field(default_factory=dict, description="事件数据")
    timestamp: datetime = Field(
        default_factory=lambda: datetime.now(timezone.utc)
    )
```

#### 4.2.3 专用插件接口

```python
# core/analyzer_plugin.py
from typing import Dict, Any, List, Optional

class AnalyzerPlugin(BasePlugin):
    """分析器插件接口"""
    
    @abstractmethod
    def analyze(self, content: str, options: Dict[str, Any] = None) -> Dict[str, Any]:
        """分析内容并返回结果"""
        pass
    
    @abstractmethod
    def get_supported_formats(self) -> List[str]:
        """获取支持的输入格式"""
        pass
    
    @abstractmethod
    def get_analysis_types(self) -> List[str]:
        """获取支持的分析类型"""
        pass

# core/generator_plugin.py
class GeneratorPlugin(BasePlugin):
    """生成器插件接口"""
    
    @abstractmethod
    def generate(self, request: GenerationRequest) -> GenerationResult:
        """生成内容 - 使用GenerationRequest参数"""
        pass
    
    @abstractmethod
    def validate_request(self, request: GenerationRequest) -> tuple[bool, List[str]]:
        """验证请求是否有效"""
        pass
    
    @abstractmethod
    def get_generation_options(self) -> Dict[str, Any]:
        """获取生成选项定义"""
        pass

# core/validator_plugin.py
class ValidatorPlugin(BasePlugin):
    """验证器插件接口"""
    
    @abstractmethod
    def validate(self, content: str, context: Dict[str, Any]) -> ValidationScores:
        """验证内容并返回评分"""
        pass
    
    @abstractmethod
    def get_validation_dimensions(self) -> List[str]:
        """获取验证维度"""
        pass
```

### 4.3 插件加载机制

#### 4.3.1 加载流程

```
发现阶段 (Discovery)
    ↓
    扫描插件目录
    解析plugin.json
    检查依赖关系
    ↓
加载阶段 (Loading)
    ↓
    动态导入模块
    验证接口兼容
    创建插件实例
    ↓
初始化阶段 (Initialization)
    ↓
    调用initialize()
    注册到Registry
    激活插件（LOADED → ACTIVE）
    绑定事件监听
    ↓
完成
```

#### 4.3.2 依赖解析算法（拓扑排序）

```python
# core/plugin_dependency_resolver.py
from typing import Dict, List, Set, Tuple, Optional
from enum import Enum

class DependencyError(Enum):
    CIRCULAR = "circular_dependency"
    MISSING = "missing_dependency"
    VERSION_CONFLICT = "version_conflict"
    CONFLICT_PLUGIN = "conflicting_plugin"

class DependencyResolver:
    """插件依赖解析器 - 拓扑排序"""
    
    def resolve(self, plugins: Dict[str, PluginMetadata]) -> Tuple[List[str], List[Dict]]:
        """
        解析插件依赖，返回加载顺序和错误列表
        使用Kahn算法进行拓扑排序
        """
        # 构建依赖图
        graph: Dict[str, Set[str]] = {}  # plugin_id -> dependencies
        in_degree: Dict[str, int] = {}
        errors: List[Dict] = []
        
        # 初始化入度为依赖数量
        for plugin_id, metadata in plugins.items():
            graph[plugin_id] = set(metadata.dependencies)
            in_degree[plugin_id] = len(metadata.dependencies)
        
        # 检查缺失依赖
        for plugin_id, deps in graph.items():
            for dep_id in deps:
                if dep_id not in graph:
                    errors.append({
                        "type": DependencyError.MISSING,
                        "plugin": plugin_id,
                        "dependency": dep_id
                    })
        
        # 拓扑排序：入度为0的节点（无未满足依赖）先加载
        queue = [pid for pid, degree in in_degree.items() if degree == 0]
        sorted_plugins = []
        
        while queue:
            current = queue.pop(0)
            sorted_plugins.append(current)
            
            # 遍历依赖当前插件的其他插件
            for plugin_id, deps in graph.items():
                if current in deps:
                    in_degree[plugin_id] -= 1
                    if in_degree[plugin_id] == 0:
                        queue.append(plugin_id)
        
        # 检测循环依赖
        if len(sorted_plugins) != len(plugins):
            remaining = set(plugins.keys()) - set(sorted_plugins)
            errors.append({
                "type": DependencyError.CIRCULAR,
                "plugins": list(remaining)
            })
        
        return sorted_plugins, errors
```

#### 4.3.3 热插拔机制

**实现方式**：使用watchdog监控插件目录变化

**关键特性**：
- 防抖重载（1秒内多次修改只重载一次）
- 自动依赖解析（拓扑排序）
- 资源清理（事件监听器、模块缓存）
- 插件沙箱隔离（安全分级策略）

**热插拔权限控制分级策略（V1.2新增）**：

| 安全等级 | 权限范围 | 可操作范围 | 适用场景 |
|----------|----------|------------|----------|
| L0-信任 | 完全信任 | 所有操作 | 官方插件、系统核心 |
| L1-受限 | 受限操作 | 沙箱内执行 | 审核通过第三方插件 |
| L2-隔离 | 最小权限 | 进程隔离执行 | 未知来源插件 |
| L3-禁止 | 无权限 | 禁止加载 | 风险插件 |

```python
class HotSwapPermission:
    """热插拔权限控制"""
    
    SECURITY_LEVELS = {
        "L0": {"can_reload": True, "can_load": True, "can_unload": True, "process_isolation": False},
        "L1": {"can_reload": True, "can_load": True, "can_unload": False, "process_isolation": False},
        "L2": {"can_reload": False, "can_load": True, "can_unload": False, "process_isolation": True},
        "L3": {"can_reload": False, "can_load": False, "can_unload": False, "process_isolation": False},
    }
    
    def __init__(self, plugin_id: str, signature_verified: bool = False):
        self.plugin_id = plugin_id
        self._security_level = self._determine_level(signature_verified)
    
    def _determine_level(self, signature_verified: bool) -> str:
        """确定插件安全等级"""
        if not signature_verified:
            return "L3"  # 未签名禁止加载
        # 已签名插件根据来源确定等级
        # 此处可根据实际需求扩展
        return "L1"  # 默认信任级别
    
    def can_reload(self) -> bool:
        return self.SECURITY_LEVELS[self._security_level]["can_reload"]
    
    def can_load(self) -> bool:
        return self.SECURITY_LEVELS[self._security_level]["can_load"]
    
    def requires_isolation(self) -> bool:
        return self.SECURITY_LEVELS[self._security_level]["process_isolation"]
```

**加载策略**：
1. 优先加载高安全级别插件（L0 > L1 > L2）
2. L2级别插件使用独立进程加载
3. L3级别插件直接拒绝加载并记录日志
4. 热重载仅允许L0/L1级别插件

### 4.4 插件目录结构

```
plugins/{plugin-id}/
    ├── plugin.json                 # 插件清单（必需）
    ├── __init__.py                 # 插件入口（必需）
    ├── plugin.py                   # 插件主类（推荐）
    ├── config_schema.json          # 配置模式定义（可选）
    ├── requirements.txt            # 依赖清单（可选）
    ├── README.md                   # 插件文档（推荐）
    ├── core/                       # 核心实现
    │   └── ...
    ├── ui/                         # UI组件（如需要）
    │   └── ...
    ├── assets/                     # 静态资源
    │   └── ...
    └── tests/                      # 测试代码
        └── ...
```

**plugin.json示例**：
```json
{
  "id": "outline-parser-v3",
  "name": "大纲解析器 V3",
  "version": "3.0.0",
  "plugin_type": "analyzer",
  "author": "项目组",
  "description": "基于LangChain的智能大纲解析",
  "api_version": "1.0",
  "priority": 100,
  "enabled": true,
  "dependencies": [],
  "conflicts": [],
  "permissions": ["file.read"],
  "min_platform_version": "6.0.0",
  "entry_class": "OutlineParserPlugin"
}
```

### 4.5 V5核心模块迁移路径

**现有模块迁移映射表**：

| V5核心模块 | 文件路径 | 迁移为插件 | 插件类型 | 迁移状态 |
|------------|----------|------------|----------|----------|
| 大纲解析 | `scripts/outline_parser_v3.py` | OutlineParserPlugin | Analyzer | 待迁移 |
| 风格学习 | `scripts/enhanced_style_learner_v2.py` | StyleLearnerPlugin | Analyzer | 待迁移 |
| 人物管理 | `scripts/character_manager/` | CharacterManagerPlugin | Analyzer | 待迁移 |
| 世界观解析 | `scripts/universal_worldview_parser.py` | WorldviewParserPlugin | Analyzer | 待迁移 |
| 上下文构建 | `scripts/context_builder.py` | ContextBuilderPlugin | Generator | 待迁移 |
| 迭代生成 | `scripts/iterative_generator_v2.py` | IterativeGeneratorPlugin | Generator | 待迁移 |
| 加权验证 | `scripts/enhanced_weighted_validator.py` | WeightedValidatorPlugin | Validator | 待迁移 |
| 热榜功能 | `scripts/hot_ranking/` | HotRankingPlugin | Tool | 待迁移 |

**迁移原则**：
- 保持核心功能不变
- 保留接口兼容性
- 逐步迁移，测试验证

---

## 五、Agent深度集成

### 5.1 MasterAgent架构

**核心职责**：协调多个专家Agent协作完成复杂任务

**专家Agent清单**：

| Agent | 职责 | 关键能力 |
|-------|------|----------|
| Thinker | 深度思考推理 | CoT（思维链）、ToT（思维树） |
| Optimizer | 迭代优化 | 反馈驱动、自我修正 |
| Validator | 质量验证 | 多维度评分、规则检查 |
| Planner | 任务规划 | 目标分解、依赖分析 |

### 5.2 Agent协作模式

#### 5.2.1 链式协作

```
Planner → Thinker → Optimizer → Validator → Planner（循环）
```

适用场景：线性任务，需要逐步验证和优化。

#### 5.2.2 树状协作

```
Planner
    ├── Thinker (章节大纲)
    ├── Thinker (角色设计)
    └── Thinker (世界观)
        ↓
    Optimizer
        ↓
    Validator
```

适用场景：需要并行处理多个子任务。

#### 5.2.3 网状协作

```
┌─────────┐
│ Planner │ ←──→ Validator (持续验证)
└────┬────┘
     │
     ├─────── Thinker (并行思考多个方面)
     │
     ↓
  Optimizer
```

适用场景：复杂任务，需要多角度思考。

### 5.3 Agent与插件系统对接

**对接原则**：
1. Agent能力包装为插件（AIPlugin）
2. Agent调用通过PluginManager
3. Agent状态持久化到SQLite

**示例**：
```python
# ThinkerAgent包装为插件
class ThinkerAgentPlugin(AIPlugin):
    def generate_text(self, prompt: str, context: Dict[str, Any]) -> str:
        # 调用MasterAgent的Thinker
        result = self._master_agent.think(prompt, context)
        return result

# 在GeneratorPlugin中使用
class NovelGeneratorPlugin(GeneratorPlugin):
    def generate(self, request: GenerationRequest) -> GenerationResult:
        # 通过PluginManager获取Thinker
        thinker = self._context.plugin_registry.get_plugin("thinker-agent")
        outline = thinker.generate_text(outline_prompt, request.model_dump())
        # ...
```

---

## 六、长期记忆架构（OpenClaw mem9）

> **版本**: V1.1
> **规划日期**: 2026-03-25
> **规划者**: 高级项目经理
> **参考文档**: 《10.升级方案✅️.md》

### ⚠️ 记忆文件位置规范（重要）

项目存在两类记忆文件，**必须严格区分**：

| 记忆类型 | 存储位置 | 用途 |
|----------|----------|------|
| **CodeBuddy构建记忆** | `.workbuddy/memory/` | AI Agent构建软件时的会话记忆、工作日志、决策记录 |
| **软件Claw化运行记忆** | `Memory-Novel Writing Assistant-Agent Pro/` | 软件运行时产生的用户数据、创作记忆、优化历史 |

**详细说明**：

**1. CodeBuddy构建记忆** (`.workbuddy/memory/`)
- `MEMORY.md` - 项目长期记忆（跨会话持久化）
- `YYYY-MM-DD.md` - 每日工作日志
- 用途：记录AI如何构建这个软件、做出的决策、遇到的问题

**2. 软件Claw化运行记忆** (`Memory-Novel Writing Assistant-Agent Pro/`)
- 四层记忆架构的实际数据存储
- L1热记忆：当前会话缓存
- L2温记忆：向量存储索引
- L3冷记忆：Git-Notes历史
- L4档案：长期知识沉淀
- 用途：软件运行时的智能记忆系统，越用越聪明

**注意**: 所有Claw化系统产生的记忆文件应保存到 `Memory-Novel Writing Assistant-Agent Pro/`，而非 `.workbuddy/memory/`

### 6.1 架构设计哲学

**核心原则：文件即真相源（File is the Source of Truth）**

OpenClaw mem9框架建立在纯文本Markdown文件之上：
1. **纯文本驱动**：所有记忆以Markdown格式存储，透明可编辑
2. **不重新训练模型**：通过写入和检索记忆文件来适应，而非改动底层LLM
3. **向量索引非真相源**：文件本身是唯一真相，向量索引仅用于加速检索

### 6.2 五层记忆架构

| 层级 | 存储介质 | 内容类型 | 更新频率 | 检索方式 |
|:---:|:---|:---|:---|:---|
| **L1: 热记忆** | `SESSION-STATE.md` | 当前会话状态、活跃任务 | 实时更新 | 直接读取 |
| **L2: 温记忆** | LanceDB向量库 | 章节向量、知识库向量 | 增量更新 | 向量检索 |
| **L3: 冷记忆** | Git-Notes | 历史决策、里程碑事件 | 按需归档 | Git log检索 |
| **L4: 精选档案** | `MEMORY.md` | 长期事实、关键决策 | 每日冥想 | 手动维护 |
| **L5: 云备份** | 外部存储 | 完整workspace镜像 | 每日备份 | 云端API |

### 6.3 L1热记忆接口设计

**文件位置**: `.workbuddy/session-state.md`

**核心模块**: `core/session_state.py`

**接口定义**：
```python
# core/session_state.py
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime
from enum import Enum

class ActiveTask(BaseModel):
    """活跃任务"""
    task_id: str = Field(..., description="任务唯一ID")
    task_type: str = Field(..., description="任务类型：generation/validation/analysis")
    description: str = Field(..., description="任务描述")
    started_at: datetime = Field(default_factory=datetime.now)
    progress: float = Field(default=0.0, ge=0.0, le=1.0)

class TempContext(BaseModel):
    """临时上下文"""
    current_chapter: Optional[int] = None
    current_file: Optional[str] = None
    last_operation: Optional[str] = None
    pending_data: dict = Field(default_factory=dict)

class ErrorState(BaseModel):
    """异常状态"""
    has_error: bool = False
    error_message: Optional[str] = None
    error_time: Optional[datetime] = None
    recovery_hint: Optional[str] = None

class SessionState(BaseModel):
    """会话状态"""
    state_id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    active_task: Optional[ActiveTask] = None
    temp_context: TempContext = Field(default_factory=TempContext)
    error_state: ErrorState = Field(default_factory=ErrorState)
    pending_data: dict = Field(default_factory=dict)
    created_at: datetime = Field(default_factory=datetime.now)
    updated_at: datetime = Field(default_factory=datetime.now)

class SessionStateManager:
    """会话状态管理器 - 单例模式"""
    
    _instance = None
    _lock = threading.RLock()
    
    def __new__(cls):
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = super().__new__(cls)
        return cls._instance
    
    def save_state(self, state: SessionState) -> bool:
        """保存状态到SESSION-STATE.md"""
        pass
    
    def load_state(self) -> Optional[SessionState]:
        """从SESSION-STATE.md加载状态"""
        pass
    
    def update_task(self, task: ActiveTask) -> bool:
        """更新活跃任务"""
        pass
    
    def set_error(self, error: Exception, recovery_hint: str = None) -> bool:
        """设置异常状态"""
        pass
    
    def clear_error(self) -> bool:
        """清除异常状态"""
        pass
```

### 6.4 WAL协议接口设计

**核心模块**: `core/wal_manager.py`

**接口定义**：
```python
# core/wal_manager.py
from pydantic import BaseModel, Field
from typing import List, Dict, Any
from datetime import datetime
from enum import Enum
import threading
from pathlib import Path

class WALRecord(BaseModel):
    """WAL记录"""
    record_id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    operation: str = Field(..., description="操作类型：ai_call/file_write/state_change")
    data: dict = Field(default_factory=dict)
    timestamp: datetime = Field(default_factory=datetime.now)
    status: str = Field(default="pending", description="状态：pending/completed/failed")

class WALState(BaseModel):
    """WAL状态"""
    records: List[WALRecord] = Field(default_factory=list)
    last_compaction: Optional[datetime] = None
    total_records: int = 0
    pending_count: int = 0

class WALManager:
    """WAL协议管理器 - 单例模式"""
    
    _instance = None
    _lock = threading.RLock()
    
    def __new__(cls):
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = super().__new__(cls)
        return cls._instance
    
    def write_before_ai_call(self, context: dict) -> str:
        """WAL协议：在调用AI前先持久化状态（阻塞式）"""
        pass
    
    def mark_completed(self, record_id: str) -> bool:
        """标记记录为已完成"""
        pass
    
    def mark_failed(self, record_id: str, error: str) -> bool:
        """标记记录为失败"""
        pass
    
    def recover_on_startup(self) -> List[WALRecord]:
        """启动时恢复未完成的记录"""
        pass
    
    def compact(self) -> bool:
        """压缩WAL日志"""
        pass
    
    def get_pending_records(self) -> List[WALRecord]:
        """获取所有待处理记录"""
        pass
```

### 6.5 L2温记忆接口设计

**文件位置**: `data/vector_store/`

**核心模块**: `infrastructure/vector_store.py`

**接口定义**：
```python
# infrastructure/vector_store.py
from typing import List, Dict, Any, Optional
from pydantic import BaseModel, Field

class ChapterVector(BaseModel):
    """章节向量"""
    chapter_id: str
    content: str
    vector: List[float]
    metadata: Dict[str, Any] = Field(default_factory=dict)

class KnowledgeVector(BaseModel):
    """知识库向量"""
    knowledge_id: str
    category: str  # "scifi", "fantasy", "history", "general"
    domain: str    # "physics", "chemistry", "religion", "philosophy"
    title: str
    content: str
    vector: List[float]
    keywords: List[str]
    references: List[str]

class VectorStoreInterface:
    """向量存储接口"""
    
    def add_chapter(self, chapter: ChapterVector) -> bool:
        """添加章节向量"""
        pass
    
    def add_knowledge(self, knowledge: KnowledgeVector) -> bool:
        """添加知识点向量"""
        pass
    
    def search_chapters(self, query: str, top_k: int = 10) -> List[ChapterVector]:
        """搜索相似章节"""
        pass
    
    def search_knowledge(self, query: str, category: str = None, top_k: int = 10) -> List[KnowledgeVector]:
        """搜索知识点"""
        pass
    
    def delete_chapter(self, chapter_id: str) -> bool:
        """删除章节向量"""
        pass
```

### 6.6 本地知识库Schema设计

**分类知识库**:
```json
{
  "knowledge_id": "scifi-physics-001",
  "category": "scifi",
  "domain": "physics",
  "title": "时间膨胀效应",
  "content": "根据狭义相对论，当物体接近光速运动时，时间流速会变慢...",
  "keywords": ["相对论", "时间", "光速", "时间膨胀"],
  "embedding": [0.123, 0.456, ...],
  "references": ["《三体》", "《星际穿越》"],
  "created_at": "2026-03-25",
  "updated_at": "2026-03-25"
}
```

**验收标准**:
- SESSION-STATE写入成功率: 100%
- WAL协议中断恢复成功率: 100%
- 知识库召回准确率: ≥85%
- 长篇冲突识别率: ≥90%

### 6.7 一致性检查Agent接口

**核心模块**: `agents/consistency_checker_agent.py`

**接口定义**：
```python
# agents/consistency_checker_agent.py
from agents.base_agent import BaseAgent
from typing import Dict, Any, List

class ConsistencyCheckerAgent(BaseAgent):
    """一致性检查Agent - 检测长篇小说冲突"""
    
    def __init__(self, name: str = "ConsistencyChecker"):
        super().__init__(name)
    
    def execute(self, context: Dict[str, Any]) -> Dict[str, Any]:
        """执行一致性检查"""
        new_chapter = context.get("new_chapter")
        existing_chapters = context.get("existing_chapters")
        
        # 1. 召回相似上下文
        recalled_context = self.recall_similar_context(new_chapter)
        
        # 2. 构建检查prompt
        prompt = self.build_check_prompt(new_chapter, recalled_context)
        
        # 3. 调用LLM检查冲突
        conflicts = self.call_llm(prompt)
        
        return {
            "conflicts": conflicts,
            "recall_context": recalled_context
        }
    
    def build_check_prompt(self, new_chapter: str, recalled_context: str) -> str:
        """构建一致性检查prompt"""
        return f"""
你是一位专业的小说编辑，负责检测长篇小说中的逻辑冲突。

## 参考上下文（前文相关章节摘要）
{recalled_context}

## 新章节内容
{new_chapter}

## 检查任务
请检测新章节与前文是否存在以下冲突：
1. 人物行为矛盾（如性格突变、前后行为不一致）
2. 情节冲突（如时间线错乱、事件前后矛盾）
3. 世界观冲突（如设定前后不一致）

## 输出格式
如果发现冲突，请按以下格式输出：
- 冲突类型: [人物/情节/世界观]
- 冲突描述: [具体描述]
- 严重程度: [P0/P1/P2]
- 修复建议: [建议如何修复]

如果没有冲突，输出："无冲突"
"""
```

---

## 七、AI工程接口设计

**本章节所有接口均为规范定义，实现参见《AgentPRO最全经验文档.md》第十一章。**

### 6.1 统一LLM客户端接口

**支持的提供商**：
- DeepSeek（主力）
- OpenAI（GPT系列）
- Anthropic（Claude系列）
- Ollama（本地模型）
- **Qwen（本地部署，新增）**

**本地大模型部署状态（2026-03-29）**:

部署位置: F:\Qwen  
模型版本: Qwen2.5-7B-Instruct (FP16)  
验证状态: ✅ 完全可用

验证结果:
- GPU显存: 3.63 GB / 8.0 GB (45%)
- CPU内存: 11.54 GB / 32 GB (36%)
- 推理速度: 0.5-0.6 tokens/s

**内存补充显存方案**:

关键参数:
```python
device_map="auto"  # 自动分配GPU/CPU层
max_memory={
    0: "5GiB",        # GPU预算5GB（预留3GB）
    "cpu": "16GiB"    # CPU预算16GB
}
torch_dtype=torch.float16
```

启动命令:
```bash
cd F:\Qwen
python start_server_v4.py
```

**Windows环境注意事项**:
- ❌ 避免GPTQ/AWQ量化格式(需CUDA编译)
- ✅ 推荐FP16/BF16格式(无额外依赖)

**参考文档**: 《12.8本地大模型调用优化报告✅️.md》

**接口定义**：
```python
# services/llm_client.py
from typing import Dict, Any, Optional, List, Protocol
from abc import ABC, abstractmethod

class LLMProvider(Protocol):
    """LLM提供商协议"""
    
    @abstractmethod
    def generate(self, prompt: str, **kwargs) -> str:
        """生成文本"""
        pass
    
    @abstractmethod
    def batch_generate(self, prompts: List[str], **kwargs) -> List[str]:
        """批量生成"""
        pass

class LLMClient:
    """统一LLM客户端"""
    
    def __init__(self, provider: str = "deepseek", model: str = "deepseek-chat"):
        self.provider = provider
        self.model = model
        self._metrics = {"latency": [], "tokens": [], "errors": 0}
    
    def generate(self, prompt: str, **kwargs) -> str:
        """生成文本"""
        pass
    
    def get_metrics(self) -> Dict[str, Any]:
        """获取性能指标（P50/P95/P99延迟）"""
        pass
```

**性能指标采集**：
- 延迟统计（延迟分布P50/P95/P99）
- Token消耗统计
- 错误率统计
- 熔断状态

### 6.2 提示词引擎接口

**支持的策略**：
- Zero-shot（零样本）
- Few-shot（少样本）
- CoT（思维链）
- ToT（思维树）
- ReAct（推理+行动）

**接口定义**：
```python
# services/prompt_engine.py
from enum import Enum
from typing import Dict, Any, List, Optional

class PromptStrategy(Enum):
    ZERO_SHOT = "zero_shot"
    FEW_SHOT = "few_shot"
    CHAIN_OF_THOUGHT = "chain_of_thought"
    TREE_OF_THOUGHT = "tree_of_thought"
    REACT = "react"

class PromptTemplate:
    """提示词模板"""
    
    def __init__(self, name: str, system_prompt: str, user_template: str,
                 strategy: PromptStrategy = PromptStrategy.ZERO_SHOT,
                 constraints: List[str] = None):
        self.name = name
        self.system_prompt = system_prompt
        self.user_template = user_template
        self.strategy = strategy
        self.constraints = constraints or []
    
    def render(self, **variables) -> tuple[str, str]:
        """渲染提示词，返回(system_prompt, user_prompt)"""
        pass
    
    def add_constraint(self, constraint: str):
        """添加约束"""
        self.constraints.append(constraint)

class PromptEngine:
    """提示词引擎"""
    
    def __init__(self, templates_dir: str = "prompts"):
        self._templates: Dict[str, PromptTemplate] = {}
    
    def load_template(self, name: str, path: str) -> PromptTemplate:
        """加载提示词模板"""
        pass
    
    def build_prompt(self, template_name: str, strategy: PromptStrategy,
                     **variables) -> tuple[str, str]:
        """构建提示词"""
        pass
```

### 6.3 RAG检索接口

**组件架构**：
```
SimpleVectorStore (向量存储)
    ↓
RAGPipeline (检索-生成流水线)
    ↓
PromptEngine (提示词构建)
    ↓
LLMClient (LLM调用)
```

**接口定义**：
```python
# services/rag_pipeline.py
from typing import Dict, Any, List, Optional

class VectorStore:
    """向量存储接口"""
    
    def add(self, texts: List[str], metadatas: List[Dict] = None, 
            collection: str = "default") -> List[str]:
        """添加文档"""
        pass
    
    def search(self, query: str, top_k: int = 5, 
               collection: str = "default") -> List[Dict]:
        """语义检索"""
        pass
    
    def delete(self, ids: List[str], collection: str = "default"):
        """删除文档"""
        pass

class RAGPipeline:
    """RAG检索-生成流水线"""
    
    def __init__(self, vector_store: VectorStore, 
                 llm_client: 'LLMClient'):
        self.vector_store = vector_store
        self.llm_client = llm_client
    
    def retrieve(self, query: str, collection: str = "default",
                 top_k: int = 5) -> List[Dict]:
        """检索相关文档"""
        pass
    
    def generate(self, query: str, context: List[Dict],
                 **kwargs) -> str:
        """基于检索结果生成答案"""
        pass
    
    def rag(self, query: str, collection: str = "default",
            top_k: int = 5, **kwargs) -> str:
        """完整RAG流程"""
        pass
```

### 6.4 模型管理接口

**接口定义**：
```python
# services/model_manager.py
from typing import Dict, Any, Optional, List
from enum import Enum

class ModelVersion:
    """模型版本"""
    
    def __init__(self, model_id: str, version: str, 
                 config: Dict[str, Any]):
        self.model_id = model_id
        self.version = version
        self.config = config

class ModelManager:
    """模型管理器"""
    
    def __init__(self, db_path: str = "配置/app_data.db"):
        self._versions: Dict[str, List[ModelVersion]] = {}
        self._active: Dict[str, str] = {}  # model_id -> version
    
    def register_model(self, model_id: str, version: str,
                       config: Dict[str, Any]) -> bool:
        """注册模型版本"""
        pass
    
    def switch_version(self, model_id: str, version: str) -> bool:
        """切换模型版本"""
        pass
    
    def get_active(self, model_id: str) -> Optional[ModelVersion]:
        """获取当前激活版本"""
        pass
    
    def get_stats(self, model_id: str, version: str) -> Dict[str, Any]:
        """获取模型性能统计"""
        pass

class ABTestConfig:
    """A/B测试配置"""
    
    def __init__(self, test_id: str, name: str,
                 variants: Dict[str, str],  # variant_name -> model_id
                 traffic_split: Dict[str, float],
                 metrics: List[str]):
        self.test_id = test_id
        self.name = name
        self.variants = variants
        self.traffic_split = traffic_split
        self.metrics = metrics

class ABTester:
    """A/B测试框架"""
    
    def create_test(self, config: ABTestConfig) -> str:
        """创建测试，返回test_id"""
        pass
    
    def assign_variant(self, test_id: str) -> str:
        """分配测试变体"""
        pass
    
    def record_result(self, test_id: str, variant: str,
                     metrics: Dict[str, float]):
        """记录测试结果"""
        pass
    
    def analyze(self, test_id: str) -> Dict[str, Any]:
        """分析测试结果（效应量、P值、置信区间）"""
        pass
```

### 6.5 内容安全接口

**多层防护**：
1. 正则匹配：快速过滤敏感词
2. ML模型：语义分析（可扩展）
3. 规则引擎：逻辑检测

**风险等级**：
- SAFE：安全，直接通过
- LOW：低风险，警告用户
- MEDIUM：中风险，需要确认
- HIGH：高风险，建议拦截
- BLOCKED：阻断，拒绝生成

**接口定义**：
```python
# services/content_safety.py
from enum import Enum
from typing import Dict, Any, List

class RiskLevel(Enum):
    SAFE = "safe"
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"
    BLOCKED = "blocked"

class SafetyResult:
    """安全检查结果"""
    
    def __init__(self, level: RiskLevel, score: float,
                 reasons: List[str] = None):
        self.level = level
        self.score = score
        self.reasons = reasons or []

class ContentSafetyFilter:
    """内容安全过滤器"""
    
    def __init__(self, config: Dict[str, Any] = None):
        self._config = config or {}
        self._patterns = []  # 敏感词正则
    
    def check(self, text: str) -> SafetyResult:
        """检查内容安全性"""
        pass
    
    def filter(self, text: str) -> tuple[str, SafetyResult]:
        """过滤内容，返回(过滤后文本, 结果)"""
        pass

class PromptInjectionGuard:
    """提示词注入防护"""
    
    def detect(self, text: str) -> bool:
        """检测提示词注入"""
        pass
    
    def sanitize(self, text: str) -> str:
        """清理注入内容"""
        pass
```

---

## 八、通信协议与数据格式

### 7.1 事件总线通信协议

#### 7.1.1 事件类型定义

```python
# core/event_types.py

# 系统事件
EVENT_PLUGIN_LOADED = "plugin.loaded"
EVENT_PLUGIN_UNLOADED = "plugin.unloaded"
EVENT_PLUGIN_ERROR = "plugin.error"
EVENT_PLUGIN_ACTIVATED = "plugin.activated"
EVENT_PLUGIN_DEACTIVATED = "plugin.deactivated"
EVENT_PLUGIN_RECOVERED = "plugin.recovered"

# 生成事件
EVENT_GENERATION_STARTED = "generation.started"
EVENT_GENERATION_PROGRESS = "generation.progress"
EVENT_GENERATION_COMPLETED = "generation.completed"
EVENT_GENERATION_FAILED = "generation.failed"

# 验证事件
EVENT_VALIDATION_STARTED = "validation.started"
EVENT_VALIDATION_COMPLETED = "validation.completed"

# 用户事件
EVENT_USER_GENERATION_REQUESTED = "user.generation_requested"
EVENT_USER_CONFIG_CHANGED = "user.config_changed"

# 配置事件
EVENT_CONFIG_CHANGED = "config.changed"
```

#### 7.1.2 事件数据格式

**生成开始事件**：
```python
{
    "event_type": "generation.started",
    "data": {
        "request_id": "gen-12345678",
        "chapter_title": "第一章 开始",
        "word_count_target": 2000,
        "timestamp": "2026-03-21T10:00:00Z"
    }
}
```

**生成完成事件**：
```python
{
    "event_type": "generation.completed",
    "data": {
        "request_id": "gen-12345678",
        "result": {
            "content": "生成的章节内容...",
            "word_count": 2156,
            "iteration_count": 3,
            "final_score": 0.85,
            "scores": {
                "word_count": 1.0,
                "outline": 0.9,
                "style": 0.8,
                "character": 0.85,
                "worldview": 0.9,
                "naturalness": 0.8
            }
        }
    }
}
```

**验证完成事件**：
```python
{
    "event_type": "validation.completed",
    "data": {
        "request_id": "gen-12345678",
        "iteration": 2,
        "scores": {
            "word_count_score": 1.0,
            "outline_score": 0.85,
            "style_score": 0.78,
            "character_score": 0.82,
            "worldview_score": 0.88,
            "naturalness_score": 0.75
        },
        "total_score": 0.8375,
        "passed_threshold": True
    }
}
```

**配置变更事件**：
```python
{
    "event_type": "config.changed",
    "data": {
        "key": "ai.temperature",
        "old_value": 0.7,
        "new_value": 0.8
    }
}
```

### 7.2 插件间通信协议

#### 7.2.1 同步调用（ServiceLocator/PluginRegistry）

插件通过ServiceLocator或PluginRegistry获取其他插件实例，直接调用方法。

```python
# 在GeneratorPlugin中调用AnalyzerPlugin
def generate(self, request: GenerationRequest) -> GenerationResult:
    # 获取大纲解析器
    outline_parser = self._context.plugin_registry.get_plugin("outline-parser-v3")
    
    # 调用分析方法
    analysis = outline_parser.analyze(request.chapter_outline)
    
    # 使用分析结果
    # ...
```

#### 7.2.2 异步通信（EventBus）

插件通过EventBus发布事件，其他插件订阅响应。

```python
# 发布方：GeneratorPlugin
def generate(self, request: GenerationRequest) -> GenerationResult:
    # 发布生成开始事件
    self._context.event_bus.publish(
        EVENT_GENERATION_STARTED,
        {
            "request_id": request.request_id,
            "chapter_title": request.chapter_title,
            "word_count_target": request.word_count_target
        },
        source="NovelGeneratorPlugin"
    )
    
    # ... 生成内容 ...
    
    # 发布生成完成事件
    self._context.event_bus.publish(
        EVENT_GENERATION_COMPLETED,
        {
            "request_id": request.request_id,
            "result": result.model_dump()
        },
        source="NovelGeneratorPlugin"
    )

# 订阅方：UI组件（线程安全）
def setup_ui(self):
    # 订阅生成完成事件
    self._context.event_bus.subscribe(
        EVENT_GENERATION_COMPLETED,
        self._on_generation_completed
    )

def _on_generation_completed(self, event: Event):
    # 【Tkinter线程安全】使用after()调度到主线程
    def update_ui():
        result = event.data.get("result")
        self.text_area.insert("1.0", result["content"])
    
    self.root.after(0, update_ui)
```

### 7.3 Pydantic模型作为数据格式标准

所有跨层通信的数据使用Pydantic模型定义，确保类型安全和数据验证。

#### 7.3.1 模型设计原则

1. **使用Pydantic v2**：充分利用Rust核心的性能优势
2. **启用from_attributes**：支持ORM对象转换
3. **使用Field**：提供详细的字段描述和验证规则
4. **添加computed properties**：便捷的计算属性
5. **使用TypeAdapter**：高性能的序列化/反序列化
6. **使用timezone-aware datetime**：避免时区歧义

#### 7.3.2 性能优化

**启用模型级配置**：
```python
class BaseModelExtended(BaseModel):
    model_config = ConfigDict(
        from_attributes=True,      # 零拷贝验证
        validate_assignment=True,   # 赋值时验证
        ser_json_bytes="base64",   # JSON序列化优化
        arbitrary_types_allowed=True  # 允许任意类型
    )
```

**使用TypeAdapter**：
```python
from pydantic import TypeAdapter

# 一次性创建适配器（避免重复编译模式）
request_adapter = TypeAdapter(GenerationRequest)

# 高效解析
request = request_adapter.validate_python(data)
```

---

## 九、性能优化

### 8.1 分层延迟加载架构

**加载层级**：
1. **核心层**（立即加载）：EventBus、PluginRegistry、ServiceLocator
2. **插件层**（按需加载）：Analyzer插件、Generator插件等
3. **Agent层**（延迟加载）：MasterAgent及专家Agent
4. **UI层**（异步加载）：复杂UI组件

**加载流程**：
```
启动 (< 0.5秒)
    ↓
加载核心层
    ↓
显示主窗口 (< 1秒)
    ↓
后台加载插件层
    ↓
用户首次使用时加载Agent层
```

### 8.2 缓存策略

**缓存层级**：

| 缓存类型 | 缓存内容 | 过期策略 | 容量 |
|----------|----------|----------|------|
| 插件缓存 | 已加载插件实例 | 永久 | 全部 |
| 风格缓存 | 风格特征向量 | 24小时 | 100MB |
| 提示词缓存 | 构建的提示词 | 永久 | 1000条 |
| 响应缓存 | LLM响应结果 | 1小时 | 500MB |

### 8.3 并发优化

**SQLite并发**：
- WAL模式（读写并发）
- 连接池（最多10个连接）
- 事务超时（30秒）

**事件总线并发**：
- 异步事件处理（ThreadPoolExecutor）
- 线程池（最多10个工作线程）
- 超时控制（每个处理器5秒）

### 8.4 数据库迁移策略

**数据库迁移机制**：

```python
# infrastructure/database_migration.py
import sqlite3
import shutil
from pathlib import Path
from typing import Dict, Callable
from datetime import datetime

class DatabaseMigration:
    """数据库迁移管理器"""
    
    SCHEMA_VERSION_KEY = "schema_version"
    CURRENT_VERSION = 1
    
    def __init__(self, db_path: str):
        self.db_path = db_path
        self._backup_dir = Path(db_path).parent / "db_backups"
        self._backup_dir.mkdir(exist_ok=True)
        self._migrations: Dict[int, Callable] = {
            1: self._migration_v1,
        }
    
    def get_current_version(self) -> int:
        """获取当前数据库版本"""
        try:
            with sqlite3.connect(self.db_path) as conn:
                cursor = conn.execute(
                    "SELECT version FROM schema_version ORDER BY version DESC LIMIT 1"
                )
                row = cursor.fetchone()
                return row[0] if row else 0
        except sqlite3.OperationalError:
            return 0
    
    def backup_database(self) -> str:
        """迁移前备份"""
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        backup_path = self._backup_dir / f"db_backup_{timestamp}.db"
        shutil.copy2(self.db_path, backup_path)
        return str(backup_path)
    
    def migrate(self) -> bool:
        """执行迁移（带备份和回滚）"""
        try:
            current_version = self.get_current_version()
            if current_version >= self.CURRENT_VERSION:
                return True
            
            # 迁移前备份
            backup_path = self.backup_database()
            
            for version in range(current_version + 1, self.CURRENT_VERSION + 1):
                migration = self._migrations.get(version)
                if not migration:
                    raise ValueError(f"Migration version {version} not defined")
                
                print(f"Executing migration v{version}...")
                migration()
                
                # 更新版本记录
                with sqlite3.connect(self.db_path) as conn:
                    conn.execute(
                        "INSERT INTO schema_version (version) VALUES (?)",
                        (version,)
                    )
                    conn.commit()
            
            print(f"Migration completed: v{current_version} → v{self.CURRENT_VERSION}")
            return True
        
        except Exception as e:
            print(f"Migration failed: {e}")
            # 回滚：恢复备份
            if 'backup_path' in locals():
                shutil.copy2(backup_path, self.db_path)
                print(f"Rollback completed using backup: {backup_path}")
            return False
    
    def _migration_v1(self):
        """V1初始化"""
        with sqlite3.connect(self.db_path) as conn:
            # schema_version表
            conn.execute("""
                CREATE TABLE IF NOT EXISTS schema_version (
                    version INTEGER PRIMARY KEY,
                    applied_at TEXT DEFAULT (datetime('now'))
                )
            """)
            
            # generation_history表
            conn.execute("""
                CREATE TABLE IF NOT EXISTS generation_history (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    chapter_title TEXT NOT NULL,
                    iteration_count INTEGER DEFAULT 1,
                    final_score REAL,
                    word_count INTEGER,
                    created_at TEXT DEFAULT (datetime('now')),
                    metadata TEXT
                )
            """)
            
            # plugin_state表
            conn.execute("""
                CREATE TABLE IF NOT EXISTS plugin_state (
                    plugin_id TEXT PRIMARY KEY,
                    state TEXT NOT NULL,
                    enabled INTEGER DEFAULT 1,
                    load_count INTEGER DEFAULT 0,
                    last_loaded TEXT,
                    config TEXT,
                    error_count INTEGER DEFAULT 0,
                    last_error TEXT
                )
            """)
            
            # operation_log表
            conn.execute("""
                CREATE TABLE IF NOT EXISTS operation_log (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    operation TEXT NOT NULL,
                    module TEXT,
                    status TEXT,
                    duration_ms INTEGER,
                    detail TEXT,
                    created_at TEXT DEFAULT (datetime('now'))
                )
            """)
            
            # V1.1：添加索引
            conn.execute("CREATE INDEX IF NOT EXISTS idx_generation_created ON generation_history(created_at DESC)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_generation_title ON generation_history(chapter_title)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_plugin_enabled ON plugin_state(enabled)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_oplog_module ON operation_log(module)")
            conn.execute("CREATE INDEX IF NOT EXISTS idx_oplog_created ON operation_log(created_at DESC)")
            
            conn.commit()
```

**核心表结构**：

```sql
-- schema_version表
CREATE TABLE IF NOT EXISTS schema_version (
    version INTEGER PRIMARY KEY,
    applied_at TEXT DEFAULT (datetime('now'))
);

-- generation_history表
CREATE TABLE IF NOT EXISTS generation_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chapter_title TEXT NOT NULL,
    iteration_count INTEGER DEFAULT 1,
    final_score REAL,
    word_count INTEGER,
    created_at TEXT DEFAULT (datetime('now')),
    metadata TEXT
);

-- plugin_state表
CREATE TABLE IF NOT EXISTS plugin_state (
    plugin_id TEXT PRIMARY KEY,
    state TEXT NOT NULL,
    enabled INTEGER DEFAULT 1,
    load_count INTEGER DEFAULT 0,
    last_loaded TEXT,
    config TEXT,
    error_count INTEGER DEFAULT 0,
    last_error TEXT
);

-- operation_log表
CREATE TABLE IF NOT EXISTS operation_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    operation TEXT NOT NULL,
    module TEXT,
    status TEXT,
    duration_ms INTEGER,
    detail TEXT,
    created_at TEXT DEFAULT (datetime('now'))
);

-- 索引优化
CREATE INDEX idx_generation_created ON generation_history(created_at DESC);
CREATE INDEX idx_generation_title ON generation_history(chapter_title);
CREATE INDEX idx_generation_score ON generation_history(final_score);
CREATE INDEX idx_plugin_enabled ON plugin_state(enabled);
CREATE INDEX idx_oplog_module ON operation_log(module);
CREATE INDEX idx_oplog_status ON operation_log(status);
CREATE INDEX idx_oplog_created ON operation_log(created_at DESC);
```

**SQLite连接池实现**：

```python
# infrastructure/database_pool.py
import sqlite3
import threading
from queue import Queue, Empty
from typing import Optional
from contextlib import contextmanager

class SQLiteConnectionPool:
    """SQLite连接池"""
    
    def __init__(self, db_path: str, max_connections: int = 10):
        self.db_path = db_path
        self.max_connections = max_connections
        self._pool: Queue[sqlite3.Connection] = Queue(maxsize=max_connections)
        self._lock = threading.Lock()
        self._active_connections = 0
        
        # 预创建连接
        for _ in range(max_connections):
            conn = self._create_connection()
            self._pool.put(conn)
    
    def _create_connection(self) -> sqlite3.Connection:
        """创建数据库连接并设置优化参数"""
        conn = sqlite3.connect(self.db_path, check_same_thread=False)
        
        # WAL模式优化
        conn.execute('PRAGMA journal_mode=WAL')
        conn.execute('PRAGMA synchronous=NORMAL')
        conn.execute('PRAGMA foreign_keys=ON')
        
        # 性能优化PRAGMA
        conn.execute('PRAGMA cache_size=-64000')  # 64MB缓存
        conn.execute('PRAGMA temp_store=MEMORY')
        conn.execute('PRAGMA mmap_size=268435456')  # 256MB内存映射
        conn.execute('PRAGMA page_size=4096')
        
        return conn
    
    def get_connection(self, timeout: float = 30.0) -> sqlite3.Connection:
        """获取连接"""
        try:
            conn = self._pool.get(timeout=timeout)
            with self._lock:
                self._active_connections += 1
            return conn
        except Empty:
            raise TimeoutError(f"Connection pool exhausted. Active: {self._active_connections}")
    
    def release_connection(self, conn: sqlite3.Connection):
        """释放连接"""
        try:
            self._pool.put_nowait(conn)
        except:
            conn.close()
        finally:
            with self._lock:
                self._active_connections -= 1
    
    @contextmanager
    def connection(self, timeout: float = 30.0):
        """连接上下文管理器"""
        conn = self.get_connection(timeout)
        try:
            yield conn
        finally:
            self.release_connection(conn)
```

---

## 九点五、附录：桌面应用性能卡顿优化（V1.7新增）

> **决策编号**: ADR-008  
> **来源**: 《11.18桌面应用性能卡顿根因分析与优化方案（继续推进版）✅️.md》  
> **实施日期**: 2026-03-28  
> **状态**: ✅ 已完成

### 9.5.1 问题现状与根因

#### 启动时间分解（优化前）

```
┌─────────────────────────────────────────────────────────────────┐
│                    启动时间分解（总计约2分钟）                      │
├─────────────────────────────────────────────────────────────────┤
│  热榜爬取阻塞      ████████████████████ 19秒                     │
│  HuggingFace模型   █████████████████████████████████████ 52秒   │
│  知识库向量库初始化 ████████████████ 约30秒                      │
│  其他初始化        ████████ 约15秒                              │
└─────────────────────────────────────────────────────────────────┘
```

#### 问题清单

| 问题编号 | 问题描述 | 影响 | 状态 |
|---------|---------|------|------|
| P1-NEW-1 | HF_HUB_OFFLINE环境变量设置无效 | 🔴 高 | ✅ 已修复 |
| P1-NEW-2 | 热榜爬取阻塞启动（约19秒） | 🟡 中 | ✅ 已修复 |
| P1-NEW-3 | 向量模型加载卡顿（约52秒） | 🟡 中 | ✅ 已修复 |
| P1-NEW-4 | gui_main.py文件叠加增大（已达1.2万行） | 🟡 中 | ⏸️ 延后执行 |

### 9.5.2 解决方案设计

#### P1-NEW-1: HF_HUB_OFFLINE环境变量修复

**根因**：环境变量在 `sentence_transformers` 导入后设置，导致知识库界面卡死。

**解决方案**：在 `gui_main.py` 入口处设置环境变量（第20-26行）

```python
# ============== 离线模式设置（必须在所有import之前）==============
import os

os.environ["TRANSFORMERS_OFFLINE"] = "1"  # Transformers离线模式
os.environ["HF_HUB_OFFLINE"] = "1"        # HuggingFace Hub离线模式
os.environ["HF_DATASETS_OFFLINE"] = "1"   # Datasets离线模式
```

#### P1-NEW-2: 热榜爬取异步化

**根因**：热榜插件在 `MainWindow.__init__()` 中同步调用，爬取3个网站串行执行。

**解决方案**：
1. 先显示缓存数据（或默认数据）
2. 后台异步爬取新数据
3. 更新完成后自动刷新UI

**关键代码**：`gui_main.py` 第10813-10837行

#### P1-NEW-3: 向量模型延迟加载

**根因**：模型文件约90MB，从磁盘加载到内存耗时。

**解决方案**：
1. VectorStore改为延迟加载模式
2. 首次使用知识库时才加载模型
3. 启动后5秒后台预加载

**关键代码**：`infrastructure/vector_store.py` 第150-211行

### 9.5.3 性能提升对比

| 指标 | 优化前 | 优化后 | 提升幅度 |
|------|--------|--------|----------|
| 冷启动时间 | ~2分钟 | ≤30秒 | **75%↓** |
| 热榜加载时间 | 19秒（阻塞） | ≤2秒（异步） | **90%↓** |
| 模型加载时间 | 52秒（阻塞） | 0秒（延迟） | **100%↓** |
| UI响应时间 | 5-30秒 | 0.2ms | **99.99%↓** |
| 知识库打开 | 卡死 | 无卡顿 | **100%↓** |

### 9.5.4 验收标准

| 验收项 | 验收标准 | 测试方法 | 状态 |
|-------|---------|---------|------|
| 启动时间 | ≤ 30秒 | 计时测试 | ✅ 通过 |
| 知识库打开 | 无卡顿 | 手动测试 | ✅ 通过 |
| 热榜显示 | 启动后立即显示缓存数据 | 手动测试 | ✅ 通过 |
| 离线模式 | 不尝试连接huggingface.co | 日志检查 | ✅ 通过 |
| UI响应 | ≤100ms | 手动测试 | ✅ 通过 |

### 9.5.5 P1-NEW-4文件外迁重构（延后执行）

**状态**：⏸️ 延后执行

**决策理由**：
- 核心卡顿问题已解决
- 架构紧耦合风险高
- 收益风险比不合适

**技术债记录**：
- ID: TECH-DEBT-001
- 文件: `gui_main.py`（13,760行）
- 类型: 架构债务（大文件、紧耦合）
- 优先级: P2（可维护性问题，非功能性）
- 计划偿还时间: V2.19迭代周期

---

## 十、附录：关键技术决策

### 9.1 技术栈版本锁定

| 技术 | 版本 | 锁定理由 |
|------|------|----------|
| Python | 3.12.x | 3.13生态尚未完全稳定，3.12是当前最佳选择 |
| Pydantic | 2.10.6 | V5.2已升级到v2，性能提升3-5倍 |
| Tkinter | 内置 | 零额外依赖，V5已验证 |
| sv_ttk | 2.2+ | Win11风格主题，轻量级 |
| openai SDK | 1.60+ | 统一LLM客户端，兼容DeepSeek API |
| LangGraph | 可选 | 不作为核心依赖，仅在复杂编排时使用 |

### 9.2 架构决策记录 (ADR)

#### ADR-001：采用微内核架构

**状态**：已接受

**上下文**：项目模块耦合度高，扩展性差，修改一处影响多处。

**决策**：采用微内核架构，核心只提供基础调度，所有功能以插件形式存在。

**后果**：
- **优势**：高内聚低耦合、热插拔支持、可扩展性强
- **劣势**：初期架构复杂度高、插件开发需要学习曲线

#### ADR-002：事件总线作为主要通信机制

**状态**：已接受

**上下文**：插件间需要解耦通信，避免直接依赖。

**决策**：采用发布订阅模式的事件总线作为主要通信机制。

**后果**：
- **优势**：解耦依赖、易于扩展、支持异步通信
- **劣势**：性能开销、调用栈追踪困难

#### ADR-003：Pydantic v2作为数据验证框架

**状态**：已接受

**上下文**：需要统一的跨层数据格式和验证机制。

**决策**：采用Pydantic v2作为数据验证框架，所有跨层数据使用Pydantic模型定义。

**后果**：
- **优势**：类型安全、自动验证、性能优化（Rust核心）
- **劣势**：学习曲线、过度设计风险

#### ADR-004：采用动态导入作为插件加载机制

**状态**：已接受

**上下文**：需要平衡实现复杂度、性能和隔离性。

**决策**：采用动态导入作为主要插件加载机制，仅在特殊场景使用进程隔离。

**后果**：
- **优势**：实现简单、性能高、Python原生支持
- **劣势**：隔离性不如进程隔离

#### ADR-005：统一GeneratorPlugin接口签名为GenerationRequest

**状态**：已接受

**上下文**：V1.0与V3.0框架方案的GeneratorPlugin接口签名不一致。

**决策**：统一使用`generate(self, request: GenerationRequest)`作为接口签名。

**后果**：
- **优势**：与V3.0保持一致、数据验证更严格、类型安全
- **劣势**：需要迁移现有实现

---

## 十一、附录：安全与可靠性设计（V1.1新增）

### 10.1 API密钥安全存储

```python
# core/secure_config.py
import keyring
from cryptography.fernet import Fernet
import os
from typing import Optional

class SecureConfigManager:
    """安全配置管理器 - 密钥安全存储"""
    
    def __init__(self):
        self._cipher = self._init_cipher()
    
    def get_api_key(self, provider: str) -> Optional[str]:
        """获取API密钥（优先从系统密钥环）"""
        # 1. 优先从环境变量读取
        env_key = os.getenv(f"AGENT_{provider.upper()}_API_KEY")
        if env_key:
            return env_key
        
        # 2. 从系统密钥管理器读取
        try:
            system_key = keyring.get_password("agent-pro", f"{provider}_api_key")
            if system_key:
                return system_key
        except Exception:
            pass
        
        # 3. 从加密配置文件读取（向后兼容）
        encrypted_key = self._get_config(f"ai.{provider}.api_key_encrypted")
        if encrypted_key:
            return self._cipher.decrypt(encrypted_key.encode()).decode()
        
        return None
    
    def set_api_key(self, provider: str, api_key: str):
        """安全存储API密钥"""
        # 优先存储到系统密钥管理器
        try:
            keyring.set_password("agent-pro", f"{provider}_api_key", api_key)
            return
        except Exception:
            pass
        
        # 降级到加密配置文件
        encrypted = self._cipher.encrypt(api_key.encode())
        self._set_config(f"ai.{provider}.api_key_encrypted", encrypted.decode())
```

### 10.2 日志敏感信息脱敏

```python
# core/log_sanitizer.py
import re
from typing import Dict, Any

class LogSanitizer:
    """日志脱敏器"""
    
    SENSITIVE_FIELDS = [
        "api_key", "apikey", "api-key", "secret", "password",
        "token", "auth", "authorization", "key"
    ]
    
    API_KEY_PATTERN = re.compile(r'(sk-[a-zA-Z0-9]{20,})')
    
    def sanitize(self, data: Dict[str, Any]) -> Dict[str, Any]:
        """递归脱敏字典数据"""
        if isinstance(data, dict):
            return {k: self._sanitize_value(k, v) for k, v in data.items()}
        return data
    
    def _sanitize_value(self, key: str, value: Any) -> Any:
        """根据键名脱敏值"""
        if isinstance(value, str):
            # 检查键名是否敏感
            if any(sf in key.lower() for sf in self.SENSITIVE_FIELDS):
                return self._mask_string(value)
            # 检查值是否包含API密钥格式
            if self.API_KEY_PATTERN.search(value):
                return self.API_KEY_PATTERN.sub(r'[MASKED]', value)
        elif isinstance(value, dict):
            return self.sanitize(value)
        elif isinstance(value, list):
            return [self.sanitize(item) if isinstance(item, dict) else item for item in value]
        return value
    
    def _mask_string(self, s: str) -> str:
        """掩码字符串，保留前4位和后4位"""
        if len(s) <= 8:
            return "*" * len(s)
        return s[:4] + "*" * (len(s) - 8) + s[-4:]
```

### 10.3 熔断器设计

```python
# core/circuit_breaker.py
import time
from enum import Enum
from typing import Callable, Any

class CircuitState(Enum):
    CLOSED = "closed"      # 正常状态
    OPEN = "open"          # 熔断状态
    HALF_OPEN = "half_open"  # 半开状态

class CircuitBreaker:
    """熔断器"""
    
    def __init__(self, failure_threshold: int = 5, 
                 recovery_timeout: int = 60,
                 half_open_max_calls: int = 3):
        self.failure_threshold = failure_threshold
        self.recovery_timeout = recovery_timeout
        self.half_open_max_calls = half_open_max_calls
        
        self.state = CircuitState.CLOSED
        self.failure_count = 0
        self.last_failure_time = None
        self.half_open_calls = 0
    
    def call(self, func: Callable, *args, **kwargs) -> Any:
        """执行带熔断保护的函数"""
        if self.state == CircuitState.OPEN:
            if self._should_attempt_reset():
                self.state = CircuitState.HALF_OPEN
                self.half_open_calls = 0
            else:
                raise CircuitBreakerOpenException("Circuit breaker is OPEN")
        
        if self.state == CircuitState.HALF_OPEN:
            if self.half_open_calls >= self.half_open_max_calls:
                raise CircuitBreakerOpenException("Half-open call limit reached")
            self.half_open_calls += 1
        
        try:
            result = func(*args, **kwargs)
            self._on_success()
            return result
        except Exception as e:
            self._on_failure()
            raise
    
    def _should_attempt_reset(self) -> bool:
        """判断是否应尝试恢复"""
        if not self.last_failure_time:
            return False
        elapsed = time.time() - self.last_failure_time
        return elapsed >= self.recovery_timeout
    
    def _on_success(self):
        """成功回调"""
        if self.state == CircuitState.HALF_OPEN:
            self.state = CircuitState.CLOSED
            self.failure_count = 0
            self.half_open_calls = 0
        else:
            self.failure_count = max(0, self.failure_count - 1)
    
    def _on_failure(self):
        """失败回调"""
        self.failure_count += 1
        self.last_failure_time = time.time()
        
        if self.state == CircuitState.HALF_OPEN:
            self.state = CircuitState.OPEN
        elif self.failure_count >= self.failure_threshold:
            self.state = CircuitState.OPEN

class CircuitBreakerOpenException(Exception):
    """熔断器打开异常"""
    pass
```

### 10.4 SLO定义

```yaml
# SLO定义
service: novel-writing-assistant-agent-pro
version: "6.0.0"

slos:
  - name: 章节生成可用性
    description: 生成请求的成功响应率
    sli: count(successful_generations) / count(total_generation_requests)
    target: 99.9%  # 每月最多43分钟不可用
    window: 30d
    
  - name: 章节生成延迟
    description: P99生成请求完成时间
    sli: percentile(generation_duration, 99)
    target: < 120s
    window: 7d
    
  - name: 插件加载成功率
    description: 插件加载/激活的成功率
    sli: count(successful_plugin_loads) / count(total_plugin_load_attempts)
    target: 99.5%
    window: 30d
    
  - name: 迭代达标率
    description: 评分≥0.8的生成占比
    sli: count(generations_with_score_ge_0_8) / count(total_generations)
    target: 95%
    window: 7d

burn_rate_alerts:
  - severity: critical
    short_window: 5m
    long_window: 1h
    factor: 14.4  # 2%错误预算在5分钟内耗尽
  - severity: warning
    short_window: 30m
    long_window: 6h
    factor: 6     # 5%错误预算在30分钟内耗尽
```

---

## 十二、附录：容器化方案（V1.2新增）

> 本章节补充AI工程师终审意见：补充Dockerfile和docker-compose开发环境配置

### 11.1 Docker打包方案

```dockerfile
# Dockerfile - 基于Python官方镜像
FROM python:3.12-slim AS builder

# 安装构建依赖
RUN apt-get update && apt-get install -y \
    build-essential \
    cmake \
    git \
    && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖（使用国内镜像加速）
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 第二阶段：运行时镜像
FROM python:3.12-slim

# 安装运行时依赖
RUN apt-get update && apt-get install -y \
    libsqlite3-0 \
    && rm -rf /var/lib/apt/lists/*

# 创建非root用户
RUN useradd -m -u 1000 appuser

# 设置工作目录
WORKDIR /app

# 从builder阶段复制依赖
COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin

# 复制应用代码
COPY --chown=appuser:appuser . .

# 切换到非root用户
USER appuser

# 暴露端口
EXPOSE 7860

# 启动命令
CMD ["python", "gui_main.py"]
```

### 11.2 Docker Compose开发环境

```yaml
# docker-compose.yml
version: '3.8'

services:
  agent-pro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: novel-assistant-agent-pro
    ports:
      - "7860:7860"
    volumes:
      - ./配置:/app/配置
      - ./小说:/app/小说
      - ./日志:/app/日志
      - ./插件:/app/插件
    environment:
      - AGENT_DEEPSEEK_API_KEY=${AGENT_DEEPSEEK_API_KEY}
      - AGENT_OPENAI_API_KEY=${AGENT_OPENAI_API_KEY}
      - AGENT_ANTHROPIC_API_KEY=${AGENT_ANTHROPIC_API_KEY}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import tkinter"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - agent-pro-net

  # 开发环境可选：本地模型服务
  ollama:
    image: ollama/ollama:latest
    container_name: ollama-service
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    profiles:
      - dev
    networks:
      - agent-pro-net

networks:
  agent-pro-net:
    driver: bridge

volumes:
  ollama-data:
```

### 11.3 容器化构建命令

```bash
# 构建镜像
docker build -t novel-assistant-agent-pro:latest .

# 运行容器
docker run -d \
  --name novel-assistant-agent-pro \
  -p 7860:7860 \
  -v $(pwd)/配置:/app/配置 \
  -v $(pwd)/小说:/app/小说 \
  -e AGENT_DEEPSEEK_API_KEY=${AGENT_DEEPSEEK_API_KEY} \
  novel-assistant-agent-pro:latest

# 使用docker-compose运行
docker-compose up -d

# 查看日志
docker-compose logs -f agent-pro
```

### 11.4 容器化注意事项

1. **GUI限制**：Docker容器内运行Tkinter需要配置X11转发，生产环境建议使用无头模式
2. **性能考虑**：容器内Python性能略低于宿主机，敏感操作需评估
3. **数据持久化**：配置、小说数据需通过volume挂载持久化
4. **密钥管理**：生产环境建议使用Docker secrets或外部密钥管理服务

---

## 十三、附录：插件代码签名机制（V1.2新增）

> 本章节补充AI工程师终审意见：完善插件代码签名机制设计

### 12.1 签名机制设计

```python
# core/plugin_signer.py
import hashlib
import hmac
from cryptography.hazmat.primitives import hashes, serialization
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.backends import default_backend
import json
from pathlib import Path

class PluginSigner:
    """插件代码签名器"""
    
    def __init__(self, private_key_path: str = None):
        self._private_key = None
        self._public_key = None
        if private_key_path:
            self._load_key(private_key_path)
    
    def _load_key(self, key_path: str):
        """加载密钥对"""
        with open(key_path, 'rb') as f:
            self._private_key = serialization.load_pem_private_key(
                f.read(), password=None, backend=default_backend()
            )
        self._public_key = self._private_key.public_key()
    
    def sign_plugin(self, plugin_dir: Path) -> str:
        """对插件目录进行签名"""
        # 1. 计算插件文件哈希
        manifest = self._calculate_file_hashes(plugin_dir)
        
        # 2. 将manifest序列化为字符串
        manifest_str = json.dumps(manifest, sort_keys=True)
        
        # 3. 使用私钥签名
        signature = self._private_key.sign(
            manifest_str.encode(),
            hashes.SHA256()
        )
        
        # 4. 保存签名文件
        signature_path = plugin_dir / "plugin.sig"
        signature_path.write_bytes(signature)
        
        return signature.hex()
    
    def _calculate_file_hashes(self, plugin_dir: Path) -> dict:
        """计算插件目录中所有文件的SHA256哈希"""
        manifest = {}
        for file_path in plugin_dir.rglob("*"):
            if file_path.is_file() and file_path.name not in ["plugin.sig", "plugin.pub"]:
                relative_path = file_path.relative_to(plugin_dir)
                with open(file_path, "rb") as f:
                    content = f.read()
                    manifest[str(relative_path)] = hashlib.sha256(content).hexdigest()
        return manifest
    
    def verify_signature(self, plugin_dir: Path, public_key_pem: bytes) -> bool:
        """验证插件签名"""
        signature_path = plugin_dir / "plugin.sig"
        if not signature_path.exists():
            return False
        
        # 加载公钥
        public_key = serialization.load_pem_public_key(
            public_key_pem, backend=default_backend()
        )
        
        # 重新计算哈希
        manifest = self._calculate_file_hashes(plugin_dir)
        manifest_str = json.dumps(manifest, sort_keys=True)
        
        # 验证签名
        signature = signature_path.read_bytes()
        try:
            public_key.verify(signature, manifest_str.encode(), hashes.SHA256())
            return True
        except Exception:
            return False
```

### 12.2 签名验证流程

```python
# core/plugin_loader.py - 加载时验证签名
class PluginLoader:
    """插件加载器 - 含签名验证"""
    
    TRUSTED_SIGNERS = [
        # 项目官方签名公钥
        "-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
    ]
    
    def load_plugin(self, plugin_id: str) -> BasePlugin:
        plugin_dir = self._plugin_dirs[plugin_id]
        
        # 1. 检查签名文件是否存在
        sig_path = plugin_dir / "plugin.sig"
        if not sig_path.exists():
            if self._require_signature:
                raise PluginSignatureError(f"插件 {plugin_id} 缺少签名")
            # 允许加载未签名插件（开发模式）
            return self._load_unsigned(plugin_dir)
        
        # 2. 验证签名
        signer = PluginSigner()
        for trusted_pubkey in self.TRUSTED_SIGNERS:
            if signer.verify_signature(plugin_dir, trusted_pubkey.encode()):
                break
        else:
            raise PluginSignatureError(f"插件 {plugin_id} 签名验证失败")
        
        # 3. 加载插件
        return self._load_plugin_impl(plugin_dir)
```

### 12.3 证书管理规范

1. **私钥保管**：项目官方私钥由核心开发团队保管，不提交到代码仓库
2. **公钥分发**：公钥嵌入应用程序，支持热更新
3. **签名策略**：
   - 官方插件：使用官方私钥签名
   - 第三方插件：建议使用自有私钥签名，应用内置白名单公钥
4. **吊销机制**：支持公钥吊销列表（ CRL），可禁用 compromised 密钥

---

## 十四、附录：架构决策记录（V1.2新增）

> 本章节补充后端架构师终审意见：添加ADR架构决策记录

### ADR-006：采用ThreadPoolExecutor实现EventBus异步

**状态**：已接受

**背景**：EventBus需要异步发布事件以避免阻塞主线程，Python异步方案有ThreadPoolExecutor和asyncio两种。

**决策**：使用ThreadPoolExecutor。

**原因**：
1. 避免引入asyncio依赖（保持零额外依赖原则）
2. 与Tkinter线程模型天然兼容
3. 代码逻辑清晰，易于理解和调试
4. 无需修改现有同步代码结构

**后果**：
- 优势：实现简单、兼容性良好、调试方便
- 劣势：线程创建有开销、无法利用协程的轻量级特性

### ADR-007：SQLite + WAL模式作为数据持久化方案

**状态**：已接受

**背景**：项目需要轻量级嵌入式数据库，候选方案有SQLite、LevelDB、BoltDB。

**决策**：SQLite + WAL模式。

**原因**：
1. Python原生支持，无需额外依赖
2. WAL模式支持读写并发（Writer和Reader不互斥）
3. 成熟的生态和工具链
4. V5项目已有SQLite使用经验

**后果**：
- 优势：零依赖、并发支持好、学习成本低
- 劣势：不适合超大规模数据、需要定期维护WAL文件

### ADR-008：采用动态导入作为插件加载机制

**状态**：已接受

**背景**：需要灵活的插件加载机制，候选方案有动态导入、进程隔离、容器化。

**决策**：采用动态导入作为主要插件加载机制。

**原因**：
1. Python原生支持，实现简单
2. 性能开销最小
3. 与项目零额外依赖原则一致
4. 特殊场景（如第三方插件）可使用进程隔离

**后果**：
- 优势：实现简单、性能高、易于调试
- 劣势：插件错误可能影响主进程、隔离性不如进程隔离

---

## 十五、附录：故障排查手册（V1.2新增）

> 本章节补充技术文档工程师终审意见：添加故障排查手册

### 14.1 插件加载失败排查

**常见错误**：

| 错误类型 | 症状 | 排查步骤 |
|----------|------|----------|
| ImportError | 无法导入插件模块 | 1. 检查插件目录结构<br>2. 验证__init__.py存在<br>3. 检查依赖是否满足 |
| PluginSignatureError | 签名验证失败 | 1. 检查plugin.sig是否存在<br>2. 验证签名密钥是否在白名单<br>3. 检查文件是否被篡改 |
| CircularDependencyError | 循环依赖检测失败 | 1. 检查插件依赖声明<br>2. 验证依赖版本兼容性<br>3. 查看依赖图 |
| PluginStateError | 状态转换非法 | 1. 检查插件生命周期<br>2. 验证状态机转换逻辑<br>3. 查看错误计数 |

**排查命令**：
```bash
# 查看插件加载日志
python -c "from core.plugin_registry import PluginRegistry; pr = PluginRegistry(); pr.enable_debug(); pr.discover()"

# 检查插件目录结构
ls -la plugins/
```

### 14.2 事件总线异常排查

**常见问题**：

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 事件未到达 | 订阅者未收到事件 | 1. 检查事件类型匹配<br>2. 验证订阅时机<br>3. 查看死信队列 |
| 阻塞主线程 | UI卡顿 | 1. 使用publish()而非publish_sync()<br>2. 检查处理器耗时<br>3. 启用熔断器 |
| 内存泄漏 | 内存持续增长 | 1. 检查订阅是否正确取消<br>2. 查看线程池状态<br>3. 验证死信队列大小 |

**调试代码**：
```python
# 启用EventBus调试模式
event_bus = EventBus(debug=True)
event_bus.subscribe("*", lambda e: print(f"事件: {e.type}"))

# 查看死信队列
print(f"死信队列大小: {len(event_bus._dead_letter_queue)}")
```

### 14.3 数据库性能优化排查

**性能问题**：

| 问题 | 表现 | 优化方案 |
|------|------|----------|
| 查询慢 | 生成历史加载慢 | 添加索引（见8.4节） |
| 写入阻塞 | 并发写入卡顿 | 启用WAL模式、调整PRAGMA |
| 连接耗尽 | 连接超时 | 检查连接池大小、添加连接超时 |

**诊断SQL**：
```sql
-- 查看查询计划
EXPLAIN QUERY PLAN SELECT * FROM generation_history WHERE chapter_title LIKE '%测试%';

-- 查看表大小
SELECT name, page_count * page_size as size FROM pragma_page_count(), pragma_page_size();

-- 查看WAL文件大小
-- 定期执行：PRAGMA wal_checkpoint(TRUNCATE)
```

### 14.4 Agent超时排查

**超时场景**：

| Agent | 默认超时 | 降级策略 | 排查方向 |
|-------|----------|----------|----------|
| Thinker | 30s | 跳过CoT直接推理 | 检查LLM响应时间 |
| Optimizer | 20s | 返回原始结果 | 检查优化逻辑复杂度 |
| Validator | 15s | 返回默认0.5分 | 检查验证器性能 |
| Planner | 10s | 使用上一次计划 | 检查规划逻辑 |

**调试配置**：
```python
# 调整超时配置
config = {
    "agent.timeouts.thinker": 60,  # 增加超时
    "agent.timeouts.optimizer": 40,
    "agent.timeouts.validator": 30,
    "agent.timeouts.planner": 20,
}
```

---

## 十六、附录：风险提示与缓解措施（V1.2新增）

> 本章节补充技术文档工程师终审意见：添加风险提示

### 15.1 高风险项

#### 15.1.1 ThreadPoolExecutor线程泄漏

**描述**：EventBus使用的ThreadPoolExecutor在异常情况下可能未正确关闭，导致线程资源泄漏。

**影响**：长时间运行后线程数持续增长，最终耗尽系统资源。

**缓解措施**：
```python
# 在应用退出时清理
import atexit

class EventBus:
    def __init__(self, ...):
        self._executor = ThreadPoolExecutor(max_workers=10)
        # 注册退出钩子
        atexit.register(self.shutdown)
    
    def shutdown(self):
        """优雅关闭线程池"""
        self._executor.shutdown(wait=True, cancel_futures=True)
```

**监控指标**：
- 线程池活跃线程数
- 任务队列积压长度
- 进程总线程数

#### 15.1.2 SQLite WAL模式文件膨胀

**描述**：WAL模式下的-WAL文件和-SHM文件可能持续膨胀，占用大量磁盘空间。

**影响**：磁盘空间耗尽、应用崩溃。

**缓解措施**：
```python
# 定期执行checkpoint
import sqlite3

def truncate_wal(db_path: str):
    conn = sqlite3.connect(db_path)
    conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
    conn.close()

# 定时任务：每天凌晨3点执行
import schedule
schedule.every().day.at("03:00").do(truncate_wal, "配置/app_data.db")
```

**监控阈值**：
- WAL文件超过100MB告警
- 超过500MB自动truncate

### 15.2 中风险项

#### 15.2.1 熔断器误触发

**描述**：故障阈值设置不当可能导致熔断器频繁开启，影响可用性。

**缓解措施**：
1. 根据实际负载调优阈值（初始：失败5次开启）
2. 添加熔断器状态变更告警
3. 提供手动重置接口

#### 15.2.2 API密钥迁移

**描述**：从配置文件迁移到系统密钥环时可能失败，导致服务中断。

**缓解措施**：
1. 提供迁移脚本和回滚方案
2. 迁移前自动备份
3. 保留双写机制（兼容旧配置）

---

## 十七、附录：实施检查清单（V1.2新增）

> 本章节补充技术文档工程师终审意见：添加实施检查清单

### 16.1 Sprint1：核心模块实施

- [ ] EventBus.publish()异步实现单元测试
- [ ] PluginRegistry状态机集成测试
- [ ] BasePlugin.execute()降级兼容性测试

### 16.2 Sprint2：数据与配置

- [ ] ConfigManager并发压力测试
- [ ] 数据库索引性能测试
- [ ] SQLite连接池稳定性测试
- [ ] API密钥安全存储验证

### 16.3 Sprint3：安全与可靠性

- [ ] 日志脱敏功能验证
- [ ] 熔断器故障注入测试
- [ ] SLO监控集成测试
- [ ] 审计日志完整性验证

### 16.4 Sprint4：文档与交付

- [ ] 编写开发者手册
- [ ] 编写运维手册
- [ ] 编写故障排查手册
- [ ] 准备发布说明

---

## 十八、附录：性能基准测试规范（V1.2新增）

> 本章节补充技术文档工程师终审意见：添加性能基准测试规范

### 17.1 事件发布延迟基准

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| P50延迟 | < 1ms | 10000次事件发布取中位数 |
| P95延迟 | < 5ms | 10000次事件发布取95分位 |
| P99延迟 | < 10ms | 10000次事件发布取99分位 |

### 17.2 插件加载时间基准

| 场景 | 目标值 |
|------|--------|
| 首次加载（冷启动） | < 500ms |
| 热加载（已加载模块） | < 100ms |

### 17.3 数据库查询性能基准

| 查询 | 目标值 |
|------|--------|
| 生成历史查询（1000条） | < 50ms |
| 插件状态查询 | < 10ms |
| 操作日志写入 | < 20ms |

### 17.4 Agent推理时间基准

| Agent | 场景 | 目标值 |
|-------|------|--------|
| Thinker | 章节大纲思考 | < 30s |
| Optimizer | 内容优化迭代 | < 20s |
| Validator | 质量评分验证 | < 15s |
| Planner | 任务规划分解 | < 10s |

---

## 十九、附录：V5强制保护机制（V1.3新增）

> **保护级别**：🔒🔒🔒 最高优先级保护（不可破坏性修改、不可删除、不可绕过）

### 18.1 五大核心功能模块保护

V5迁移升级为Agent Pro时，必须强制保护以下五大核心功能模块的功能实现和运行逻辑：

| 序号 | 模块名称 | 文件路径 | 插件类型 | 保护内容 |
|------|---------|----------|----------|---------|
| 🔒1 | **大纲解析** | `scripts/outline_parser_v3.py` | Analyzer | 解析逻辑、数据结构、章节树构建 |
| 🔒2 | **风格学习** | `scripts/enhanced_style_learner_v2.py` | Analyzer | 7维度分析算法、风格档案生成 |
| 🔒3 | **人物设定** | `scripts/enhanced_character_manager.py` | Analyzer | 人物档案、关系网络、一致性检查 |
| 🔒4 | **世界观构建** | `scripts/universal_worldview_parser.py` | Analyzer | 世界观元素分类、解析算法、一致性验证 |
| 🔒5 | **热榜功能** | `scripts/hot_ranking/` | Tool | 数据抓取、排行榜分析、创意灵感生成 |

### 18.2 【评分反馈，循环优化生成流程】工作流保护

**核心工作流强制保护**（不可修改运行逻辑）：

```
┌─────────────────────────────────────────────────────────────┐
│           【评分反馈，循环优化生成流程】核心算法              │
├─────────────────────────────────────────────────────────────┤
│  Step 1: 上下文构建 → Step 2: AI生成 → Step 3: 质量验证    │
│      ↓                                                      │
│  Step 4: 评分判断（≥0.8 且有【本章完】→ 完成）              │
│      ↓（否则）                                              │
│  Step 5: 反馈生成 → Step 6: 迭代优化（最多5次）→ Step 2    │
│      ↓（超限）                                              │
│  Step 7: 取最高分结果 + 警告日志                            │
└─────────────────────────────────────────────────────────────┘
```

**保护规则**（不可变更）：

| 规则 | 内容 | 强制要求 |
|------|------|---------|
| 🔒1 | 【本章完】标记必须存在 | 三重保险：提示词要求+智能补充+双重检查 |
| 🔒2 | 评分阈值 ≥ 0.8 | 低于阈值必须迭代优化 |
| 🔒3 | 迭代上限 5 次 | 超过则取最高分结果 |
| 🔒4 | 8维度评分权重固定(V1.7) | 字数8%/知识点引用8%/大纲13%/风格19%/人设19%/世界观12%/逆向反馈11%/自然度10% |
| 🔒5 | 上下文记忆前 5 章 | 保持连贯性 |

**保护模块**：

| 模块 | 文件 | 代码行数 |
|------|------|---------|
| 🔒 上下文构建 | `scripts/context_builder.py` | ~600行 |
| 🔒 迭代生成 | `scripts/iterative_generator_v2.py` | ~700行 |
| 🔒 加权验证 | `scripts/enhanced_weighted_validator.py` | ~1300行 |
| 🔒 生成入口 | `scripts/optimized_generator_v2.py` | ~400行 |

### 18.3 运行时保护机制

**PluginRegistry V1.3 保护检查**：

```python
# core/plugin_registry.py

# V5核心模块ID列表（不可变更，与实际部署一致）
V5_PROTECTED_MODULES = [
    "outline-parser-v3",       # 大纲解析
    "style-learner-v5",        # 风格学习
    "character-manager-v1",    # 人物管理
    "worldview-parser-v1",     # 世界观解析
    "context-builder-v1",      # 上下文构建
    "iterative-generator-v2",  # 迭代生成
    "quality-validator-v1",    # 加权验证
    "novel-generator-v3",      # 生成入口
    "hot-ranking-v1",          # 热榜功能
]

class PluginProtectionError(Exception):
    """插件保护异常 - 尝试操作受保护模块时抛出"""
    pass

def unregister(self, plugin_id: str) -> bool:
    # V5保护模块检查
    if plugin_id in V5_PROTECTED_MODULES:
        raise PluginProtectionError(f"禁止卸载V5保护模块: {plugin_id}")
    # ...
```

---

## 二十、附录：新增功能接口预设（V1.3新增）

> **预设状态**：接口已定义，实现待开发
> **优先级**：P1（Sprint3-4）

### 19.1 创作进度管理（ProgressManager）

**功能描述**：跟踪和管理小说创作进度

**预设接口**：

```python
class ProgressStatus(str, Enum):
    NOT_STARTED = "not_started"
    IN_PROGRESS = "in_progress"
    COMPLETED = "completed"
    PAUSED = "paused"

class ProgressManager:
    def get_project_progress(self, project_id: str) -> Dict
    def get_chapter_progress(self, chapter_id: str) -> ChapterProgress
    def update_chapter_progress(self, chapter_id: str, status: ProgressStatus, ...) -> bool
    def get_progress_timeline(self, project_id: str) -> List[Dict]
    def export_progress_report(self, project_id: str, format: str = "md") -> str
```

**实现要点**：
- 基于 SQLite 存储进度数据
- 支持可视化进度条
- 支持进度导出和报告生成

### 19.2 逆向优化（ReverseOptimizer）

**功能描述**：根据已写内容反向优化设定文件

**预设接口**：

```python
class OptimizationType(str, Enum):
    STYLE = "style"
    CHARACTER = "character"
    WORLDVIEW = "worldview"
    PLOT = "plot"

class ReverseOptimizer:
    def analyze_written_content(self, chapters: List[str], analysis_types: List[OptimizationType]) -> Dict
    def optimize_style_profile(self, current_profile: Dict, written_chapters: List[str]) -> Tuple[Dict, List[str]]
    def optimize_character_profiles(self, current_profiles: Dict, written_chapters: List[str]) -> Tuple[Dict, List[str]]
    def optimize_worldview_elements(self, current_worldview: Dict, written_chapters: List[str]) -> Tuple[Dict, List[str]]
    def batch_optimize(self, project_path: str, optimization_types: List[OptimizationType], auto_apply: bool = False) -> Dict
```

**实现要点**：
- 调用 StyleLearner 分析已写内容的风格
- 调用 CharacterManager 检测人物行为变化
- 调用 WorldviewParser 检测世界观元素
- 生成优化建议，支持人工审核

### 19.3 一键生成前期内容（PregenerationService）

**功能描述**：根据大纲自动生成创作所需的前期内容

**预设接口**：

```python
class PregenerationTask(str, Enum):
    CHARACTER_PROFILES = "character_profiles"    # 人物小传
    WORLDVIEW_ELEMENTS = "worldview_elements"    # 世界观细节
    PLOT_OUTLINE = "plot_outline"               # 情节大纲
    STYLE_GUIDE = "style_guide"                 # 风格指南
    OPENING_HOOKS = "opening_hooks"             # 开篇钩子

class PregenerationService:
    def analyze_outline(self, outline_content: str) -> Dict
    def generate_character_profiles(self, outline: str, character_names: List[str], ...) -> Dict
    def generate_worldview_elements(self, outline: str, element_types: List[str]) -> Dict
    def generate_plot_outline(self, outline: str, granularity: str = "chapter") -> PregenerationResult
    def generate_style_guide(self, outline: str, reference_texts: Optional[List[str]] = None) -> PregenerationResult
    def generate_opening_hooks(self, outline: str, style_guide: Dict, count: int = 3) -> List[PregenerationResult]
    def one_click_pregeneration(self, outline_path: str, tasks: List[PregenerationTask], ...) -> Dict
```

**实现要点**：
- 调用 OutlineParser 解析大纲
- 调用 LLM 生成人物小传、世界观细节
- 支持批量生成和一键生成
- 支持人工审核和修改

### 19.4 逆向反馈功能（ReverseFeedbackPlugin）

**⏳ 实现状态**: UI框架已搭建，插件和后端逻辑待实现

**验证结果**:
- ✅ UI按钮和布局已完成 (gui_main.py:2841-2953)
- ⏳ ReverseFeedbackPlugin 未实现 (plugins/目录下无 reverse-feedback-analyzer/)
- ⏳ 所有回调方法为占位符 (gui_main.py:4236-4262)
- ⏳ SettingsCorrector 模块未实现

**功能描述**：根据已生成的章节内容，反向修正项目中的大纲、人物设定和世界观设定，确保后续生成内容与已有章节保持一致性。

**使用场景**：
- 用户已生成若干章节，发现某些角色行为与初始设定不符，希望自动修正设定
- 检查已写章节是否与大纲/人设/世界观存在矛盾，获得修改建议
- 删除某个章节后，希望系统自动评估设定是否需要回退

**预设接口**：

```python
from dataclasses import dataclass
from typing import List, Dict, Any

@dataclass
class ConsistencyIssue:
    """冲突项"""
    type: str  # "outline", "character", "worldview"
    description: str
    severity: str  # "low", "medium", "high"
    suggested_fix: str
    original_content: str
    chapter_reference: str  # 章节标题或ID

class ReverseFeedbackPlugin(AnalyzerPlugin):
    """逆向反馈分析插件 - 继承 AnalyzerPlugin"""

    @classmethod
    def get_metadata(cls) -> PluginMetadata:
        return PluginMetadata(
            id="reverse-feedback-analyzer",
            name="逆向反馈分析器",
            version="1.0.0",
            description="根据已生成章节分析设定冲突",
            author="QClaw",
            plugin_type=PluginType.ANALYZER,
            api_version="1.0"
        )

    def analyze(self, content: str, options: Dict[str, Any] = None) -> Dict[str, Any]:
        """
        分析章节与当前项目设定的冲突

        Args:
            content: 章节文本
            options: 应包含当前项目设定（outline, characters, worldview）

        Returns:
            {
                "success": bool,
                "issues": List[ConsistencyIssue],
                "summary": str
            }
        """
        pass

    def get_supported_formats(self) -> List[str]:
        return ["txt", "md"]

    def get_analysis_types(self) -> List[str]:
        return ["consistency_check", "conflict_detection"]

# 设定修正模块
class SettingsCorrector:
    """设定修正器"""

    def generate_corrections(
        self,
        issues: List[ConsistencyIssue],
        current_settings: Dict[str, Any]
    ) -> Dict[str, Any]:
        """
        根据冲突列表生成修正后的设定

        Args:
            issues: 冲突列表
            current_settings: 当前设定（outline, characters, worldview）

        Returns:
            {
                "updated_outline": str,
                "updated_characters": List[Dict],
                "updated_worldview": str,
                "suggestions": List[str]
            }
        """
        pass
```

**数据结构**：

```json
{
  "project_name": "示例项目",
  "chapters": [
    {
      "id": "chap_001",
      "title": "第一章 启程",
      "content": "...",
      "word_count": 3500,
      "status": "verified",
      "created_at": "2026-03-24T10:00:00Z"
    }
  ],
  "outline": "...",
  "characters": [...],
  "worldview": "..."
}
```

**实现要点**：
- 调用现有 AnalyzerPlugin 从章节中提取人物、情节、世界观元素
- 调用大模型API，将提取的元素与设定对比，生成冲突列表
- 为每个冲突生成修正建议
- 支持章节增删改查，删除章节时同步从项目文件中移除
- 分析过程中断/异常时，UI有提示

**依赖关系**：
- 依赖插件：outline-parser-v3、character-manager-v1、worldview-parser-v1
- 依赖大模型API（DeepSeek/OpenAI）进行语义分析
- 依赖 EventBus 发布分析进度事件

### 19.5 续写功能（ContinuationPlugin）

**⏳ 实现状态**: UI框架已搭建，插件和后端逻辑待实现

**验证结果**:
- ✅ UI按钮和布局已完成 (gui_main.py:3162-3199)
- ⏳ ContinuationPlugin 未实现 (plugins/目录下无 continuation-generator/)
- ⏳ 所有回调方法为占位符 (gui_main.py:4504-4516)
- ⏳ 多版本管理逻辑未实现

**功能描述**：基于已生成的小说内容（起始文本），指定续写字数和方向，调用大模型生成后续内容。支持生成多个版本供用户选择，并可将满意的版本保存为项目章节。

**使用场景**：
- 用户写了一段开头，希望AI继续写下去
- 用户对当前生成结果不满意，想换一种风格或方向重新生成
- 用户需要生成多个版本，从中挑选最佳

**预设接口**：

```python
class ContinuationPlugin(GeneratorPlugin):
    """续写生成插件 - 继承 GeneratorPlugin"""

    @classmethod
    def get_metadata(cls) -> PluginMetadata:
        return PluginMetadata(
            id="continuation-generator",
            name="续写生成器",
            version="1.0.0",
            description="基于起始文本续写小说",
            author="QClaw",
            plugin_type=PluginType.GENERATOR,
            api_version="1.0"
        )

    def generate(self, request: GenerationRequest) -> GenerationResult:
        """
        生成续写内容

        Args:
            request: 包含起始文本、项目设定等

        Returns:
            GenerationResult 包含生成内容和元数据
        """
        pass

    def stream_generate(
        self,
        context: Dict[str, Any],
        params: Dict[str, Any],
        callback: callable
    ) -> Dict[str, Any]:
        """流式生成（可选）"""
        pass

    def validate_request(self, request: GenerationRequest) -> tuple[bool, List[str]]:
        """验证请求是否有效"""
        pass

    def get_generation_options(self) -> Dict[str, Any]:
        return {
            "word_count": {"type": "int", "range": [100, 10000], "default": 500},
            "direction": {"type": "str", "options": ["自然续写", "戏剧化", "轻松化"]},
            "temperature": {"type": "float", "range": [0.0, 2.0], "default": 0.7}
        }
```

**Prompt模板示例**：

```markdown
你是一位资深小说作家。请根据以下设定和起始文本，续写故事。

【世界观】
{worldview}

【人物设定】
{characters}

【故事大纲】
{outline}

【当前文本】
{starting_text}

请续写约{word_count}字，风格{style_direction}。仅输出续写内容，不要包含任何解释。
```

**数据结构**：

```json
{
  "versions": [
    {
      "id": "ver_001",
      "text": "...",
      "timestamp": "2026-03-24T11:00:00Z",
      "params": { "word_count": 500, "direction": "自然续写" }
    }
  ],
  "selected_version": "ver_001"
}
```

**实现要点**：
- 上下文智能注入：自动读取当前项目的大纲、人设、世界观作为生成上下文
- 多版本生成：设置不同的随机种子（temperature、top_p）来生成多个版本
- 前端展示版本列表，用户点击切换预览
- 用户选择"保存"时，将选定版本文本保存为项目新章节

**依赖关系**：
- 依赖大模型API（DeepSeek/OpenAI）进行文本生成
- 依赖 AsyncEventHandler 处理异步生成，避免UI卡顿
- 依赖项目管理模块保存章节

### 19.6 快捷创作功能（QuickCreationPlugin）

**⏳ 实现状态**: UI框架已搭建，插件和后端逻辑待实现

**验证结果**:
- ✅ UI按钮和布局已完成 (gui_main.py:2967-3093)
- ✅ 四结果框布局已完成 (世界观/大纲/人设/情节 2×2布局)
- ⏳ QuickCreationPlugin 未实现 (plugins/目录下无 quick-creator/)
- ⏳ 生成回调为占位符，仅显示"正在生成中..." (gui_main.py:4268-4293)
- ✅ 保存/导出UI已完成

**功能描述**：通过输入关键词（如"修仙世界、现代都市"）和上传参考文本（可选），一键生成完整的小说设定，包括世界观、大纲、人物设定、关键情节。支持单项生成，并可导出为独立文件或导入当前项目。

**使用场景**：
- 用户灵感迸发，想快速构建一个小说世界
- 用户已有一些参考文本（如喜欢的作品片段），希望AI模仿风格生成设定
- 用户只需要生成大纲，其他设定自己设计

**预设接口**：

```python
class QuickCreationPlugin(ToolPlugin):
    """快捷创作插件 - 继承 ToolPlugin"""

    @classmethod
    def get_metadata(cls) -> PluginMetadata:
        return PluginMetadata(
            id="quick-creator",
            name="快捷创作器",
            version="1.0.0",
            description="根据关键词和参考文本生成小说设定",
            author="QClaw",
            plugin_type=PluginType.TOOL,
            api_version="1.0"
        )

    def execute(self, action: str, params: Dict[str, Any]) -> Dict[str, Any]:
        """
        执行生成操作

        Args:
            action: "generate_all", "generate_worldview", "generate_outline",
                    "generate_characters", "generate_plot"
            params: 包含 keywords, reference_texts 等

        Returns:
            {
                "success": bool,
                "worldview": str,
                "outline": str,
                "characters": List[Dict],
                "plot": str
            }
        """
        pass
```

**Prompt模板示例**：

```markdown
请根据以下关键词和参考文本，生成一个完整的奇幻小说设定。

【关键词】
{keywords}

【参考文本（可选）】
{reference_texts}

请输出以下内容：

1. 世界观：描述世界的物理规则、文明、历史等。
2. 大纲：分章节的故事概要。
3. 人物设定：主要角色列表，包括姓名、性格、背景、能力。
4. 关键情节：故事的核心冲突和高潮点。

要求：
- 风格统一，设定合理。
- 输出格式为Markdown，各部分用标题分隔。
```

**数据结构**：

```json
{
  "worldview": "这是一片被神秘力量笼罩的大陆...",
  "outline": "第一章：觉醒\n第二章：试炼...",
  "characters": [
    {
      "name": "林逸",
      "gender": "男",
      "age": 18,
      "personality": "坚毅、勇敢",
      "background": "孤儿，被师傅收养",
      "ability": "御剑术"
    }
  ],
  "plot": "主角发现世界真相，对抗邪恶组织..."
}
```

**实现要点**：
- 参考文本解析：利用NLP和大模型提取参考文本中的风格、元素
- 结构化输出：确保生成的设定与现有项目文件格式兼容
- 导入导出：支持保存为独立项目文件（.md/.json）或合并到当前项目
- 一致性保证：若生成全部设定，需确保各设定间协调（例如人设符合世界观）

**依赖关系**：
- 依赖大模型API进行生成
- 依赖文件服务保存/读取参考文本和导出结果
- 依赖项目管理模块读取/写入项目设定

---

### 19.7 未实装功能审查总结

| 功能 | UI状态 | 插件状态 | 后端逻辑 | 优先级 |
|------|--------|---------|---------|--------|
| 逆向反馈 | ✅ 已搭建 | ⏳ 待实现 | ⏳ 待实现 | P1 |
| 续写功能 | ✅ 已搭建 | ⏳ 待实现 | ⏳ 待实现 | P1 |
| 快捷创作 | ✅ 已搭建 | ⏳ 待实现 | ⏳ 待实现 | P1 |

**已完成工作**:
- GUI框架完整搭建
- 按钮布局和回调方法已定义
- 数据结构设计完成

**待开发工作**:
- 三个插件的主类实现
- AI服务调用逻辑
- 结果解析和展示逻辑

---

## 参考文档

1. [✅️AgentPRO最全经验文档.md](./✅️AgentPRO最全经验文档.md) - 综合经验文档（包含AI工程7模块完整代码）
2. [✅️Novel Writing Assistant-Agent Pro程序框架方案_V3.0.md](./✅️Novel Writing Assistant-Agent Pro程序框架方案_V3.0.md) - 完整架构设计方案
3. [Agent Pro实现指南.md](./Agent Pro实现指南.md) - 详细实现指南
4. [✅️V5.5小说生成核心功能代码样本.md](./✅️V5.5小说生成核心功能代码样本.md) - V5核心功能
5. [未实装功能.txt](./未实装功能.txt) - 待开发功能技术文档代码

---

## 文档审查机制
- AI工程师：AI工程完善度、模型管理审查
- 后端工程师：接口可实现性、并发安全、错误处理审查
- 技术文档工程师：文档规范性、一致性、可读性审查
- 每次重大更新后，需经上述角色审查后方可发布

---

## 文档修订记录（终审意见）

| 终审角色 | 评审日期 | 评审结论 | 核心修订 |
|----------|----------|----------|----------|
| AI工程师 | 2026-03-21 | 有条件通过 | 补充容器化方案、代码签名机制 |
| 后端架构师 | 2026-03-21 | 有条件通过 | 补充AI接口实现要点、热插拔策略 |
| 技术文档工程师 | 2026-03-21 | 通过 | 补充ADR、故障排查、风险提示 |

**文档状态**：最终修订完成，V1.2版本已整合全部终审意见

**最终评审完成日期**：2026-03-21

---

## 二十一、附录：知识库验证器（V1.9新增）

### 21.1 模块概述

**模块名称**: KnowledgeVerifierPlugin  
**插件类型**: ToolPlugin  
**位置**: `data/知识库验证器/`  
**功能**: 批量检查和清理低质量、重复的知识词条  
**验收评分**: 100/100（满分）

### 21.2 核心功能

| 功能 | 说明 | 性能 |
|------|------|:----:|
| 三层查重 | 完全匹配+语义相似+内容重叠 | 40%提升 |
| 五维度质量评估 | 内容长度/关键词/参考作品/相关性/语言质量 | 100%权重 |
| Claw优化集成 | EventBus事件+记忆文件+Prompt优化建议 | ✅ |
| 安全文件清理 | 只删词条+自动备份+用户确认+恢复功能 | ✅ |
| 配置化阈值 | TOML配置文件支持自定义 | ✅ |
| 统计监控 | 清理统计看板+定期调度 | ✅ |

### 21.3 架构设计

```
KnowledgeVerifierPlugin (ToolPlugin接口实现)
├── KnowledgeVerifier (主控制器)
│   ├── DeduplicationEngine (查重引擎)
│   │   ├── 完全匹配去重 (O(n))
│   │   ├── 语义相似度去重 (LanceDB向量, 阈值0.85)
│   │   └── 内容重叠去重 (SimHash优化, Jaccard≥0.70)
│   ├── QualityEvaluator (质量评估器)
│   │   └── 五维度评分 (内容长度25%+关键词20%+参考作品20%+相关性20%+语言质量15%)
│   ├── ClawOptimizer (Claw集成)
│   │   └── 优化报告生成+EventBus事件发布
│   └── FileCleaner (文件清理器)
│       └── 安全删除+备份+恢复功能
├── ConfigLoader (配置加载器)
└── StatsMonitor (统计监控器)
```

### 21.4 接口定义

```python
class KnowledgeVerifierPlugin:
    """知识库验证器 - ToolPlugin接口实现"""
    
    def initialize(self) -> bool:
        """初始化插件"""
        pass
    
    def verify_all(self, 
                  enable_dedup: bool = True,
                  enable_quality: bool = True,
                  dry_run: bool = False,
                  progress_callback: Optional[callable] = None) -> Dict[str, Any]:
        """批量验证所有知识点"""
        pass
    
    def cleanup(self, 
               enable_dedup: bool = True,
               enable_quality: bool = True,
               progress_callback: Optional[callable] = None) -> Dict[str, Any]:
        """GUI便捷方法：直接执行清理"""
        pass
```

### 21.5 事件定义

**EventBus事件**: `knowledge.cleanup.completed`

```python
class KnowledgeCleanupEvent(BaseModel):
    timestamp: datetime
    source: str  # "KnowledgeVerifierPlugin"
    removed_count: int
    reasons: Dict[str, int]  # 删除原因统计
    common_issues: List[str]  # 共性问题
    optimization_suggestions: List[str]  # 优化建议
```

### 21.6 配置文件

**位置**: `data/知识库验证器/verifier_config.toml`

```toml
[duplicates]
similarity_threshold = 0.85      # 语义相似度阈值
overlap_threshold = 0.70         # 内容重叠度阈值
simhash_hamming_threshold = 10   # SimHash汉明距离阈值

[quality]
quality_threshold = 6.0          # 质量评分阈值（0-10分）
content_length_weight = 0.25     # 内容长度权重
keyword_count_weight = 0.20      # 关键词权重
reference_count_weight = 0.20    # 参考作品权重
content_relevance_weight = 0.20  # 内容相关性权重
language_quality_weight = 0.15   # 语言质量权重

[backup]
auto_cleanup_days = 30           # 备份自动清理天数
```

### 21.7 性能指标

| 操作 | 数据量 | 耗时 |
|------|:------:|:----:|
| 完全匹配去重 | 1000条 | <1秒 |
| 内容重叠去重(SimHash) | 1000条 | <15秒 |
| 质量评估 | 1000条 | <10秒 |
| 完整清理流程 | 1130条 | <70秒 |

### 21.8 GUI集成

| 按钮 | 功能 | 位置 |
|------|------|------|
| 🧹 清除垃圾 | 执行知识库清理 | 知识库管理界面 |
| 🔙 恢复清理 | 从备份恢复知识点 | 知识库管理界面 |
| 📊 清理统计 | 查看清理历史和趋势 | 知识库管理界面 |

### 21.9 相关文档

- 《11.19垃圾知识词条清理方案✅️.md》（V3.0）- 完整整合版
- 《2.2 插件接口定义✅️.md》（V2.2）- ToolPlugin接口规范

---

**文档结束**
