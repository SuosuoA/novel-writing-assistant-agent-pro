# UI搭建说明文档

## 概述

Novel Writing Assistant-Agent Pro 的GUI界面采用现代科技玻璃态（Glass Morphism）设计风格，实现无边框透明窗口、毛玻璃效果、发光边框等现代UI特性。

**版本**: V2.2
**创建日期**: 2026-03-22
**更新日期**: 2026-03-22
**技术栈**: Python 3.12.x + Tkinter + sv_ttk + Windows Acrylic API

---

## 设计理念

### 色彩体系

| 名称 | 色值 | 用途 |
|------|------|------|
| 主色（科技蓝） | #0078D4 | 主色调、按钮高亮 |
| 主色浅 | #60CDFF | 悬停态、链接 |
| 主色深 | #003D6B | 按下态 |
| 强调色-粉 | #FF6B9D | 热榜、人物 |
| 强调色-紫 | #8B5CF6 | 插件、世界观 |
| 强调色-绿 | #10B981 | 项目管理、风格 |
| 强调色-橙 | #F59E0B | 设置、逆向 |
| 玻璃背景 | #1E1E1E | 主背景 |
| 玻璃表面 | #2D2D2D | 卡片、输入框 |
| 文字主色 | #FFFFFF | 主文字 |
| 文字次色 | #B0B0B0 | 次要文字 |

### 视觉特点

1. **无边框窗口**：完全无边框设计，自定义标题栏，移除系统白边框
2. **毛玻璃效果**：Windows Acrylic/Mica效果
3. **悬浮卡片**：内容以卡片形式悬浮展示
4. **发光边框**：选中态带颜色高亮
5. **深色主题**：科技感深色UI

### 交互原则

1. **响应式按钮**：防抖保护 + 异步执行 + 加载状态
2. **页面切换**：侧边栏导航，无横向标签页
3. **线程安全回调**：所有UI更新通过 `root.after(0, callback)` 调度
4. **玻璃态动画**：悬停颜色过渡、点击反馈

---

## 核心模块

### 1. GlassWindowManager - 玻璃窗口管理器

**功能**：
- 启用Windows Acrylic效果
- 设置无边框窗口（完全移除系统边框）
- 控制窗口透明度

**使用示例**：
```python
from gui_main import GlassWindowManager

# 初始化
glass_mgr = GlassWindowManager(root)

# 启用Acrylic效果
glass_mgr.enable_acrylic(color=0x80000000)  # ARGB格式

# 设置无边框（移除系统边框和标题栏）
glass_mgr.make_frameless()

# 设置透明度
glass_mgr.set_window_opacity(0.95)
```

### 2. GlassTheme - 玻璃态主题配置

**功能**：
- 定义所有颜色变量
- 配置ttk Style
- 统一UI风格

**使用示例**：
```python
from gui_main import GlassTheme

# 使用主题色
bg_color = GlassTheme.GLASS_BG
primary = GlassTheme.PRIMARY

# 应用到Style
style = ttk.Style()
GlassTheme.apply_to_style(style)
```

### 3. ResponsiveButton - 响应式按钮

**特性**：
- 500ms防抖保护
- 异步执行耗时操作
- 自动禁用/启用
- 加载状态文本显示

**使用示例**：
```python
btn = ResponsiveButton(
    parent=frame,
    text="生成章节",
    command=self._on_generate,
    async_handler=self._async_handler,
    debounce_ms=500,
    auto_disable=True
)
```

### 4. CustomTitleBar - 自定义标题栏

**功能**：
- 窗口拖动
- 最小化/最大化/关闭按钮
- 显示完整标题 "Novel Writing Assistant-Agent Pro"

**特性**：
- 完全替代系统标题栏
- 玻璃态风格
- 悬停效果
- 双击最大化

### 5. MainWindow - 主窗口类

**结构**：
```
MainWindow
├── 自定义标题栏（拖动/最小化/最大化/关闭）
├── 侧边栏（导航按钮）
│   ├── 热榜
│   ├── 工作台
│   ├── 创作进度
│   ├── 项目管理
│   ├── 插件管理
│   └── 设置
├── 内容区（根据侧边栏选择显示）
│   ├── 热榜页面
│   ├── 工作台页面
│   ├── 创作进度页面
│   ├── 项目管理页面
│   ├── 插件管理页面
│   └── 设置页面
└── 状态栏（项目名称/字数统计/状态/AI连接/后台任务进度）
```

---

## 页面结构（V2.2更新）

### 1. 热榜页面

**位置**：侧边栏第一个按钮

**功能**：
- 展示三大网站小说排行榜（番茄、起点、晋江）前10名
- 题材热度榜（男频/女频）前5名
- 设定类型/创作流派榜（男频/女频）前5名
- 网络作家排行榜前10名
- 清缓存、更新数据按钮

**特点**：
- 完整迁移V5热榜实现
- 四个独立数据区块（网站榜单/题材热度/类型流派/作家排行）
- 滚动容器支持鼠标滚轮
- 横向三列布局展示网站榜单
- 条形图可视化题材热度

### 2. 工作台页面（核心功能区）

**位置**：侧边栏第二个按钮

**布局结构**：
- **上半部分**：八个功能按钮（两行四列，带悬停高亮效果）
- **下半部分**：当前选中功能的详细内容区

**性能优化（V2.2）**：
- **延迟创建机制**：首次切换到标签页时才创建内容，避免一次性创建所有标签页导致卡顿
- **页面缓存机制**：创建后缓存到 `_workbench_tabs` 字典，后续切换直接显示

**子功能模块**：

| 序号 | 功能名称 | 图标 | 核心功能 | 新建功能 |
|------|----------|------|----------|----------|
| 1 | 世界观管理 | 🌍 | 导入/创建/编辑/查看/批量删除小说世界观设定 | ✅ 弹窗新建 |
| 2 | 人物设定 | 👥 | 人物列表、新建/编辑人物、关系图谱、出场章节 | ✅ 弹窗新建 |
| 3 | 大纲管理 | 📄 | 大纲导入/导出、树形结构、章节编辑器、进度条 | ✅ 弹窗新建 |
| 4 | 风格学习 | ✍️ | 范文管理、七维度风格分析、风格档案库、权重编辑 | ✅ 弹窗创建模板 |
| 5 | 开始创作 | 🚀 | 生成配置、进度展示、评分反馈循环、结果预览 | ⏳ 待实现 |
| 6 | 逆向反馈 | 🔄 | 已完成章节列表、文本分析、反馈报告、自动修正 | ✅ 新增章节列表 |
| 7 | 快捷创作 | ⚡ | 一次性生成世界观/大纲/人设/关键情节四个结果 | ✅ 四结果框 |
| 8 | 续写功能 | 📝 | 智能续写、方向控制、多次尝试、最佳版本选择 | ⏳ 待实现 |

**世界观管理详细功能**：
- 文件导入：支持TXT/DOCX格式
- 世界观项目列表：名称、核心元素、状态、修改时间
- 详情预览：树形结构展示各元素
- 关联要素：人物、事件、物品与世界观的关系绑定
- **新建弹窗**：包含名称、核心元素、详细描述输入

**人物设定详细功能**：
- 人物列表：头像、姓名、角色类型、状态、情绪、出场章节
- **新建人物弹窗**：姓名、角色类型、外貌描述、性格特点、背景故事、能力特长
- 人物详情页：多标签展示完整档案、关系图谱、关键章节-人生轨迹
- 关系网络图：可视化展示角色间关系（可拖拽编辑）

**大纲管理详细功能**：
- 大纲树形结构：按卷/章/节组织，可拖拽排序
- 章节编辑器：标题、内容摘要、字数目标、关键事件
- 导入/导出：支持TXT/DOCX格式
- 大纲进度条：显示已完成章节占比
- **新建大纲弹窗**：名称、小说类型、目标字数、简介

**风格学习详细功能**：
- 范文管理：上传、删除范文文件
- 风格分析器：七维度评分（词汇、句式、修辞、情感、节奏、结构、细节）
- 风格档案库：保存已分析的风格，可命名、切换、编辑权重
- **创建风格模板弹窗**：名称、七维度滑块评分、风格描述
- 风格应用：将选定风格应用到生成器

**开始创作详细功能**：
- 生成配置：选择风格、章节范围、生成字数、出场人物、生成模式、温度参数
- 生成过程展示：实时显示生成进度、当前章节、评分反馈循环优化过程
- 生成结果展示：小说正文预览区，支持分章浏览
- 保存/导出：保存为项目文件，导出为TXT/DOCX等格式

**逆向反馈详细功能（V2.2增强）**：
- **已完成章节列表**：显示所有已上传章节，包含章节号、标题、字数、状态
- **章节管理**：上传章节、删除章节、标记完成/未完成、右键菜单快捷操作
- 文本分析：上传或选择已生成内容，运行分析（一致性、逻辑漏洞、人设偏离、风格匹配度）
- 反馈报告：问题列表、建议修改方向、优先级
- 自动修正：一键将反馈结果应用到相关模块
- 细分修正：调整大纲细节、修改人物性格、补充人物轨迹、增减世界观设定

**快捷创作详细功能（V2.2重构）**：
- **参考文本上传**：上传案例文本或已有设定作为生成参考
- **上传状态指示**：显示四种文本（世界观、大纲、人设、关键情节）的上传状态
- 需求描述：关键词模式输入
- **一次性四结果生成**：同时生成世界观、大纲、人设、关键情节四个结果框（2x2布局）
- **灵活生成模式**：一键生成全部 或 单独生成某一个
- 导入当前项目：将生成结果导入到当前项目

**续写功能详细功能**：
- 续写起点：选择已有文本的末尾位置（光标定位）
- 续写长度：指定字数或自然段落数
- 方向控制：自然续写、制造冲突、引入新角色、转折情节、高潮推进
- 结果预览：显示续写内容，支持多次尝试、选择最佳版本

**交互**：
- 上半部分按钮悬停变色，点击激活显示对应颜色
- 下半部分动态切换显示当前功能详细内容
- 选中按钮保持高亮状态
- 右键菜单支持（人物列表、大纲树、章节列表等）

### 3. 创作进度页面

**位置**：侧边栏第三个按钮（新增）

**功能**：
- 显示当前项目状态
- 统计数据（章节、字数、目标）
- 刷新、导出、重置按钮

### 4. 项目管理页面

**位置**：侧边栏第四个按钮

**子模块**：
- 当前项目信息
- 新建项目（弹窗）
- 打开项目
- 导出备份

**新增特性**：
- 新建项目弹窗对话框
- 项目名称和路径输入
- 路径浏览选择

### 5. 插件管理页面

**位置**：侧边栏第五个按钮

**功能**：
- 插件列表（Treeview展示）
- 刷新、安装、卸载按钮
- 插件状态显示（active/inactive）
- 插件类型分类（Analyzer/Generator/Validator/Tool）

### 6. 设置页面

**位置**：侧边栏第六个按钮

**配置项**：
- **AI服务配置**：
  - 服务模式：本地大模型 / 线上API
  - API提供商：DeepSeek / OpenAI / Anthropic / Ollama
  - 模型选择：deepseek-chat / deepseek-reasoner / gpt-4 / claude-3
  - API Key输入（密码模式）
  - Temperature滑块（0.0-2.0）
- **界面设置**：
  - 主题：深色 / 浅色
  - 字体大小：10-20
  - 默认窗口大小：1280x720 / 1366x768 / 1920x1080
- **文件路径设置**：
  - 默认保存位置：路径选择
  - 自动备份间隔：10-120分钟
- **偏好学习设置**：
  - AI偏好学习开关
  - 清除学习数据按钮
  - 导出偏好配置按钮
- 配置持久化到config.yaml

---

## 状态栏（V2.2增强）

**信息显示**：
1. **项目名称**：当前打开的项目名称
2. **字数统计**：当前文档字数统计（带千位分隔符）
3. **状态消息**：操作状态提示
4. **后台任务进度**：显示正在运行的后台任务（如热榜更新、风格分析）
5. **AI连接状态**：在线/离线/未连接（带颜色指示）

**颜色指示**：
- 在线：绿色 (#10B981)
- 离线：红色 (#EF4444)
- 未连接：橙色 (#F59E0B)

---

## 快捷键（V2.2增强）

| 快捷键 | 功能 |
|--------|------|
| Ctrl+W | 切换到工作台 |
| Ctrl+P | 切换到项目管理 |
| Ctrl+S | 切换到设置 |
| Ctrl+G | 快速生成（切换到工作台快捷创作） |
| Ctrl+N | 新建项目 |
| Ctrl+O | 打开项目 |
| Ctrl+B | 备份项目 |
| F5 | 刷新当前页 |
| Escape | 取消操作 |

---

## 界面交互建议

1. **侧边栏导航**：点击按钮切换页面，悬停变色反馈
2. **右键快捷菜单**：在人物列表、大纲树、章节列表等区域提供右键快捷操作（编辑、删除、导出、标记完成等）
3. **工具栏快捷图标**：常用操作（保存、生成、分析）放在工作区顶部工具栏（待实现）
4. **实时保存**：所有编辑内容自动保存到项目文件夹，避免数据丢失（待实现）
5. **鼠标滚轮适配**：长页面使用鼠标滚轮翻页（已支持）

---

## 技术要点

### 玻璃效果实现

```python
# Windows Acrylic效果
class GlassWindowManager:
    def enable_acrylic(self, color: int = 0x99000000) -> bool:
        accent = AccentPolicy()
        accent.AccentState = self.ACCENT_ENABLE_ACRYLICBLURBEHIND
        accent.AccentFlags = 2
        accent.GradientColor = color
        
        data = WindowCompositionAttrData()
        data.Attribute = self.WCA_ACCENT_POLICY
        data.Data = ctypes.cast(ctypes.byref(accent), ctypes.c_void_p)
        data.SizeOfData = ctypes.sizeof(accent)
        
        return self.user32.SetWindowCompositionAttribute(self.hwnd, ctypes.byref(data))
    
    def make_frameless(self) -> None:
        """完全移除系统边框"""
        style = self.user32.GetWindowLongW(self.hwnd, self.GWL_STYLE)
        style &= ~(self.WS_CAPTION | self.WS_SYSMENU)
        style |= self.WS_THICKFRAME | self.WS_MINIMIZEBOX | self.WS_MAXIMIZEBOX
        self.user32.SetWindowLongW(self.hwnd, self.GWL_STYLE, style)
```

### 线程安全机制

```python
# 所有UI操作通过root.after调度
def _invoke_callback(self, callback: Callable, *args) -> None:
    if self._root is None:
        callback(*args)
    else:
        self._root.after(0, lambda: callback(*args))
```

### 页面切换机制

```python
def _switch_to_page(self, page_id: str) -> None:
    """切换到指定页面"""
    # 隐藏当前页面
    if self._current_page and self._current_page in self._pages:
        self._pages[self._current_page].pack_forget()
    
    # 创建或显示新页面
    if page_id not in self._pages:
        self._pages[page_id] = self._create_page(page_id)
    
    self._pages[page_id].pack(fill=tk.BOTH, expand=True, in_=self._content_frame)
    self._current_page = page_id
```

### 工作台延迟创建机制（V2.2新增）

```python
def _create_all_workbench_tabs(self):
    """预创建工作台标签页（延迟创建策略，仅初始化为None）"""
    # 所有标签页初始化为None，首次切换时才创建
    # 这样可以避免首次点击工作台时一次性创建所有标签页导致卡顿
    for tab_id in ["worldview", "characters", "outline", "style", 
                   "generation", "reverse", "quick", "continue"]:
        self._workbench_tabs[tab_id] = None

def _switch_workbench_tab(self, tab_id: str):
    """切换工作台标签页（延迟创建）"""
    # ... 隐藏其他标签页 ...
    
    # 延迟创建：首次切换时才创建标签页内容
    if self._workbench_tabs.get(tab_id) is None:
        create_method = {
            "worldview": self._create_worldview_content,
            # ... 其他方法 ...
        }.get(tab_id)
        if create_method:
            self._workbench_tabs[tab_id] = create_method()
    
    # 显示当前标签页
    if self._workbench_tabs.get(tab_id) is not None:
        self._workbench_tabs[tab_id].pack(fill=tk.BOTH, expand=True, in_=self._workbench_content_frame)
```

---

## 文件位置

- **主文件**: `gui_main.py`
- **核心模块**: `core/`
- **异步处理器**: `core/async_handler.py`
- **配置文件**: `config.yaml`

---

## 运行方法

```bash
cd "E:\WorkBuddyworkspace\Novel Writing Assistant-Agent Pro"
python gui_main.py
```

---

## V2.2更新记录（2026-03-22）

### 性能优化

1. **修复工作台点击卡顿问题**
   - 问题：首次点击工作台会卡一下
   - 原因：`_create_all_workbench_tabs()` 一次性创建所有8个标签页内容
   - 解决：改为延迟创建机制，所有标签页初始化为None，首次切换时才创建

### 功能实装

2. **世界观管理新建按钮**
   - 新增弹窗对话框
   - 包含：名称、核心元素、详细描述输入
   - 创建后自动添加到列表并更新预览

3. **人物设定新建按钮**
   - 新增弹窗对话框（可滚动）
   - 包含：姓名、角色类型、外貌描述、性格特点、背景故事、能力特长
   - 创建后自动添加到列表并更新详情

4. **大纲管理新建按钮**
   - 新增弹窗对话框
   - 包含：名称、小说类型、目标字数、简介
   - 创建后自动添加树形结构根节点

5. **风格学习创建模板按钮**
   - 新增弹窗对话框
   - 包含：名称、七维度滑块评分、风格描述
   - 创建后自动添加到风格档案库并更新分析器显示

6. **逆向反馈界面增强**
   - 新增【已完成章节列表】区域
   - 章节列表包含：章节号、标题、字数、状态
   - 支持上传章节、删除章节、标记完成/未完成
   - 新增右键菜单快捷操作

7. **快捷创作重构**
   - 改为一次性四个生成结果框（世界观、大纲、人设、关键情节）
   - 新增【参考文本上传】功能（替代原快捷模式选择）
   - 新增【上传状态指示】显示四种文本上传状态
   - 四结果框采用2x2布局
   - 支持【一键生成全部】或【单独生成某一个】

### 设置页面增强

8. **界面设置**
   - 新增字体大小设置
   - 新增默认窗口大小设置

9. **文件路径设置**（新增）
   - 默认保存位置选择
   - 自动备份间隔设置

10. **偏好学习设置**（新增）
    - AI偏好学习开关
    - 清除学习数据按钮
    - 导出偏好配置按钮

### 状态栏增强

11. **状态栏信息扩展**
    - 新增字数统计显示（带千位分隔符）
    - 新增后台任务进度显示
    - AI连接状态颜色指示（在线绿/离线红/未连接橙）

### 快捷键增强

12. **新增快捷键**
    - Ctrl+N：新建项目
    - Ctrl+O：打开项目
    - Ctrl+B：备份项目
    - Escape：取消操作

### 右键菜单支持

13. **已完成章节列表右键菜单**
    - 标记为完成
    - 标记为未完成
    - 删除章节

---

## 待完善项

1. [ ] 热榜数据实时获取（集成HotRankingController）
2. [ ] 开始创作完整实现（模型选择、章节选择、生成配置）
3. [ ] 续写功能完整实现
4. [ ] 与V5核心模块集成
5. [ ] 主题切换（深/浅色）
6. [ ] 工具栏快捷图标（保存、生成、分析）
7. [ ] 实时保存机制
8. [ ] 国际化支持
9. [ ] 无边框窗口的边缘调整大小优化
