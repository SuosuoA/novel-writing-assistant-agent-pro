# 加权验证器集成逆向反馈评分说明

> **版本**: V1.0  
> **创建日期**: 2026-03-24  
> **作者**: 高级开发工程师  
> **状态**: ✅ 已完成

---

## 一、任务概述

### 1.1 任务目标

修改 `plugins/quality-validator-v1/plugin.py`（加权验证器），在验证生成内容时，调用逆向反馈分析器检查"上下文不违背"维度，将分析结果中的冲突数量/严重程度转化为该维度的评分（0-1），确保评分影响整体迭代生成流程。

### 1.2 完成状态

| 任务项 | 状态 | 说明 |
|--------|------|------|
| 阅读质量验证器代码 | ✅ 完成 | 理解6维度评分逻辑和权重配置 |
| 阅读逆向反馈分析器代码 | ✅ 完成 | 理解分析接口和返回格式 |
| 设计集成方案 | ✅ 完成 | 确定评分转化公式和集成位置 |
| 修改质量验证器插件 | ✅ 完成 | 添加上下文一致性评分方法 |
| 测试验证 | ✅ 完成 | 功能测试通过 |

---

## 二、集成方案设计

### 2.1 集成位置

在 `QualityValidatorPlugin` 类中：

1. **`__init__()` 方法**：初始化 `_reverse_feedback_analyzer` 属性为 `None`
2. **`initialize()` 方法**：从 `PluginContext.plugin_registry` 获取逆向反馈分析器引用
3. **`_score_context_consistency()` 方法**：新增方法，调用逆向反馈分析器
4. **`validate()` 方法**：在验证流程中调用上下文一致性评分

### 2.2 评分转化公式

将冲突数量和严重程度转化为 0-1 评分：

```
基础分 = 1.0（无冲突）
惩罚值 = 高优先级冲突数 × 0.20
        + 中优先级冲突数 × 0.10
        + 低优先级冲突数 × 0.05

最终评分 = max(0.4, 基础分 - 惩罚值)
```

**评分示例**：
- 无冲突：1.0
- 1个低优先级：0.95
- 1个中优先级：0.90
- 1个高优先级：0.80
- 2个高优先级 + 1个中优先级：1.0 - 0.40 - 0.10 = 0.50
- 最低分保底：0.4

### 2.3 影响整体评分的方式

上下文一致性评分作为额外加权因子影响总分：

```python
# 如果上下文一致性评分较低，会降低总分
if context_consistency < 0.7:
    # 应用惩罚因子：评分作为乘数
    penalty_factor = max(0.7, context_consistency)
    total_score = total_score * penalty_factor
```

**影响示例**：
- 原始总分 0.85，上下文一致性 0.60 → 最终总分 0.85 × 0.60 = 0.51
- 原始总分 0.85，上下文一致性 0.80 → 无惩罚，最终总分 0.85
- 原始总分 0.85，上下文一致性 0.40 → 最终总分 0.85 × 0.70 = 0.595（保底0.7）

---

## 三、代码修改详情

### 3.1 版本号更新

```python
PLUGIN_VERSION = "1.1.0"  # 从 1.0.0 升级
```

### 3.2 `__init__()` 方法修改

```python
def __init__(self):
    # ... 原有代码 ...
    
    # 逆向反馈分析器引用（在initialize中设置）
    self._reverse_feedback_analyzer = None
    
    # ... 后续代码 ...
```

### 3.3 `initialize()` 方法修改

```python
def initialize(self, context: PluginContext) -> bool:
    if not super().initialize(context):
        return False

    # 获取逆向反馈分析器插件引用
    self._reverse_feedback_analyzer = None
    try:
        if hasattr(context, 'plugin_registry') and context.plugin_registry:
            self._reverse_feedback_analyzer = context.plugin_registry.get_plugin("reverse-feedback-analyzer")
            if self._reverse_feedback_analyzer:
                self._logger.info("[质量验证器] 成功获取逆向反馈分析器插件引用")
            else:
                self._logger.warning("[质量验证器] 逆向反馈分析器插件未找到，上下文一致性检查将跳过")
    except Exception as e:
        self._logger.warning(f"[质量验证器] 获取逆向反馈分析器插件失败: {e}")

    self._logger.info(f"[{self.PLUGIN_ID}] 插件初始化成功")
    return True
```

### 3.4 新增 `_score_context_consistency()` 方法

```python
def _score_context_consistency(
    self,
    text: str,
    context: Dict[str, Any]
) -> Tuple[float, List[str]]:
    """上下文一致性评分（集成逆向反馈分析器）

    调用逆向反馈分析器检查章节与设定的一致性，将冲突数量和严重程度转化为评分。

    评分公式：
    - 基础分：1.0（无冲突）
    - 每个低优先级冲突：-0.05
    - 每个中优先级冲突：-0.10
    - 每个高优先级冲突：-0.20
    - 最低分：0.4（保底）

    Args:
        text: 章节文本内容
        context: 验证上下文，需包含：
            - chapter_id: 章节ID
            - chapter_outline: 章节大纲
            - character_profiles: 人物设定列表
            - world_view: 世界观设定
            - project_name: 项目名称

    Returns:
        Tuple[float, List[str]]: (评分, 问题列表)
    """
    # 如果逆向反馈分析器不可用，返回默认评分
    if not self._reverse_feedback_analyzer:
        self._logger.debug("逆向反馈分析器不可用，跳过上下文一致性检查")
        return 0.75, ["逆向反馈分析器未启用"]

    try:
        # 准备逆向反馈分析器所需的参数
        chapter_id = context.get('chapter_id', 'unknown')

        current_settings = {
            "project_name": context.get('project_name', ''),
            "chapter_title": context.get('chapter_title', ''),
            "outline": context.get('chapter_outline', ''),
            "characters": context.get('character_profiles', []),
            "worldview": context.get('world_view', '')
        }

        # 调用逆向反馈分析器
        self._logger.info(f"调用逆向反馈分析器检查章节一致性: {chapter_id}")
        report = self._reverse_feedback_analyzer.analyze_chapter_vs_settings(
            chapter_text=text,
            current_settings=current_settings,
            chapter_id=chapter_id
        )

        # 提取冲突信息
        issues = report.issues
        high_count = report.high_priority_count
        medium_count = report.medium_priority_count
        low_count = report.low_priority_count

        self._logger.info(
            f"逆向反馈分析完成: 共{len(issues)}个冲突 "
            f"(高:{high_count}, 中:{medium_count}, 低:{low_count})"
        )

        # 计算评分
        base_score = 1.0
        penalty = (
            high_count * 0.20 +    # 高优先级扣分
            medium_count * 0.10 +   # 中优先级扣分
            low_count * 0.05        # 低优先级扣分
        )

        score = max(0.4, base_score - penalty)

        # 生成问题列表
        problem_list = []
        for issue in issues[:5]:  # 只取前5个问题
            severity_map = {
                "high": "【高】",
                "medium": "【中】",
                "low": "【低】"
            }
            severity_label = severity_map.get(issue.severity.value, "【未知】")
            problem_list.append(f"{severity_label}{issue.description}")

        return score, problem_list

    except Exception as e:
        self._logger.error(f"逆向反馈分析失败: {e}")
        return 0.7, [f"逆向反馈分析异常: {str(e)}"]
```

### 3.5 `validate()` 方法修改

在原有6维度评分后，添加上下文一致性评分：

```python
# 7. 上下文一致性评分（集成逆向反馈分析器）
context_consistency, context_issues = self._score_context_consistency(content, context)

# 如果存在高优先级冲突，记录警告
if context_consistency < 0.6:
    self._logger.warning(f"上下文一致性评分较低: {context_consistency:.2f}")
    for issue in context_issues:
        self._logger.warning(f"  - {issue}")

# ... 计算加权总分 ...

# 上下文一致性作为额外加权因子（影响总分但不参与权重计算）
if context_consistency < 0.7:
    # 应用惩罚因子：每低于0.1扣减5%的总分
    penalty_factor = max(0.7, context_consistency)
    total_score = total_score * penalty_factor
    self._logger.info(f"上下文一致性惩罚因子: {penalty_factor:.2f}")

self._logger.info(f"验证完成: 总分={total_score:.2f}, 通过={passed}, 上下文一致性={context_consistency:.2f}")
```

---

## 四、测试验证

### 4.1 测试场景

#### 场景1: 插件元数据验证

**测试内容**：确认版本号已更新为 1.1.0

**结果**：✅ 通过

```
Plugin ID: quality-validator-v1
Plugin Version: 1.1.0
Has _score_context_consistency: True
```

#### 场景2: 无逆向反馈分析器时的默认行为

**测试内容**：在逆向反馈分析器不可用时，返回默认评分

**结果**：✅ 通过

```
Context consistency score: 0.75
Issues: ['逆向反馈分析器未启用']
```

#### 场景3: 完整验证流程

**测试内容**：执行完整的6维度评分 + 上下文一致性评分

**结果**：✅ 通过

```
Total score: 0.7925
Word count: 1.0
Outline: 1.0
Style: 0.67
Character: 0.5
Worldview: 1.0
Naturalness: 1.0
Has chapter end: True
```

---

## 五、依赖关系

### 5.1 插件依赖

| 插件 | 类型 | 说明 |
|------|------|------|
| `reverse-feedback-analyzer` | 可选 | 逆向反馈分析器，用于上下文一致性检查 |

**降级处理**：如果逆向反馈分析器不可用，返回默认评分 0.75，不影响其他评分维度。

### 5.2 数据依赖

`validate()` 方法的 `context` 参数需要以下额外字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `chapter_id` | str | 章节ID（用于逆向反馈分析） |
| `project_name` | str | 项目名称 |
| `chapter_title` | str | 章节标题 |

---

## 六、性能考虑

### 6.1 缓存机制

逆向反馈分析器内置缓存机制，相同章节的重复分析会直接返回缓存结果，避免重复调用LLM。

### 6.2 异步影响

上下文一致性评分是同步执行的，如果逆向反馈分析器调用LLM进行深度分析，可能会增加验证耗时（约3-5秒）。

**优化建议**：
- 在迭代生成流程中，可以考虑异步执行上下文一致性检查
- 对于快速验证场景，可以禁用深度分析（`deep_analysis=False`）

---

## 七、错误处理

### 7.1 异常场景

| 异常场景 | 处理策略 | 日志级别 |
|----------|----------|----------|
| 逆向反馈分析器未找到 | 返回默认评分 0.75 | WARNING |
| LLM调用超时 | 返回默认评分 0.70 | ERROR |
| 逆向反馈分析异常 | 返回默认评分 0.70，记录异常信息 | ERROR |

### 7.2 降级策略

```
逆向反馈分析器不可用
    ↓
返回默认评分 0.75
    ↓
记录问题列表：["逆向反馈分析器未启用"]
    ↓
继续执行其他评分维度
```

---

## 八、已知限制

### 8.1 功能限制

1. **依赖插件状态**：逆向反馈分析器需要正确初始化才能使用
2. **同步执行**：当前实现是同步的，可能增加验证耗时
3. **评分权重**：上下文一致性作为额外因子，不参与原有6维度权重计算

### 8.2 性能限制

1. **LLM调用**：首次分析需要调用LLM，耗时约3-5秒
2. **内存占用**：逆向反馈分析器会缓存分析结果

---

## 九、后续优化方向

### 9.1 功能优化

1. **异步执行**：将上下文一致性检查改为异步执行
2. **配置化**：允许用户配置是否启用上下文一致性检查
3. **权重集成**：将上下文一致性作为第7个评分维度，参与权重计算

### 9.2 性能优化

1. **并行执行**：在验证流程中并行执行多个评分维度
2. **增量分析**：只分析新增或修改的内容
3. **预测性分析**：在生成过程中实时进行一致性检查

---

## 十、总结

### 10.1 完成情况

✅ **集成完成**：质量验证器已成功集成逆向反馈分析器

✅ **功能验证**：
- 版本号更新为 1.1.0
- 新增 `_score_context_consistency()` 方法
- 在 `validate()` 中调用上下文一致性评分
- 评分影响整体迭代生成流程

✅ **测试通过**：
- 插件元数据验证
- 默认行为测试
- 完整验证流程测试

### 10.2 代码统计

| 修改内容 | 行数 | 说明 |
|----------|------|------|
| 版本号更新 | 1 | PLUGIN_VERSION = "1.1.0" |
| `__init__` 修改 | 3 | 添加 `_reverse_feedback_analyzer` 属性 |
| `initialize` 新增 | 15 | 获取逆向反馈分析器引用 |
| `_score_context_consistency` 新增 | 70 | 上下文一致性评分方法 |
| `validate` 修改 | 20 | 调用上下文一致性评分 |
| **总计** | **109** | - |

### 10.3 技术亮点

1. **非侵入式集成**：逆向反馈分析器作为可选依赖，不影响原有功能
2. **智能评分转化**：将冲突数量/严重程度合理转化为评分
3. **降级策略完善**：分析器不可用时返回默认评分，保证系统稳定性
4. **日志完善**：详细记录分析过程和结果，便于调试

---

## 十一、参考文档

1. [逆向反馈分析器插件实现说明](./9.4.2.2实现逆向反馈分析器插件说明✅️.md)
2. [验证器与工具插件迁移说明](./6.1%20验证器与工具插件✅️.md)
3. [项目总体架构设计说明书V1.2](./1.1项目总体架构设计说明书修订执行版✅️.md)
4. [插件接口定义V2.1](./2.2%20插件接口定义✅️.md)

---

**文档结束**
