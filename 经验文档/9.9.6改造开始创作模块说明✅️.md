# 9.9.6 改造开始创作模块说明

> **版本**: V1.0
> **创建日期**: 2026-03-24
> **任务来源**: 9.9大模型集成.txt
> **改造目标**: 将生成流水线中所有AI调用替换为AIServiceManager统一调用

---

## 一、改造范围

### 1.1 涉及模块

根据《9.9大模型集成.txt》要求，需要改造三个核心功能模块：

| 序号 | 功能模块 | 插件位置 | 改造状态 |
|------|---------|----------|---------|
| 1 | 开始创作（迭代生成） | `plugins/iterative-generator-v2/plugin.py` | ✅ 已完成 |
| 2 | 快捷创作 | `plugins/quick-creator-v1/plugin.py` | ✅ 已完成 |
| 3 | 续写功能 | `plugins/continuation-generator-v1/plugin.py` | ✅ 已完成 |

### 1.2 改造文件清单

```
plugins/
├── iterative-generator-v2/
│   └── plugin.py          # 迭代生成插件（1处改造）
├── quick-creator-v1/
│   └── plugin.py          # 快捷创作插件（1处改造）
└── continuation-generator-v1/
    └── plugin.py          # 续写插件（2处改造）
```

---

## 二、改造方案

### 2.1 改造前（直接调用OpenAI API）

```python
# 旧代码
response = self._api_client.chat.completions.create(
    model=self.model_name,
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": prompt}
    ],
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=120
)
content = response.choices[0].message.content
```

**问题**：
- 直接依赖OpenAI SDK
- 硬编码模型名称
- 无法动态切换本地/线上模型
- 需要手动管理API客户端

### 2.2 改造后（使用AIServiceManager）

```python
# 新代码
from core.ai_service_manager import get_ai_service_manager
from core.ai_provider import GenerationConfig

ai_manager = get_ai_service_manager()

# 构建配置
config = GenerationConfig(
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=120
)

# 调用生成（支持messages格式）
result = ai_manager.generate_text(
    prompt=prompt,
    config=config,
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": prompt}
    ]
)

content = result.text
```

**优势**：
- 统一调用接口
- 自动根据配置选择Provider
- 支持本地/线上动态切换
- 配置变更实时生效
- 内置容错和降级机制

---

## 三、详细改造记录

### 3.1 迭代生成插件（iterative-generator-v2）

**文件**: `plugins/iterative-generator-v2/plugin.py`

**改造位置**: 第628行

**改造内容**:
```python
# 改造前
response = self._api_client.chat.completions.create(
    model=self.model_name,
    messages=[...],
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=120
)
content = response.choices[0].message.content

# 改造后
from core.ai_service_manager import get_ai_service_manager
from core.ai_provider import GenerationConfig

ai_manager = get_ai_service_manager()
config = GenerationConfig(
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=120
)
result = ai_manager.generate_text(
    prompt=prompt,
    config=config,
    messages=[...]
)
content = result.text
```

**影响范围**:
- `_generate_content()` 方法
- 迭代生成流程的核心调用

---

### 3.2 快捷创作插件（quick-creator-v1）

**文件**: `plugins/quick-creator-v1/plugin.py`

**改造位置**: 第510行

**改造内容**:
```python
# 改造前
response = self.api_client.chat.completions.create(
    model=self.config.get("model", "deepseek-chat"),
    messages=[...],
    temperature=temperature,
    max_tokens=max_tokens,
    stream=False
)
return response.choices[0].message.content

# 改造后
from core.ai_service_manager import get_ai_service_manager
from core.ai_provider import GenerationConfig

ai_manager = get_ai_service_manager()
config = GenerationConfig(
    temperature=temperature,
    max_tokens=max_tokens
)
result = ai_manager.generate_text(
    prompt=user_prompt,
    config=config,
    messages=[...]
)
return result.text
```

**影响范围**:
- `_call_llm()` 方法
- 世界观/大纲/人物/情节生成功能

---

### 3.3 续写插件（continuation-generator-v1）

**文件**: `plugins/continuation-generator-v1/plugin.py`

**改造位置**: 第1378行 和 第2030行

#### 第一处改造（流式生成）

```python
# 改造前（流式API调用）
response = self._api_client.chat.completions.create(
    model=self.default_model,
    messages=[...],
    temperature=request.temperature or self.default_temperature,
    max_tokens=max_tokens,
    stream=True,
    timeout=self._timeout
)
for chunk in response:
    if chunk.choices and chunk.choices[0].delta.content:
        content = chunk.choices[0].delta.content
        full_text += content
        if callback:
            callback(content)
        yield content

# 改造后
from core.ai_service_manager import get_ai_service_manager
from core.ai_provider import GenerationConfig

ai_manager = get_ai_service_manager()
config = GenerationConfig(
    temperature=request.temperature or self.default_temperature,
    max_tokens=max_tokens,
    timeout=self._timeout
)
result = ai_manager.generate_text_stream(
    prompt=prompt,
    callback=callback,
    config=config,
    messages=[...]
)
full_text = result.text
```

#### 第二处改造（普通生成）

```python
# 改造前
response = self._api_client.chat.completions.create(
    model=self.default_model,
    messages=[...],
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=self._timeout
)
return response.choices[0].message.content

# 改造后
from core.ai_service_manager import get_ai_service_manager
from core.ai_provider import GenerationConfig

ai_manager = get_ai_service_manager()
config = GenerationConfig(
    temperature=temperature,
    max_tokens=max_tokens,
    timeout=self._timeout
)
result = ai_manager.generate_text(
    prompt=prompt,
    config=config,
    messages=[...]
)
return result.text
```

**影响范围**:
- `stream_continuation()` 方法（流式生成）
- `_call_llm()` 方法（普通生成）
- 续写功能的核心调用

---

## 四、配置适配

### 4.1 配置结构（config.yaml）

```yaml
# AI服务配置
service_mode: local          # online 或 local
provider: Qwen               # DeepSeek / OpenAI / Qwen
model: Qwen2.5-14B-GPTQ-Int4
api_key: ''                  # 线上API密钥
local_url: http://localhost:11434/v1
temperature: 0.7

# 本地AI配置（当service_mode=local时使用）
local:
  model_path: F:/Qwen
  max_memory:
    0: 7GB
    cpu: 16GB
```

### 4.2 动态切换流程

```
用户修改配置 
  → 设置界面保存
  → ConfigService.update_ai_config()
  → 发布 config.changed 事件
  → AIServiceManager 监听事件
  → 重新创建 Provider
  → 后续调用使用新 Provider
```

---

## 五、测试验证

### 5.1 单元测试

**测试范围**:
- AIServiceManager获取Provider
- 生成文本接口调用
- 流式生成接口调用
- 配置切换后Provider重建

**测试方法**:
```python
from core.ai_service_manager import get_ai_service_manager

ai_manager = get_ai_service_manager()

# 测试生成
result = ai_manager.generate_text("你好")
print(result.text)

# 测试健康检查
health = ai_manager.health_check()
print(health)
```

### 5.2 集成测试

**测试场景**:
1. **线上模式**: 设置service_mode=online，验证调用DeepSeek API
2. **本地模式**: 设置service_mode=local，验证调用Qwen本地模型
3. **动态切换**: 运行时切换配置，验证立即生效
4. **错误处理**: 故意填写错误配置，验证异常处理

### 5.3 验证结果

| 测试项 | 状态 | 说明 |
|-------|------|------|
| 导入检查 | ✅ 通过 | AIServiceManager导入正常 |
| 配置检查 | ✅ 通过 | config.yaml配置正确 |
| 本地模型加载 | ✅ 通过 | Qwen模型加载成功 |
| 生成测试 | ✅ 通过 | 生成文本正常返回 |
| 上下文生成 | ✅ 通过 | 支持messages格式 |
| 健康检查 | ✅ 通过 | health_check()正常 |

---

## 六、注意事项

### 6.1 线程安全

- AIServiceManager是单例，线程安全
- Provider创建和切换使用锁保护
- 并发调用时共享同一个Provider实例

### 6.2 错误处理

- Provider不可用时抛出AIProviderUnavailableError
- 生成失败时抛出AIProviderError
- 需要在业务代码中捕获异常并提示用户

### 6.3 性能影响

- Provider创建耗时约15-20秒（首次加载模型）
- 后续调用直接使用缓存的Provider
- 配置切换时重建Provider（约15-20秒）

### 6.4 兼容性

- 保留`set_api_client()`方法作为兼容接口
- 插件不再直接使用api_client
- 改为从AIServiceManager获取Provider

---

## 七、后续工作

### 7.1 已完成
- ✅ AIServiceManager设计与实现
- ✅ OnlineProvider实现（DeepSeek/OpenAI）
- ✅ LocalProvider实现（Qwen本地模型）
- ✅ 配置文件更新
- ✅ 迭代生成插件改造
- ✅ 快捷创作插件改造
- ✅ 续写插件改造

### 7.2 待完成
- ⏳ UI设置界面集成（实时保存配置）
- ⏳ 完整集成测试（三个功能端到端测试）
- ⏳ 性能监控与日志优化
- ⏳ 用户文档更新

---

## 八、参考文档

- 《9.9大模型集成.txt》- 总体架构设计
- 《9.9.1设计AIProvider接口与AIServiceManager说明✅️.md》- 接口设计
- 《9.9.2实现OnlineProvider说明✅️.md》- 线上Provider实现
- 《core/local_provider.py》- 本地Provider实现
- 《config.yaml》- 配置结构

---

**维护者**: AI工程师
**最后更新**: 2026-03-24
