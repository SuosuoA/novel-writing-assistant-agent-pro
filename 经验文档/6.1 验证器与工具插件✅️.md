# 验证器与工具插件迁移说明

## 1. 概述

本文档记录 V5核心模块 `enhanced_weighted_validator.py` 和 `hot_ranking/` 向Agent Pro插件系统的迁移过程。

## 2. 质量验证器插件 (quality-validator-v1)
### 2.1 源文件: `scripts/enhanced_weighted_validator.py` 和 `scripts/hot_ranking/` 向Agent Pro插件系统的迁移过程。

迁移时间: 2026-03-23

## 2. 质量验证器插件(quality-validator-v1)

### 2.1 插件信息
- **插件ID**: `quality-validator-v1`
- **插件名称**: 质量验证器 V1
- **插件类型**: `ValidatorPlugin`
- **源文件**: `scripts/enhanced_weighted_validator.py`
- **目标路径**: `plugins/quality-validator-v1/`
- **功能**: 6维度加权评分系统

- **接口实现**: 
  - `ValidatorPlugin` 崩件 - `validate(content, context)` - 核心验证方法
  - `validate_with_weights(...)` - 兼容V5接口
  - `get_validation_dimensions()` - 获取验证维度

- **依赖项**: jieba（可选)、requests、 BeautifulSoup

- **强制保护规则**:
  1. 章节结束必须添加【本章完】标记
  2. 评分阈值 >= 0.8 才能输出
  3. 迭代上限 5 次
  4. 6维度评分权重固定
  5. 世界观严重违背一票否决

- **文件结构**:
  ```
plugins/quality-validator-v1/
├── plugin.json        # 插件清单
├── __init__.py             # 入口文件
└── plugin.py              # 核心实现
```

### 2.2 核心功能

#### 2.2.1 评分权重配置
```python
WEIGHTS = {
    'word_count': 0.10,  # 字数（10%）
    'outline': 0.15,    # 大纲（15%）
    'style': 0.25,      # 风格（25%）
    'character': 0.25,  # 人设（25%）
    'worldview': 0.20,  # 世界观（20%）
    'naturalness': 0.05   # 自然度（5%）
```

权重总和 = 1.0

#### 2.2.2 验证维度
| 维度 | 权重 | 功能 |
|------|------|------|
| 字数符合性 | 10% | 检查字数是否在目标范围内 |
| 大纲符合性 | 15% | 检查大纲情节点是否被覆盖 |
| 风格一致性 | 25% | 检查风格档案匹配度 |
| 人设一致性 | 25% | 检查人物行为是否符合设定 |
| 世界观一致性 | 20% | 检查世界观设定是否被违背（一票否决） |
| 自然度 | 5% | 检测AI生成痕迹 |

#### 2.2.3 核心规则
1. **章节结束标记**: 必须包含`【本章完】`，2. **评分阈值**: 总分 >= 0.8 + 包含结束标记 = 输出
3. **迭代上限**: 最多5次重试
4. **世界观一票否决**: 严重违背时返回0分，5. **降级数据**: 网络故障时使用默认数据

### 2.3 热榜工具插件(hot-ranking-v1)
### 2.3.1 插件信息
- **插件ID**: `hot-ranking-v1`
- **插件名称**: 热榜工具 V1
- **插件类型**: `ToolPlugin`
- **源文件**: `scripts/hot_ranking/` 目录下所有文件
- **目标路径**: `plugins/hot-ranking-v1/`
- **功能**: 小说网站排行榜数据爬取和管理
- **接口实现**:
  - `ToolPlugin` 勒
  - `execute(action, params)` - 工具操作入口
  - `refresh_async(callback)` - 异步刷新
  - `get_ranking_data()` - 获取排行榜数据
  - `get_cache_info()` - 缓存信息
  - `clear_cache()` - 清除缓存
- **依赖项**: requests, BeautifulSoup, concurrent.futures
- **强制保护规则**:
  1. 禁止破坏性修改爬取逻辑
  2. 保留真实数据标记(is_real=True)
  3. 保留降级数据机制
  4. 保留并发爬取机制
- **文件结构**:
  ```
plugins/hot-ranking-v1/
├── plugin.json        # 插件清单
├── __init__.py             # 入口文件
└── plugin.py              # 核心实现
```

### 2.4 核心功能
#### 2.4.1 支持的网站
| 网站 | URL | 离线数据 |
|------|-----|------|
| 红番茄小说 | fanqienovel.com/rank | ✅ (默认数据) |
| 起点中文网 | m.qidian.com/rank/ | ✅ (默认数据) |
| 晋江文学城 | jjwxc.net/topten.php | ✅ (默认数据) |
#### 2.4.2 缓存策略
- **缓存有效期**: 10分钟
- **缓存位置**: `热榜数据/` 目录
- **降级机制**: 网络故障时使用内置默认数据
#### 2.4.3 数据处理流程
1. **爬取数据** → 并发爬取三个网站
2. **归一化热度** → 统一到0-100评分体系
3. **生成聚合数据** → 题材榜/类型榜/作家榜
4. **缓存管理** → 保存/加载/清理旧缓存

#### 2.4.4 异步刷新机制
```python
def refresh_async(self, callback: Callable[[Dict], None], force_update: bool = True):
    """异步刷新排行榜数据"""
    thread = threading.Thread(target=self._update_task, daemon=True)
    thread.start()
```
### 2.5 测试结果
### 2.5.1 质量验证器插件测试
运行测试脚本: `tests/test_plugins_v1.py`

**测试结果**:
- ✅ 验证器插件导入成功
- ✅ 热榜插件导入成功
- 所有测试通过

### 2.6 注意事项
1. **强制保护规则**: 两个插件均遵循V5强制保护机制（见记忆库）
2. **测试文件**: 测试完成后删除
3. **降级处理**: 网络不可用时使用默认数据

---

## 3. 插件UI适配优化（2026-03-23）

### 3.1 优化背景

根据《UI搭建说明V2.3》和《插件接口定义V2.1》对插件进行适配优化，确保插件与UI层正确交互。

### 3.2 热榜插件优化

#### 3.2.1 事件发布机制

根据UI搭建说明，热榜页面需要监听数据更新事件来刷新显示。优化后的热榜插件发布以下事件：

| 事件类型 | 说明 | 数据格式 |
|----------|------|----------|
| `hot_ranking.updated` | 数据更新完成 | `{data, timestamp, source}` |
| `hot_ranking.progress` | 进度更新 | `{progress, status, timestamp, source}` |
| `hot_ranking.error` | 错误事件 | `{error, timestamp, source}` |

#### 3.2.2 UI层订阅示例

```python
# 在UI初始化时订阅事件
def _setup_hot_ranking_listener(self):
    """设置热榜数据监听器"""
    if self._event_bus:
        self._event_bus.subscribe(
            "hot_ranking.updated",
            self._on_hot_ranking_updated,
            handler_id="ui.hot_ranking_listener"
        )

def _on_hot_ranking_updated(self, event):
    """热榜数据更新回调
    
    注意：此回调在后台线程执行，UI更新必须通过 root.after() 调度到主线程
    """
    data = event.data.get("data", {})
    
    # 线程安全：调度到主线程更新UI
    self.root.after(0, lambda: self._update_ranking_display(data))

def _update_ranking_display(self, data):
    """更新热榜显示（主线程执行）"""
    # 在这里更新Treeview等UI组件
    pass
```

#### 3.2.3 异步刷新机制

```python
def refresh_async(self, callback: Callable[[Dict], None] = None, force_update: bool = True):
    """异步刷新数据
    
    根据UI搭建说明，热榜页面需要监听数据更新事件来刷新显示。
    此方法在数据更新完成后会发布 'hot_ranking.updated' 事件。
    
    注意：UI层需要通过 root.after(0, callback) 调度回调到主线程。
    """
    if self._is_updating:
        return

    def update_task():
        self._is_updating = True
        try:
            data = self._refresh_rankings(force_update)
            
            # 发布数据更新事件（供UI监听）
            if self._context and hasattr(self._context, 'event_bus'):
                try:
                    self._context.event_bus.publish(
                        "hot_ranking.updated",
                        {
                            "data": data,
                            "timestamp": time.strftime('%Y-%m-%d %H:%M:%S'),
                            "source": self.PLUGIN_ID
                        }
                    )
                except Exception as e:
                    self._logger.warning(f"发布事件失败: {e}")
            
            if callback:
                callback(data)
        finally:
            self._is_updating = False

    thread = threading.Thread(target=update_task, daemon=True)
    thread.start()
```

### 3.3 所有插件接口规范检查结果

#### 3.3.1 AnalyzerPlugin（分析器插件）

| 插件ID | 接口实现 | 符合性 | 说明 |
|--------|----------|--------|------|
| outline-parser-v3 | ✅ | ✅ | 正确实现 analyze(), get_supported_formats(), get_analysis_types() |
| style-learner-v5 | ✅ | ✅ | 正确实现 analyze(), get_supported_formats(), get_analysis_types() |
| character-manager-v1 | ✅ | ✅ | 正确实现 analyze(), get_supported_formats(), get_analysis_types() |
| worldview-parser-v1 | ✅ | ✅ | 正确实现 analyze(), get_supported_formats(), get_analysis_types() |

#### 3.3.2 GeneratorPlugin（生成器插件）

| 插件ID | 接口实现 | 符合性 | 说明 |
|--------|----------|--------|------|
| context-builder-v1 | ✅ | ✅ | 正确实现 generate(), validate_request(), get_generation_options() |
| iterative-generator-v2 | ✅ | ✅ | 正确实现 generate(), validate_request(), get_generation_options() |
| novel-generator-v3 | ✅ | ✅ | 正确实现 generate(), validate_request(), get_generation_options() |

#### 3.3.3 ValidatorPlugin（验证器插件）

| 插件ID | 接口实现 | 符合性 | 说明 |
|--------|----------|--------|------|
| quality-validator-v1 | ✅ | ✅ | 正确实现 validate(), get_validation_dimensions() |

#### 3.3.4 ToolPlugin（工具插件）

| 插件ID | 接口实现 | 符合性 | 说明 |
|--------|----------|--------|------|
| hot-ranking-v1 | ✅ | ✅ | 正确实现 execute()，新增事件发布机制 |

### 3.4 V5核心模块保护检查结果

所有插件都正确标注了V5强制保护区域：

| 插件ID | 保护规则 | 状态 |
|--------|----------|------|
| context-builder-v1 | 【本章完】强制要求、上下文记忆前5章 | ✅ |
| iterative-generator-v2 | 评分阈值0.8、迭代上限5次、【本章完】检查 | ✅ |
| novel-generator-v3 | 完整流程保护 | ✅ |
| quality-validator-v1 | 6维度评分权重、世界观一票否决 | ✅ |

### 3.5 线程安全原则

根据UI搭建说明V2.3，所有插件回调必须遵循线程安全原则：

1. **UI操作必须在主线程执行**
   - 所有UI更新通过 `root.after(0, callback)` 调度
   - 插件的异步回调不能直接操作UI组件

2. **事件发布机制**
   - 插件发布事件后，UI层订阅者负责调度到主线程
   - 事件总线本身是线程安全的

3. **插件间通信**
   - 通过EventBus发布/订阅事件
   - 避免直接调用其他插件的内部方法

### 3.6 优化完成时间

- 优化日期：2026-03-23
- 优化人员：高级开发工程师
- 审核状态：待审核
