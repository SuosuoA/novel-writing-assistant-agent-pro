# 数据库设计规范 - Novel Writing Assistant-Agent Pro

> **版本**: V1.0  
> **更新日期**: 2026-03-26  
> **数据库类型**: SQLite + LanceDB

---

## 一、数据库架构

### 1.1 数据库类型

| 数据库 | 用途 | 特性 |
|--------|------|------|
| SQLite | 关系型数据存储 | 零配置、单文件、WAL模式并发 |
| LanceDB | 向量数据库 | 零配置嵌入式、支持大规模向量检索 |

### 1.2 数据库文件位置

```
项目根目录/
├── data/
│   ├── novel_assistant.db         # SQLite主数据库
│   ├── novel_assistant.db-wal    # WAL日志文件
│   ├── novel_assistant.db-shm    # 共享内存文件
│   ├── vector_store/              # LanceDB向量数据库
│   │   ├── chapters.lance/       # 章节向量表
│   │   ├── knowledge.lance/      # 知识向量表
│   │   └── styles.lance/         # 风格向量表
│   ├── knowledge/                 # 知识库JSON文件
│   │   ├── scifi/
│   │   ├── xuanhuan/
│   │   ├── history/
│   │   └── general/
│   └── chapter_encoding_history.json  # 章节编码历史
```

---

## 二、SQLite数据库设计

### 2.1 配置参数

**WAL模式**：
```python
conn = sqlite3.connect('data/novel_assistant.db')
conn.execute('PRAGMA journal_mode=WAL')      # WAL模式
conn.execute('PRAGMA synchronous=NORMAL')    # 同步模式
conn.execute('PRAGMA cache_size=-64000')     # 64MB缓存
conn.execute('PRAGMA busy_timeout=5000')     # 5秒超时
```

### 2.2 表结构设计

#### 2.2.1 项目表 (projects)

```sql
CREATE TABLE IF NOT EXISTS projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    genre TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata TEXT  -- JSON格式元数据
);

CREATE INDEX idx_projects_path ON projects(path);
CREATE INDEX idx_projects_updated_at ON projects(updated_at);
```

#### 2.2.2 章节表 (chapters)

```sql
CREATE TABLE IF NOT EXISTS chapters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    chapter_number INTEGER NOT NULL,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    word_count INTEGER DEFAULT 0,
    status TEXT DEFAULT 'draft',  -- draft/published/archived
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata TEXT,  -- JSON格式元数据
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_chapters_project_id ON chapters(project_id);
CREATE INDEX idx_chapters_number ON chapters(project_id, chapter_number);
CREATE INDEX idx_chapters_status ON chapters(status);
```

#### 2.2.3 人物表 (characters)

```sql
CREATE TABLE IF NOT EXISTS characters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    role_type TEXT,  -- protagonist/antagonist/supporting
    gender TEXT,
    age TEXT,
    appearance TEXT,
    personality TEXT,
    background TEXT,
    abilities TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata TEXT,
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_characters_project_id ON characters(project_id);
CREATE INDEX idx_characters_name ON characters(project_id, name);
```

#### 2.2.4 大纲表 (outlines)

```sql
CREATE TABLE IF NOT EXISTS outlines (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    chapter_number INTEGER NOT NULL,
    chapter_title TEXT NOT NULL,
    chapter_type TEXT,  -- main/side/extra
    word_count_target INTEGER,
    summary TEXT,
    plot_points TEXT,  -- JSON格式情节要点
    dependencies TEXT,  -- JSON格式依赖章节
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_outlines_project_id ON outlines(project_id);
CREATE INDEX idx_outlines_number ON outlines(project_id, chapter_number);
```

#### 2.2.5 世界观表 (worldviews)

```sql
CREATE TABLE IF NOT EXISTS worldviews (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    category TEXT,  -- setting/rule/faction/location
    elements TEXT,  -- JSON格式核心元素
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_worldviews_project_id ON worldviews(project_id);
CREATE INDEX idx_worldviews_category ON worldviews(project_id, category);
```

#### 2.2.6 风格档案表 (style_profiles)

```sql
CREATE TABLE IF NOT EXISTS style_profiles (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    vocabulary_features TEXT,  -- JSON格式词汇特征
    sentence_features TEXT,    -- JSON格式句式特征
    emotion_features TEXT,     -- JSON格式情感特征
    sample_texts TEXT,         -- JSON格式样本文本
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_style_profiles_project_id ON style_profiles(project_id);
```

#### 2.2.7 生成历史表 (generation_history)

```sql
CREATE TABLE IF NOT EXISTS generation_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    chapter_id INTEGER NOT NULL,
    iteration INTEGER DEFAULT 0,
    prompt TEXT NOT NULL,
    content TEXT NOT NULL,
    scores TEXT,  -- JSON格式评分
    model_name TEXT,
    temperature REAL,
    max_tokens INTEGER,
    generation_time REAL,  -- 秒
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (chapter_id) REFERENCES chapters(id)
);

CREATE INDEX idx_generation_history_chapter_id ON generation_history(chapter_id);
CREATE INDEX idx_generation_history_created_at ON generation_history(created_at);
```

#### 2.2.8 用户配置表 (user_settings)

```sql
CREATE TABLE IF NOT EXISTS user_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2.2.9 插件配置表 (plugin_settings)

```sql
CREATE TABLE IF NOT EXISTS plugin_settings (
    plugin_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value TEXT NOT NULL,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (plugin_id, key)
);

CREATE INDEX idx_plugin_settings_plugin_id ON plugin_settings(plugin_id);
```

### 2.3 视图设计

#### 2.3.1 项目概览视图

```sql
CREATE VIEW IF NOT EXISTS v_project_overview AS
SELECT 
    p.id,
    p.name,
    p.genre,
    COUNT(DISTINCT c.id) AS chapter_count,
    COUNT(DISTINCT ch.id) AS character_count,
    COUNT(DISTINCT w.id) AS worldview_count,
    SUM(c.word_count) AS total_words,
    p.updated_at
FROM projects p
LEFT JOIN chapters c ON p.id = c.project_id
LEFT JOIN characters ch ON p.id = ch.project_id
LEFT JOIN worldviews w ON p.id = w.project_id
GROUP BY p.id;
```

#### 2.3.2 章节生成统计视图

```sql
CREATE VIEW IF NOT EXISTS v_chapter_generation_stats AS
SELECT 
    c.id AS chapter_id,
    c.chapter_number,
    c.title,
    c.word_count,
    COUNT(gh.id) AS generation_count,
    AVG(gh.generation_time) AS avg_generation_time,
    MAX(gh.created_at) AS last_generated_at
FROM chapters c
LEFT JOIN generation_history gh ON c.id = gh.chapter_id
GROUP BY c.id;
```

---

## 三、LanceDB向量数据库设计

### 3.1 章节向量表 (chapters)

**Schema定义**：
```python
import pyarrow as pa

schema = pa.schema([
    ("chapter_id", pa.string()),
    ("project_id", pa.string()),
    ("chapter_number", pa.int32()),
    ("title", pa.string()),
    ("content", pa.string()),
    ("word_count", pa.int32()),
    ("embedding", pa.list_(pa.float32(), 384)),  # 向量维度
    ("created_at", pa.string()),
    ("updated_at", pa.string()),
])
```

**索引定义**：
```python
import lancedb

db = lancedb.connect("data/vector_store")
table = db.create_table("chapters", schema=schema)

# 创建向量索引（IVF-PQ）
table.create_index(
    num_partitions=256,
    num_sub_vectors=16
)
```

### 3.2 知识向量表 (knowledge)

**Schema定义**：
```python
schema = pa.schema([
    ("knowledge_id", pa.string()),
    ("category", pa.string()),  # scifi/xuanhuan/history/general
    ("domain", pa.string()),    # physics/chemistry/religion
    ("title", pa.string()),
    ("content", pa.string()),
    ("keywords", pa.list_(pa.string())),
    ("embedding", pa.list_(pa.float32(), 384)),
    ("created_at", pa.string()),
    ("updated_at", pa.string()),
])
```

**索引定义**：
```python
table = db.create_table("knowledge", schema=schema)
table.create_index(
    num_partitions=256,
    num_sub_vectors=16
)
```

### 3.3 风格向量表 (styles)

**Schema定义**：
```python
schema = pa.schema([
    ("style_id", pa.string()),
    ("style_name", pa.string()),
    ("style_category", pa.string()),  # classical/modern/webnovel
    ("author", pa.string()),
    ("content", pa.string()),
    ("style_features", pa.string()),  # JSON格式风格特征
    ("embedding", pa.list_(pa.float32(), 384)),
    ("created_at", pa.string()),
    ("updated_at", pa.string()),
])
```

---

## 四、知识库JSON设计

### 4.1 知识点Schema

**JSON格式**：
```json
{
  "knowledge_id": "scifi-physics-001",
  "category": "scifi",
  "domain": "physics",
  "title": "时间膨胀效应",
  "content": "根据狭义相对论...",
  "keywords": ["相对论", "时间", "光速", "时间膨胀"],
  "created_at": "2026-03-25T10:00:00",
  "updated_at": "2026-03-25T10:00:00",
  "references": ["三体", "星际穿越"],
  "difficulty": "medium",
  "tags": ["物理学", "相对论"],
  "embedding": [0.123, 0.456, ...]  // 可选，向量嵌入
}
```

### 4.2 知识库目录结构

```
data/knowledge/
├── scifi/
│   ├── physics.json      # 科幻-物理学
│   ├── chemistry.json    # 科幻-化学
│   ├── biology.json      # 科幻-生物学
│   └── astronomy.json    # 科幻-天文学
├── xuanhuan/
│   ├── religion.json     # 玄幻-宗教学
│   ├── mythology.json    # 玄幻-神话
│   └── daoism.json       # 玄幻-道教
├── history/
│   ├── events.json       # 历史-历史事件
│   ├── figures.json      # 历史-历史人物
│   └── dynasties.json    # 历史-朝代
└── general/
    ├── philosophy.json   # 通用-哲学
    ├── logic.json        # 通用-逻辑
    └── linguistics.json  # 通用-语言学
```

---

## 五、数据访问层设计

### 5.1 SQLite连接池

**实现**：
```python
import sqlite3
import threading
from queue import Queue

class ConnectionPool:
    def __init__(self, db_path, max_connections=10):
        self.db_path = db_path
        self.max_connections = max_connections
        self._pool = Queue(max_connections)
        self._lock = threading.Lock()
        self._all_connections = []
        
        # 初始化连接池
        for _ in range(max_connections):
            conn = self._create_connection()
            self._pool.put(conn)
    
    def _create_connection(self):
        conn = sqlite3.connect(self.db_path, check_same_thread=False)
        conn.execute('PRAGMA journal_mode=WAL')
        conn.execute('PRAGMA synchronous=NORMAL')
        return conn
    
    def get_connection(self):
        return self._pool.get()
    
    def return_connection(self, conn):
        self._pool.put(conn)
    
    def close_all(self):
        for conn in self._all_connections:
            conn.close()
```

### 5.2 数据库管理器

**接口设计**：
```python
class DatabaseManager:
    def __init__(self, db_path: str):
        self.pool = ConnectionPool(db_path)
    
    def execute_query(self, query: str, params: tuple = None):
        conn = self.pool.get_connection()
        try:
            cursor = conn.cursor()
            if params:
                cursor.execute(query, params)
            else:
                cursor.execute(query)
            return cursor.fetchall()
        finally:
            self.pool.return_connection(conn)
    
    def execute_update(self, query: str, params: tuple = None):
        conn = self.pool.get_connection()
        try:
            cursor = conn.cursor()
            if params:
                cursor.execute(query, params)
            else:
                cursor.execute(query)
            conn.commit()
            return cursor.lastrowid
        finally:
            self.pool.return_connection(conn)
```

---

## 六、数据迁移策略

### 6.1 版本控制

**迁移表**：
```sql
CREATE TABLE IF NOT EXISTS schema_migrations (
    version TEXT PRIMARY KEY,
    applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 6.2 迁移脚本

**示例**：
```python
# migrations/001_initial_schema.py
def up(db):
    db.execute("""
        CREATE TABLE IF NOT EXISTS projects (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            name TEXT NOT NULL,
            path TEXT NOT NULL UNIQUE
        )
    """)

def down(db):
    db.execute("DROP TABLE IF EXISTS projects")
```

---

## 七、备份与恢复

### 7.1 备份策略

**自动备份**：
- SQLite数据库：每日备份到 `backups/sqlite/`
- LanceDB向量：每周备份到 `backups/vector_store/`
- 知识库JSON：每次更新后备份

**备份脚本**：
```python
import shutil
from datetime import datetime

def backup_database():
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    
    # SQLite备份
    shutil.copy(
        "data/novel_assistant.db",
        f"backups/sqlite/novel_assistant_{timestamp}.db"
    )
    
    # LanceDB备份
    shutil.copytree(
        "data/vector_store",
        f"backups/vector_store/vector_store_{timestamp}",
        dirs_exist_ok=True
    )
```

### 7.2 恢复策略

**恢复流程**：
1. 停止应用
2. 复制备份文件到data目录
3. 验证数据完整性
4. 重启应用

---

## 八、性能优化

### 8.1 索引优化

**查询优化**：
```sql
-- 分析查询计划
EXPLAIN QUERY PLAN SELECT * FROM chapters WHERE project_id = 1;

-- 添加复合索引
CREATE INDEX idx_chapters_project_status ON chapters(project_id, status);
```

### 8.2 查询优化

**批量插入**：
```python
# 批量插入（比单条插入快10倍）
cursor.executemany(
    "INSERT INTO chapters (project_id, title, content) VALUES (?, ?, ?)",
    [(1, "第1章", "内容1"), (1, "第2章", "内容2")]
)
conn.commit()
```

### 8.3 向量检索优化

**参数调优**：
```python
# IVF-PQ索引参数
table.create_index(
    num_partitions=256,   # 聚类中心数量
    num_sub_vectors=16    # 子向量数量
)

# 检索参数
results = table.search(query_vector)
    .limit(10)            # 返回结果数量
    .nprobes=20           # 探测的聚类数量
    .refine_factor=10     # 重排因子
    .to_pandas()
```

---

## 九、数据完整性约束

### 9.1 外键约束

```sql
-- 启用外键约束
PRAGMA foreign_keys = ON;

-- 定义外键
CREATE TABLE chapters (
    id INTEGER PRIMARY KEY,
    project_id INTEGER NOT NULL,
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
```

### 9.2 触发器

**自动更新时间戳**：
```sql
CREATE TRIGGER update_chapter_timestamp
AFTER UPDATE ON chapters
FOR EACH ROW
BEGIN
    UPDATE chapters SET updated_at = CURRENT_TIMESTAMP WHERE id = NEW.id;
END;
```

---

## 十、数据安全

### 10.1 数据加密

**敏感数据加密**：
```python
from cryptography.fernet import Fernet

class DataEncryptor:
    def __init__(self, key: bytes):
        self.cipher = Fernet(key)
    
    def encrypt(self, data: str) -> bytes:
        return self.cipher.encrypt(data.encode())
    
    def decrypt(self, encrypted_data: bytes) -> str:
        return self.cipher.decrypt(encrypted_data).decode()
```

### 10.2 数据脱敏

**日志脱敏**：
```python
import re

def sanitize_log(log_message):
    # 移除API密钥
    log_message = re.sub(r'sk-[a-zA-Z0-9]{32}', '***API_KEY***', log_message)
    # 移除邮箱
    log_message = re.sub(r'\b[\w\.-]+@[\w\.-]+\.\w{2,4}\b', '***EMAIL***', log_message)
    return log_message
```

---

**最后更新**: 2026-03-26  
**维护者**: 后端架构师、数据库优化专家
